<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Compose | 稀有猿诉]]></title>
  <link href="https://alexhilton.github.io/blog/categories/compose/atom.xml" rel="self"/>
  <link href="https://alexhilton.github.io/"/>
  <updated>2026-04-06T12:11:25+00:00</updated>
  <id>https://alexhilton.github.io/</id>
  <author>
    <name><![CDATA[Alex Hilton]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[在Compose中用Shader实现透明的粘稠元球效果]]></title>
    <link href="https://alexhilton.github.io/blog/2026/04/06/transparent-metaball/"/>
    <updated>2026-04-06T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/04/06/transparent-metaball</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Compose AGSL Shader: Gooey Outline Metaball Effect with Transparent Background」，原文链接<a href="https://medium.com/@yuriyskul/compose-agsl-shader-gooey-outline-metaball-effect-with-transparent-background-cb8b0e72286c">https://medium.com/@yuriyskul/compose-agsl-shader-gooey-outline-metaball-effect-with-transparent-background-cb8b0e72286c</a>，由Yuriy Skul发布于2024年12月2日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1200/1*sTO1Btb7Di56_kZK9JKJkA.gif" title="auto auto" ></a></p>

<!-- more -->


<p>本文探讨如何通过两个圆形之间的简单交互创建<strong>元球效果</strong>。<strong>元球形状带有边框，而背景保持完全透明</strong>，从而确保底层内容的可见性。元球内部的图标也保持可见。此效果是通过对整个容器应用高级<strong>AGSL 着色器</strong>，并结合对每个交互元素应用模糊效果来实现的。</p>

<p>创建带有轮廓效果的透明边框元球背景的概念可以参考这篇文章：在 Jetpack Compose 中使用模糊和 AGSL 运行时渲染效果链创建带有轮廓描边边框的透明元球背景。</p>

<p>圆形按钮的 Gooey Metaball 特效：</p>

<ul>
<li><p><strong>第一部分</strong>：<a href="/@yuriyskul/gooey-metaball-effects-on-android-with-jetpack-compose-blurring-and-alpha-filtering-concepts-63f1cf879257">使用 Jetpack Compose 在 Android 上实现 Gooey（Metaball）特效：跨所有 API 级别的模糊和 Alpha 过滤概念。</a></p></li>
<li><p><strong>第二部分</strong>：<a href="/@yuriyskul/compose-gooey-metaball-button-effect-fixing-blur-mask-issues-on-pre-android-10-devices-with-d8f3fadc2680">使用渐变光晕和图标简化修复 Android 10 之前设备上的模糊蒙版问题。</a></p></li>
<li><p><strong>第三部分</strong>：<a href="/@yuriyskul/jetpack-compose-gooey-metaball-interaction-using-agsl-shader-and-blur-fixing-coloring-issues-c70affdcde0f">Jetpack Compose：使用 AGSL 着色器和模糊效果实现 Gooey（元球）交互——修复颜色问题。</a></p></li>
<li><p><strong>第四部分</strong>：当前状态。</p></li>
</ul>


<p>需求：</p>

<ul>
<li><p><strong>不透明边框</strong>：元球形状应具有特定颜色的不透明边框，例如<strong>黑色</strong>。</p></li>
<li><p><strong>透明内背景</strong>：元球的内背景应完全透明，以确保整个区域的背景完全不受影响且可见。</p></li>
<li><p><strong>可见按钮图标</strong>：元球内部的图标必须保持完全<strong>不透明</strong>，并具有定义的颜色且可见。</p></li>
<li><p><strong>处理折叠后的元球</strong>：当元球折叠成一个点时，应仅保留<strong>最顶部的图标</strong>可见，以确保折叠状态下的清晰度。</p></li>
</ul>


<p>让我们从<a href="/@yuriyskul/gooey-metaball-effects-on-android-with-jetpack-compose-blurring-and-alpha-filtering-concepts-63f1cf879257"><strong>第一部分</strong></a>中的第一个AGSL基础示例的代码开始。在这个示例中，我们将<strong>AGSL着色器效果</strong>应用于父级<strong>MetaballBox</strong>，并为每个圆形按钮添加了模糊效果：</p>

<p>正如我在<a href="/@yuriyskul/chaining-blur-and-agsl-runtime-rendereffects-to-create-transparent-metaball-backgrounds-with-15680ad1e1a9"><strong>创建透明边框metaball背景的概念</strong></a>中所述，为metaball创建边框的思路是在AGSL脚本中使用<strong>阈值透明度范围</strong>进行过滤。透明度值落在指定范围内的像素将被设置为<strong>完全不透明颜色</strong>，而所有其他像素则被设置为<strong>完全透明</strong>，从而形成清晰锐利的边界效果。</p>

<p>这并非本文的主要关注点。然而，由于元球形状内部包含不透明图标，因此会出现一些问题。为了解决这些问题，本文将探讨一些变通方法和技巧，以满足上述要求。</p>

<h2>AGSL：颜色测试探究</h2>

<p>让我们运行测试着色器，并根据每个点的输入<strong>alpha</strong>值为其分配特定颜色。着色器逻辑应用以下规则：</p>

<ul>
<li><strong>如果 alpha = 0.0</strong>：</li>
</ul>


<p>该点<strong>完全透明</strong>，为了便于调试，被标记为<strong>黄色</strong>。</p>

<ul>
<li><strong>如果 alpha &lt; cutoff_min</strong>：</li>
</ul>


<p>该点位于元球的<strong>内部区域</strong>，并被标记为<strong>绿色</strong>以示区别。</p>

<ul>
<li><strong>如果 alpha 介于 cutoff_min 和 cutoff_max 之间</strong>：</li>
</ul>


<p>该点位于<strong>边界区域</strong>，并被标记为<strong>黑色</strong>，以标记过渡区域。</p>

<ul>
<li><strong>如果 alpha > cutoff_max 但小于 1.0</strong>：</li>
</ul>


<p>该点位于<strong>外部混合区域</strong>，透明度开始逐渐减弱。它被标记为<strong>蓝色</strong>以表示此范围。</p>

<ul>
<li><strong>如果 alpha = 1.0</strong>：</li>
</ul>


<p>该点<strong>完全不透明</strong>，并以<strong>红色</strong>标记其最大不透明度。</p>

<pre><code class="kotlin">cutoff_min = 0.5f  
cutoff_max = 0.6f

@Language("AGSL")  
const val ShaderSource_outlined_color_test_1 = """  
    uniform shader composable;  

    uniform float cutoff_min; // Minimum cutoff value for transparency  
    uniform float border_thickness; // Thickness of the border  
    uniform float3 rgbColor;  // Custom RGB color (not directly used in this logic)  

    half4 main(float2 fragCoord) {  
        half4 color = composable.eval(fragCoord);  

        // Calculate cutoff_max dynamically using border_thickness  
        float cutoff_max = cutoff_min + border_thickness;  

        // If alpha is exactly 0.0 (fully transparent), set it to yellow  
        if (color.a == 0.0) {  
            color.rgb = half3(1.0, 1.0, 0.0); // Yellow for fully transparent  
        }  
        // If alpha is below the minimum cutoff, make it green  
        else if (color.a &lt; cutoff_min) {  
            color.rgb = half3(0.0, 1.0, 0.0); // Green  
        }   
        // If alpha is within the cutoff range (border area), make it black  
        else if (cutoff_min &lt;= color.a &amp;&amp; color.a &lt;= cutoff_max) {  
            color.rgb = half3(0.0, 0.0, 0.0); // Black  
        }   
        // If alpha is above the maximum cutoff but less than fully opaque (outer area), make it blue  
        else if (cutoff_max &lt; color.a &amp;&amp; color.a &lt; 1.0) {  
            color.rgb = half3(0.0, 0.0, 1.0); // Blue  
        }   
        // Fully opaque case where alpha = 1, make it red  
        else {  
            color.rgb = half3(1.0, 0.0, 0.0); // Red  
        }  

        // Ensure alpha is fully opaque for all cases  
        color.a = 1.0;  

        return color;  
    }  
"""
</code></pre>

<p>模糊半径 = 16dp 时的结果（元球大小 = 100dp）</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*f0t_6j6AZqr74cWtWWTkbQ.gif" alt="模糊半径 16dp (R = 50dp)" /></p>

<p>如前所述，本文的重点并非创建边框（黑色区域），而是解决与内部透明部分相关的额外要求，同时保持图标不透明。难点在于如何根据透明度进行过滤，以确定给定点属于图标还是背景。</p>

<p>但在解决这个问题之前，我们先来了解一下为什么在图像内部会看到一个红色圆圈（一个完全不透明的像素）。</p>

<p>假设我们有一个半径为 50 dp 的某种颜色的完全不透明的圆，并应用一个半径很小的模糊效果（1-2 像素），结果会显示这个完全不透明的圆会比原来的 50 dp 半径略小一些。需要强调的是，“完全不透明的圆”指的是圆内所有 alpha 值恰好为 1.0（完全不透明）的点，而 alpha 值小于 1.0（半透明或完全透明）的点则不属于这个圆。模糊半径越大，得到的完全不透明的圆就越小。本质上，模糊效果起到了一种柔化或扩散的作用，从而缩小了模糊圆的尺寸。</p>

<p>如果模糊半径大于不透明圆的半径会发生什么呢？在这种情况下，圆内将不再存在完全不透明的像素，因为模糊会将不透明度完全分散到整个区域，只留下半透明像素。</p>

<p>实际上，要完全模糊一个完全不透明的圆并移除所有不透明像素，我们需要应用一个大于 Metaball 圆形形状的<strong>半径/2</strong>的模糊半径。</p>

<p>在这种情况下，Metaball 背景将不再包含任何完全不透明的像素。但是，由于图标渲染在一个单独的图层上（参见<strong>MetaballBox</strong>的实现），该图层未应用模糊效果，因此图标像素的不透明度将保持不变，<strong>alpha = 1.0</strong>。</p>

<p>Metaball 圆形按钮 = 100 dp，因此 R = 50 dp。</p>

<p>让我们设置一个大于 R/2 + 1 dp = 25 dp 的模糊半径。</p>

<p>对于 <strong>25 dp</strong> 的模糊半径，仍然会存在一个小的内部不透明点。为了确保不留下任何完全不透明的像素，我们将模糊半径设置为 <strong>26.dp</strong>。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*9xZ9Py3k9Dyd52lpF74T3g.gif" alt="模糊半径 26.dp (R = 50.dp)" /></p>

<p>越来越接近目标了！在按钮展开状态下，我们可以使用 <code>alpha = 1</code> 来过滤图标。但是，当按钮折叠时，模糊的元球层会产生<strong>累积效应</strong>，在给定的模糊半径下，最终完全不透明像素的区域会变大。我不确定具体的计算公式，而且这并不特别重要，因为我会在文章后面提出不同的解决方案。现在，我们先采用一个<strong>变通方法</strong>，进一步增大模糊半径。</p>

<p>应用模糊半径 = 36.dp:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*zyEJpSm3eq9ixMPbj6my5w.gif" alt="模糊半径 36.dp (R = 50.dp)" /></p>

<h2>简易 AGSL：按 Alpha 值过滤区域</h2>

<p>我们来设置输入 RGB 颜色，并将 Alpha 值设为 1，用于<strong>红色</strong>区域（<strong>图标</strong>）和<strong>黑色</strong>区域（<strong>边框</strong>）。</p>

<p>然后，将透明度（Alpha = 0）应用于所有其他区域。但这里有个小细节：</p>

<p>在我的示例中，我使用黑色作为圆形按钮的颜色，但需要注意的是，黑色是“零”色 (0, 0, 0)。如果圆形按钮的背景色是任何非“零”色，例如绿色，那么我们通过将 Alpha 通道设置为 0 来使其完全透明的模糊半透明区域仍然会影响混合效果。这种混合效果的出现是因为颜色分量（RGB）的值不为零，从而导致出现意想不到的视觉效果，例如：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1136/1*BXNddgNOlwNZqNvBJVdCZQ.png" alt="绿色背景圆形按钮示例。" /></p>

<p>因此，如果我们希望某个像素完全透明，并且<strong>背景图层不受影响，不会产生任何副作用</strong>，我们不仅应该将 <code>alpha = 0</code> 设置为 0，还应该将 <strong>“零”RGB 颜色</strong> 设置为黑色 (<code>0, 0, 0</code>)。</p>

<p>之前，我使用的边框粗细为 <strong>0.1f</strong>。为了使边框更细，我们将其减小到 <strong>0.05f</strong>。</p>

<pre><code class="kotlin">@Language("AGSL")  
const val ShaderSource_outlined_simple = """  
    uniform shader composable;  

    uniform float cutoff_min; // Minimum cutoff value for transparency  
    uniform float border_thickness; // Thickness of the border  
    uniform float3 rgbColor;  // RGB color passed as parameter  

     // Constant for black (zero) RGB color  
    const half3 BLACK_MASK_RGB = half3(0.0, 0.0, 0.0);  

    half4 main(float2 fragCoord) {  
        half4 color = composable.eval(fragCoord);  

        // Calculate cutoff_max dynamically using border_thickness  
        float cutoff_max = cutoff_min + border_thickness;  

        // If alpha is exactly 0.0 (fully transparent), do nothing  
        if (color.a == 0.0) {  
            // Do nothing  
        }  
        // If alpha is below the minimum cutoff,  
        // make it fully transparent and black  
        else if (color.a &lt; cutoff_min) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        }   
        // If alpha is within the cutoff range (border area),   
        // make it the custom color  
        else if (cutoff_min &lt;= color.a &amp;&amp; color.a &lt;= cutoff_max) {  
            color.rgb = rgbColor; // Set to custom color  
            color.a = 1.0; // Fully opaque  
        }   
        // If alpha is above the maximum cutoff but less than   
        //  fully opaque (outer area), make it fully transparent and black  
        else if (cutoff_max &lt; color.a &amp;&amp; color.a &lt; 1.0) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        }   
        // Fully opaque case where alpha = 1 (Icon area),  
        // make it the custom color  
        else {  
            color.rgb = rgbColor; // Set to custom color  
            color.a = 1.0; // Fully opaque  
        }  

        return color;  
    }  
"""
</code></pre>

<p>结果：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:892/1*s5cJYscet9Gr2Pzoil7WvA.gif" alt="简单 AGSL：仅按透明度值过滤区域" /></p>

<p>由于内部背景是透明的，我们需要解决<strong>图标重叠问题</strong>。一个快速的解决方案是隐藏下方按钮的图标，同时保持上方按钮的图标可见。</p>

<p>我们可以跟踪按钮的偏移量，当偏移量小于<strong>20dp</strong>时，将下方图标的可见性状态设置为<strong>false</strong>。相反，当偏移量大于<strong>20dp</strong>时，将其设置为<strong>true</strong>。</p>

<p>为了优化，将此逻辑封装在 <code>**derivedStateOf**</code> 中。此外，将按钮图标封装在 <code>**AnimatedVisibility**</code> 中。或者，你可以尝试根据按钮之间的偏移量手动更改 alpha 色调。</p>

<p>但是，如果我们希望图标的可见性过渡平滑，这种方法就行不通了。在下面的代码片段中，我将展开动画的持续时间设置为 <strong>7 秒</strong>，将隐藏动画的持续时间设置为 <strong>4 秒</strong>。但由于 AGSL 会根据 alpha 值进行过滤，一旦图标的 alpha 值逐渐低于 <strong>1</strong>，它就会立即完全透明。在展开动画期间，图标不会逐渐淡入——它会一直保持不可见，直到 <strong>4 秒动画结束</strong>。此时，当 <code>AnimatedVisibility</code> 容器将其 alpha 值设置为完全不透明（<code>alpha = 1</code>）时，图标会立即弹出。</p>

<pre><code class="kotlin">@Composable  
fun ExampleAGSLOutlineContent(  
    modifier: Modifier = Modifier  
) {  
        var isExpanded by remember { mutableStateOf(false) }  
        val offsetDistance = (80).dp  
        val buttonOffset by animateDpAsState(  
            targetValue = if (isExpanded) offsetDistance else 0.dp,  
            animationSpec = tween(durationMillis = 7000)  
        )  

        val color = Color.Black  

        //test visibility start  
        var isVisible by remember { mutableStateOf(false) } // Another state for visibility  
        // Derived state to update visibility based on buttonOffset  
        val derivedVisibility = remember(buttonOffset) {  
            derivedStateOf { buttonOffset &gt; 40.dp }  
        }  


        // Observe derived state and update visibility  
        LaunchedEffect(derivedVisibility.value) {  
            isVisible = derivedVisibility.value  
        }  

//

val blurRadius = 16.dp
//

val blurRadius = 26.dp
        val blurRadius = 36.dp  


        OutlineShaderMetaballBox(  
            modifier = modifier,  
            color = color  
        ) {  

            Box(  
                contentAlignment = Alignment.Center,  
                modifier = Modifier  
                    .fillMaxWidth()  
                    .padding(vertical = 24.dp)  
            ) {  

                BlurCircularButton(  
                    Modifier.offset { IntOffset(x = -buttonOffset.roundToPx(), y = 0) },  
                    onClick = { isExpanded = !isExpanded },  
                    color = color,  
                    blur = blurRadius,  
                ) {  

                    AnimatedVisibility(  
                        visible = isVisible,  
                        enter = fadeIn(animationSpec = tween(durationMillis = 4000)),  
                        exit = fadeOut(animationSpec = tween(durationMillis = 4000)),  
                    ) {  
                        Icon(  
                            modifier = Modifier.size(28.dp),  
                            imageVector = Icons.Filled.Delete,  
                            contentDescription = null,  
                            tint = color,  
                        )  
                    }  
                }  
                BlurCircularButton(  
                    Modifier  
                        .offset { IntOffset(x = +buttonOffset.roundToPx(), y = 0) },  
                    onClick = { isExpanded = !isExpanded },  
                    color = color,  
                    blur = blurRadius,  
                ) {  
                    Icon(  
                        modifier = Modifier.size(28.dp),  
                        imageVector = Icons.Filled.Build,  
                        contentDescription = null,  
                        tint = color,  
                    )  
                }  
            }  
        }  
}
</code></pre>

<p>图标可见性过渡闪烁不流畅：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*QmmZVzScXVjnTz5ebteIiw.gif" alt="AGSL Alpha 通道过滤破坏了动画可见性过渡" /></p>

<p>对于快速展开/折叠动画和可见性过渡的持续时间，例如 <strong>150 毫秒</strong> 或 <strong>300 毫秒</strong>，此问题可能不明显，并且可以正常工作。</p>

<ul>
<li><p>实现简单：AGSL 脚本非常简单，仅依赖于输入像素颜色的 Alpha 通道进行过滤。</p></li>
<li><p>对最小模糊半径有严格的依赖性。合并的元素越多，就越难计算或通过实验确定稳定实现所需的最小可接受模糊半径。这使得该方法不可靠，不适合实现健壮稳定的效果。</p></li>
<li><p>由于 AGSL 脚本中基于 alpha 通道的滤波特性，图标的平滑出现和消失难以实现。</p></li>
<li><p>随着元球交互中涉及的元素数量增加，以及膨胀物理机制的复杂性提升，确定和实现图标出现或消失的精确时机变得困难，进一步增加了控制的复杂性和整体逻辑的复杂性。</p></li>
</ul>


<p>让我们寻找更好的解决方案：</p>

<h2>中级 AGSL：按 Alpha 值和标记颜色过滤区域</h2>

<p>让我们再次回到较小的模糊半径 16dp。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1084/1*AMLBHaITVM_8pT3SzURMbA.png" alt="Alpha = 1 的红色区域" /></p>

<p>我们需要检测红色不透明区域中的像素是否属于图标。</p>

<p>我建议使用一个额外的 RGB 颜色作为标记。唯一的要求是，对于应用 AGSL 效果的容器及其子元素，该颜色必须是唯一的。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*nubnETXkwfYHQlIEk0XQCw.png" alt="为 AGSL 着色器添加一个浮点型参数 markerColor。" /></p>

<p>我们使用<strong>青色 RGB</strong>作为标记颜色。使用 <code>Color.Cyan</code> 为图标着色，并将相同的<strong>青色</strong>作为参数传递给 AGSL 脚本。</p>

<p>在 AGSL 中定义以下参数：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*yq17jZov8ZTSQEspwqJ73Q.png" alt="添加新参数：rgbMarkerColor，类型为 uniform float3" /></p>

<p>AGSL 着色器源代码的结构与上一章相同，唯一的区别在于最后一个 <code>else</code> 代码块，其中 alpha 值为 1，代表测试屏幕上的中心红色。</p>

<p>在不透明部分，检查像素的 RGB 颜色是否等于 <code>markerColor</code>。如果为真，则它是图标像素——你可以将其着色为任何你想要的颜色。设置 <code>alpha = 1</code> 是多余的，因为我们已经在不透明的 <code>else</code> 语句块中了。</p>

<p>否则，如果为假，则它肯定不是图标像素——通过将 RGB 值设置为黑色并将 <code>alpha = 0</code> 设置为 0 使其完全透明。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*HT2R2H2yKApfzo3EII58bA.png" alt="使用标记颜色检测像素是否属于图标" /></p>

<p>完整的 AGSL 脚本：</p>

<pre><code class="kotlin">@Language("AGSL")  
const val ShaderSource_outlined_marker_color = """  
    uniform shader composable;  

    uniform float cutoff_min; // Minimum cutoff value for transparency  
    uniform float border_thickness; // Thickness of the border  
    uniform float3 rgbColor;  // RGB color passed as parameter  

    uniform float3 rgbMarkerColor;  // marker for Icon tint color  

     // Constant for black (zero) RGB color  
    const half3 BLACK_MASK_RGB = half3(0.0, 0.0, 0.0);  

    half4 main(float2 fragCoord) {  
        half4 color = composable.eval(fragCoord);  

        // Calculate cutoff_max dynamically using border_thickness  
        float cutoff_max = cutoff_min + border_thickness;  

        // If alpha is exactly 0.0 (fully transparent), do nothing  
        if (color.a == 0.0) {  
            // Do nothing  
        }  
        // If alpha is below the minimum cutoff,  
        // make it fully transparent and black  
        else if (color.a &lt; cutoff_min) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        }   
        // If alpha is within the cutoff range (border area),   
        // make it the custom color  
        else if (cutoff_min &lt;= color.a &amp;&amp; color.a &lt;= cutoff_max) {  
            color.rgb = rgbColor; // Set to custom color  
            color.a = 1.0; // Fully opaque  
        }   
        // If alpha is above the maximum cutoff but less than   
        //  fully opaque (outer area), make it fully transparent and black  
        else if (cutoff_max &lt; color.a &amp;&amp; color.a &lt; 1.0) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        } else {  
         // Fully opaque case where alpha = 1  
             if(color.rgb == rgbMarkerColor){  
             //Icon area  
                color.rgb = rgbColor;  // Set to Icon color  
             }else{  
             //pixel is out of icon shape - make it transparent  
                color.rgb = BLACK_MASK_RGB;  
                color.a = 0.0;  
            }  
        }  

        return color;  
    }  
"""
</code></pre>

<p>以下是三种情况的解决方案示例：</p>

<ul>
<li><p>当图标中心部分在折叠和展开状态下均存在不透明边框时（<strong>blurRadius = 16dp</strong>）。</p></li>
<li><p>当图标在展开状态下仅形状不透明，但在折叠状态下仍然存在不透明边框时（<strong>blurRadius = 26dp</strong>）。</p></li>
<li><p>当模糊半径足够大，以至于在展开和折叠状态下均不存在不透明边框时（<strong>blurRadius = 36dp</strong>）。</p></li>
</ul>


<p><img src="https://miro.medium.com/v2/resize:fit:900/1*f-mqRDLEVi_iUsCEjl8J1A.gif" alt="blurRadius 16.dp" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*ucu3DGrhCrb2cuKJTe3_vQ.gif" alt="blurRadius 26.dp" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*lh0kitLN7mLi99BdNvu5mA.gif" alt="blurRadius 26.dp" /></p>

<p>请注意，尽管所有示例均使用 <code>borderAlphaThickness = 0.05f</code> 执行，但增加 <code>blurRadius</code> 的值也会增加边框的粗细。</p>

<ul>
<li><p>AGSL 的实现相对简单，满足文章开头概述的要求。</p></li>
<li><p>重叠部分会自动处理，因此折叠时无需手动隐藏底层图标。</p></li>
<li><p>应该也能处理更多数量的元球。</p></li>
<li><p>可以很好地适应不同的模糊半径。</p></li>
<li><p>如果传递了额外的标记颜色参数但未正确设置为色调颜色，则该参数可能会成为潜在的错误来源。</p></li>
<li><p>必须确保标记颜色具有唯一的 RGB 值，并且与用于模糊效果的颜色不同。</p></li>
<li><p>消失的重叠图标就像一个不可见的透明图层。虽然这对于 Material 3（更抽象）来说没有问题，但对于 Material Design 2 的老用户来说，这可能会破坏设计理念。</p></li>
</ul>


<h2>高级 AGSL：按 Alpha 值、标记颜色和计算亮度过滤区域。</h2>

<p>上一章的解决方案，持续时间更长，并略微放大：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*k3nkqjSeE-Z9tutJNYcRGg.gif" alt="图标突然消失（不平滑）" /></p>

<p>左侧图标在清晰的、不可见的边框下方突然消失。我们通过使其在接近边框时平滑淡出，并在折叠状态下完全消失来修复此问题。</p>

<p>基于上一章中 <code>else</code> 语句（本例中为 <code>alpha = 1</code>）检查是否与 <code>colorMarker</code> 值相等，我将进行修改，增加一项检查是否与黑色 RGB 值相等的检查，并将 <code>else</code> 语句留空：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*hW55iNq39TUn2BJZg-fcsQ.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*cN0p-qw9Nsm6224Ph7wtaQ.gif" alt="展开/折叠过程中，左侧图标被模糊区域覆盖" /></p>

<p>在折叠过程中，我们看到左侧（后方）图标的颜色接近 <code>rgbMarkerColor</code>（Color.CYAN），该图标部分被顶部的元球形状覆盖。然而，情况略有不同，因此条件 <code>color.rgb == rgbMarkerColor</code> 不匹配。随着图标逐渐靠近，这种“接近标记颜色”的颜色会逐渐过渡到接近黑色（即用于模糊效果的元球圆形按钮背景色）。</p>

<p>颜色并非完全是青色的原因在于混合效果。当左侧图标被右侧元球的模糊图层覆盖时，标记颜色会发生扭曲。根据标记颜色上方黑色模糊层的透明度，我们看到的颜色要么非常接近标记颜色，要么更接近黑色。</p>

<p>从视频中，我们可以观察到颜色之间的平滑过渡。</p>

<p>为了最大程度地增强亮度变化的效果，我们使用亮度最高的 <code>MarkerColor</code>——纯白色 <code>(1, 1, 1)</code>。最终颜色由用于模糊效果的圆形按钮背景色决定，在本例中为黑色——这非常适合接下来的解决方案。</p>

<p><strong>将 <code>**MarkerColor**</code> </strong>更改为白色：**</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*hlfNRuBhjq8rnh0CADk8ng.gif" alt="从接近白色的不透明过渡到接近黑色的不透明。" /></p>

<p>在最终的 GIF 动画中，当 <code>markerColor = (1, 1, 1)</code>（白色）时，我们可以看到，左侧图标在与右侧元球重叠并逐渐靠近的过程中，其 RGB 值从接近白色的完全不透明过渡到接近黑色的完全不透明。</p>

<p>本章的解决方案是利用亮度从亮到暗的变化，并将这些变化转换​​为透明度从不透明到透明的变化。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*8CPstoAxFyCZYYsCnISZAw.png" alt="使用亮度作为 Alpha 通道" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*x_0aiGzvUnQfN71wBPJc3A.gif" alt="亮度作为 Alpha 通道（模糊半径 16dp）" /></p>

<p>当模糊半径非常小时，元球中心呈现不透明的圆形（我在第一章“AGSL：颜色测试探究”中提到的区域），这种方法效果很好（半径为 16dp）。但是，当使用较大的半径时，在折叠状态下，只有属于图标的不透明像素没有边框，此时会出现 UI 问题。</p>

<p>让我们用大于 36dp 的模糊值来测试一下：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*PSbFrsDXlC3JmyaWZcMaFA.gif" alt="亮度作为 alpha 通道（模糊半径 45dp）" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:584/1*APS4Fdja9DbZlsZNcx5SoA.png" alt="问题：底层垃圾桶图标可见。" /></p>

<p>让我们创建一个名为 <code>adjustAlpha</code> 的辅助函数来动态调整 alpha 通道。该函数以当前的 alpha 值作为输入，并执行两个关键操作：</p>

<ol>
<li><p><strong>指数运算</strong>：将 alpha 值提升到 4 次方。此操作通过增强较高的 alpha 值并更快地降低较小的 alpha 值，来帮助平滑过渡。</p></li>
<li><p><strong>阈值处理</strong>：指数运算后，函数会检查调整后的 alpha 值是否小于预定义的阈值（例如 <code>0.01</code>）。如果小于，则返回 <code>0.0</code>（完全透明）；否则，返回调整后的 alpha 值。</p></li>
</ol>


<p>这种操作组合确保即使是较小的 alpha 值也能平滑过渡到完全透明，而不会出现突兀的视觉瑕疵。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*s9nU3I1tSSW79jxUCTl9Uw.png" alt="调整 alpha 值" /></p>

<p>通过使用此辅助函数，我可以轻松地平滑处理 alpha 过渡。</p>

<p>此外，当调整后的透明度为 <code>0</code> 时，我们将 RGB 值设置为遮罩颜色（例如黑色）。如果不是 <code>0</code>，则函数会赋值 <code>rgbColor</code>。在当前示例中，<code>rgbColor</code> 也为黑色，但如果我们使用不同的颜色进行模糊处理，此逻辑可确保正确处理透明度。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*M6Xf-RuXN8kctauNGGavuw.png" alt="ajustAlpha() 辅助函数的使用。" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:900/1*2hy494VgjfI7aXgp0Wcr-g.gif" alt="最终解决方案。模糊半径 36dp" /></p>

<p>完整的 AGSL 脚本：</p>

<pre><code class="kotlin">@Language("AGSL")  
const val ShaderSource_outlined_marker_color_and_marker_brightness = """  
    uniform shader composable;  

    uniform float cutoff_min; // Minimum cutoff value for transparency  
    uniform float border_thickness; // Thickness of the border  
    uniform float3 rgbColor;  // RGB color passed as a parameter  
    uniform float3 rgbMarkerColor;  // Marker for Icon tint color  

    // Constant for black (zero) RGB color  
    const half3 BLACK_MASK_RGB = half3(0.0, 0.0, 0.0);  

    // Helper function to adjust alpha dynamically  
    float adjustAlpha(float alpha) {  
        // Define the threshold value  
        const float threshold = 0.01;  

        // Calculate the alpha raised to the power of 4  
        float adjustedAlpha = pow(alpha, 4.0);  

        // Return either the adjusted alpha or 0 if below the threshold  
        return adjustedAlpha &lt; threshold ? 0.0 : adjustedAlpha;  
    }  

    half4 main(float2 fragCoord) {  
        half4 color = composable.eval(fragCoord);  

        // Calculate cutoff_max dynamically using border_thickness  
        float cutoff_max = cutoff_min + border_thickness;  

        // If alpha is exactly 0.0 (fully transparent), do nothing  
        if (color.a == 0.0) {  
            // Do nothing  
        }  
        // If alpha is below the minimum cutoff, make it fully transparent and black  
        else if (color.a &lt; cutoff_min) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        }   
        // If alpha is within the cutoff range (border area), make it the custom color  
        else if (cutoff_min &lt;= color.a &amp;&amp; color.a &lt;= cutoff_max) {  
            color.rgb = rgbColor; // Set to custom color  
            color.a = 1.0; // Fully opaque  
        }   
        // If alpha is above the maximum cutoff but less than fully opaque (outer area),  
        // make it fully transparent and black  
        else if (cutoff_max &lt; color.a &amp;&amp; color.a &lt; 1.0) {  
            color.rgb = BLACK_MASK_RGB; // Set to black  
            color.a = 0.0; // Fully transparent  
        }   
        // Fully opaque case where alpha = 1  
        else {  
            if (color.rgb == rgbMarkerColor) {  
                // Icon area  
                color.rgb = rgbColor; // Set to Icon color  
            } else if (color.rgb == rgbColor) {  
                // Some black inner metaball area could be opaque - make it transparent  
                color.rgb = BLACK_MASK_RGB;  
                color.a = 0.0;  
            } else {  
                // Compute current brightness  
                float brightness = (color.r + color.g + color.b) / 3.0;  
                float adjustedBrightness = adjustAlpha(brightness);  

                // Set brightness as the alpha channel  
                color.a = adjustedBrightness;  

                // Set RGB color based on adjusted brightness  
                if (adjustedBrightness == 0) {  
                    color.rgb = BLACK_MASK_RGB; // Fully transparent  
                } else {  
                    color.rgb = rgbColor; // Custom color  
                }  
            }  
        }  

        return color;  
    }  
"""
</code></pre>

<ul>
<li><p>效果极佳。符合 UI 要求，确保当元球按钮靠近时，图标平滑消失。</p></li>
<li><p><strong>复杂逻辑</strong>：此实现引入了更多复杂性，尤其是在处理亮度到透明度的计算方面，这可能会增加出现错误的可能性。</p></li>
<li><p><strong>模糊半径限制</strong>：非常大的模糊半径值需要通过亮度（AGSL：<code>adjustAlpha()</code>）微调透明度计算，以保持预期行为。</p></li>
<li><p><strong>可扩展性问题</strong>：当处理两个以上的元球元素时，可能需要对透明度计算函数进行额外调整。</p></li>
<li><p><strong>标记和模糊颜色限制</strong>：标记颜色必须为纯白色，而模糊颜色必须为纯黑色，以避免混合不一致。</p></li>
<li><p><strong>设计依赖性</strong>：所有必需的颜色，包括图标颜色、边框颜色和内部区域的透明度，都必须通过 AGSL 着色器显式设置，这增加了对着色器逻辑的依赖性。</p></li>
</ul>


<p><a href="https://github.com/yuriyskulskiy/android-compose-playground/blob/main/app/src/main/java/com/skul/yuriy/composeplayground/feature/gooey/blurConcept/ExampleRuntimeRenderEffectOutline.kt"><strong>当前可组合部分的 GitHub 链接</strong></a> <strong>以及</strong> <a href="https://github.com/yuriyskulskiy/android-compose-playground/blob/main/app/src/main/java/com/skul/yuriy/composeplayground/feature/gooey/blurConcept/util/shader/OutlineAgslShader.kt"><strong>AGSL 着色器链接</strong></a><strong>。</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Jetpack Compose中的富文本输入]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/30/rich-content-in-text-input/"/>
    <updated>2026-03-30T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/30/rich-content-in-text-input</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Rich Content in Text Input in Jetpack Compose」，原文链接<a href="https://medium.com/proandroiddev/rich-content-in-text-input-in-jetpack-compose-0f4c1e8e830f">https://medium.com/proandroiddev/rich-content-in-text-input-in-jetpack-compose-0f4c1e8e830f</a>，由Oğuzhan Aslan发布于2026年3月23日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:2000/0*svwp7DDlW4sNI_rX" title="auto auto" ></a></p>

<!-- more -->


<h2>引言</h2>

<p>在当今的移动环境中，文本输入不再仅仅局限于文本。用户已经习惯了丰富且富有表现力的沟通方式，而不仅仅局限于简单的字符。无论是发送有趣的 GIF、分享贴纸，还是从其他应用拖放图片，富文本支持都已成为用户的默认期望。</p>

<p>大多数流行的即时通讯和社交媒体应用都能无缝支持这些功能。然而，对于 Android 开发者来说，支持这种“默认”行为需要特定的实现步骤。标准的文本字段无法自动处理从 Gboard 发送的 GIF 或从剪贴板粘贴的图片。</p>

<h2>“默认”体验（以及它为何失败）</h2>

<p>如果你只是将一个 <code>TextField</code> 拖放到 Compose UI 中，它就能完美地处理文本。但是，如果你尝试从键盘插入 GIF 或粘贴图片，却发现……没有任何反应。运气好的话，键盘可能会提交一个 URI 字符串；但更常见的情况是，输入会被直接忽略，因为文本字段只接受文本。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:540/1*grhVk_LiTugRc9qf2JozEw.gif" alt="" /></p>

<p>为了解决这个问题，Jetpack Compose 引入了一个强大的修饰符：<code>contentReceiver</code>。</p>

<h2>解决方案：<code>contentReceiver</code></h2>

<p><code>contentReceiver</code> 修饰符是一个统一的 API，旨在处理来自多个来源的富文本内容：</p>

<ol>
<li><p><strong>软键盘</strong>：来自 Gboard 等应用的 GIF 和贴纸。</p></li>
<li><p><strong>剪贴板</strong>：粘贴的图片或富文本。</p></li>
<li><p><strong>拖放</strong>：从其他应用（在分屏模式下）或同一应用内拖拽的内容。</p></li>
</ol>


<p><code>contentReceiver</code> 为我们提供了一个单一的入口点来处理所有 <code>TransferableContent</code>，而无需为键盘管理 <code>OnCommitContentListener</code>，也无需为拖放监听器单独管理。</p>

<h2>逐步实现</h2>

<p>让我们构建一个可以接收和显示图片的文本输入区域。</p>

<h3>步骤 1：ViewModel 逻辑</h3>

<p>首先，我们需要一种方法来处理传入的内容。我们将创建一个 <code>RichContentViewModel</code> 来处理 <code>TransferableContent</code>。核心逻辑涉及使用 <code>.consume</code> 函数，该函数允许我们提取可以处理的内容部分（例如图片 URI），并返回其余部分（如果已处理完所有内容，则返回 null）。</p>

<pre><code class="kotlin">class RichContentViewModel : ViewModel() {  
    // State to hold our received images  
    var selectedImages by mutableStateOf&lt;List&lt;Uri&gt;&gt;(emptyList())  

    @OptIn(ExperimentalFoundationApi::class)  
    fun handleContent(  
        transferableContent: TransferableContent  
    ): TransferableContent? {  
        val newUris = mutableListOf&lt;Uri&gt;()  

        // consume returns the parts of the content we didn't use.  
        // inside the lambda, we return true if we consumed the item.  
        val remaining = transferableContent.consume { item -&gt;  
            // If the item has a URI, we take it  
            if (item.uri != null) {  
                newUris += item.uri  
                true // Consumed  
            } else {  
                false // Not consumed, leave it for other handlers  
            }  
        }  

        // Update our state with the new images  
        selectedImages = newUris  

        return remaining  
    }  
}
</code></pre>

<h3>步骤 2：UI 实现</h3>

<p>现在，让我们将其应用到 UI 中。<code>contentReceiver</code> 的一个关键优势是灵活性。我们不必将其直接应用于 <code>TextField</code>。通过将其应用于父容器（例如 <code>Column</code>），我们可以将整个区域转换为拖放操作的“放置区”，从而改善用户体验。</p>

<p>以下是我们的 <code>RichContentTextField</code> 可组合组件：</p>

<pre><code class="kotlin">@OptIn(ExperimentalFoundationApi::class)  
@Composable  
fun RichContentTextField(  
    richContentViewModel: RichContentViewModel = viewModel()  
) {  
    Column(  
        modifier = Modifier  
            .fillMaxWidth()  
            .border(1.dp, color = Color.Black)  
            // Attach the content receiver here!  
            // This delegates the handling logic to our ViewModel  
            .contentReceiver(richContentViewModel::handleContent)  
    ) {  
        // 1. Display received images  
        LazyRow(  
            modifier = Modifier.fillMaxWidth()  
        ) {  
            items(richContentViewModel.selectedImages) { imageUri -&gt;  
                AsyncImage(  
                    model = imageUri,  
                    contentDescription = null,  
                    modifier = Modifier  
                        .size(64.dp)  
                        .padding(end = 4.dp)  
                        .clip(RoundedCornerShape(8.dp)),  
                    contentScale = ContentScale.Crop  
                )  
            }  
        }  
        // 2. The actual text input  
        val count = rememberTextFieldState()  

        // Optional: Update text state based on images (just for demo purposes)  
        LaunchedEffect(richContentViewModel.selectedImages) {  
            if (richContentViewModel.selectedImages.isNotEmpty()) {  
                 count.setTextAndPlaceCursorAtEnd(richContentViewModel.selectedImages.size.toString())  
            }  
        }  
        TextField(  
            state = count,  
            placeholder = { Text("Rich Content Field") },  
        )  
    }  
}
</code></pre>

<h2>代码要点：</h2>

<ol>
<li><p><code>**contentReceiver**</code> <strong>在父级上</strong>：我们将修饰符放在了 <code>Column</code> 上。这意味着如果用户将图像拖到此框中的任何位置，它都会被捕获。</p></li>
<li><p><code>**AsyncImage**</code>：我们使用 Coil 的 <code>AsyncImage</code> 来渲染提取的 URI。</p></li>
<li><p><strong>关注点分离</strong>：UI 只负责显示状态（<code>selectedImages</code>），而 ViewModel 则处理 <code>TransferableContent</code> 的复杂性。</p></li>
</ol>


<h2>结果</h2>

<p>只需几行代码，我们就将一个基本的文本输入框转换成了一个富媒体接收器。用户现在可以从键盘插入 GIF 动画，或者直接将照片拖到应用中。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:540/1*Dj6SN5VpNzHI98zW9s6Zww.gif" alt="" /></p>

<h2>结论</h2>

<p>支持富媒体内容不再是“额外”功能，而是现代 Android 应用开发的核心组成部分。Jetpack Compose 中的 <code>contentReceiver</code> 修饰符极大地简化了这一过程，将原本三个独立的 API 统一到一个简洁的声明式接口中。通过实现这一点，你可以确保你的应用拥有原生应用般的体验，并且能够响应用户如今的实际交互方式。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Compose中的ContentScale：终极可视化指南]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/27/contentscale-in-jetpack-compose/"/>
    <updated>2026-03-27T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/27/contentscale-in-jetpack-compose</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「ContentScale in Jetpack Compose: The Ultimate Visual Guide」，原文链接<a href="https://medium.com/proandroiddev/contentscale-in-jetpack-compose-the-ultimate-visual-guide-43599d1e36b9">https://medium.com/proandroiddev/contentscale-in-jetpack-compose-the-ultimate-visual-guide-43599d1e36b9</a>，由Sehaj kahlon发布于2026年2月19日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*K3K157lExVJ2JYOS8Aqu4g.png" title="auto auto" ></a></p>

<!-- more -->


<p>你是否曾经盯着一张 <code>Image()</code> 可组合图片，疑惑为什么你那张漂亮的 4K 照片看起来像是被垃圾压缩机碾过一样？或者为什么图片周围会有一圈神秘的空白，仿佛在和边缘保持社交距离？</p>

<p>没错，这就是 <code>ContentScale</code> 在作祟。或者更确切地说，是你不知道该选择哪个 <code>ContentScale</code>。</p>

<p>让我们彻底解决这个问题。</p>

<h2>缘起：我们到底要解决什么问题？</h2>

<p>问题的关键在于：你的<strong>图片</strong>是有尺寸的。你的<strong>容器</strong>（带有修饰符的可组合 <code>Image</code>）有尺寸。而且它们的尺寸几乎永远不会相同。</p>

<pre><code class="bash">Your Image: 1200 x 800 (landscape photo)  
Your Container: 300 x 400 (portrait box on screen)
</code></pre>

<p>因此，系统必须做出决定：<strong>如何将这个矩形的插销插入一个形状不同的矩形孔中？</strong></p>

<p>这个决定就是 <code>ContentScale</code>。</p>

<pre><code class="kotlin">Image(  
    painter = painterResource(id = R.drawable.puppy),  
    contentDescription = "Good boy",  
    modifier = Modifier.size(300.dp, 400.dp),  
    contentScale = ContentScale.Fit  // &lt;-- THIS. This is what we're talking about.  
)
</code></pre>

<h2>心智模型：想象一个相框</h2>

<p>想象一下，你从宜家买了一个相框（容器）。现在你有一张照片（图像），它的尺寸与相框不太匹配。你会怎么做？</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*wR0gyLJ0dom2Ir9XxAeIXA.png" alt="" /></p>

<h2>视觉解析</h2>

<p>以下每个示例均假设：</p>

<ul>
<li><p>图片：1200 x 600（宽幅横向，2:1 比例）</p></li>
<li><p>容器：400 x 400（正方形）</p></li>
</ul>


<p>容器显示为外框。图片位于容器内部。</p>

<h3>1. <code>ContentScale.Fit</code></h3>

<p><strong>规则</strong>：缩放图片使其完全适应容器。保持宽高比。图片沿容器的<strong>窄边</strong>方向与容器接触。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*2ur2RjaNhOY1Dy30PqxtQw.png" alt="" /></p>

<p><strong>实际效果：</strong> 图片被缩小至 400x200（保持 2:1 比例）。宽度完美匹配（400），但高度只有 200，顶部和底部各留出 100 像素的空白。</p>

<p><strong>计算方法：</strong></p>

<pre><code class="bash">scaleX = containerWidth / imageWidth   = 400/1200 = 0.333  
scaleY = containerHeight / imageHeight = 400/600  = 0.667  
scale  = min(scaleX, scaleY)

= 0.333
Result: 1200 * 0.333 = 400 wide, 600 * 0.333 = 200 tall
</code></pre>

<p><strong>适用场景：</strong> 你需要用户无论如何都能看到<strong>完整的</strong>图像。例如：电商应用中的产品图片、文档预览、以及完整性至关重要的缩略图。</p>

<h3>2. <code>ContentScale.Crop</code></h3>

<p><strong>规则：</strong> 缩放图像使其<strong>填充</strong>整个容器。保持宽高比。裁剪掉超出容器边缘的部分。图像会在容器较宽的一侧与容器边缘接触。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*pbX883JjKDI9F1BDwOqGFA.png" alt="" /></p>

<p><strong>发生了什么：</strong> 图片被放大到 800x400（保持 2:1 的比例）。高度完美匹配 (400)，但宽度为 800 — 因此两侧各裁剪掉 200 像素。</p>

<p><strong>计算过程：</strong></p>

<pre><code class="bash">scaleX = containerWidth / imageWidth   = 400/1200 = 0.333  
scaleY = containerHeight / imageHeight = 400/600  = 0.667  
scale  = max(scaleX, scaleY)

= 0.667
Result: 1200 * 0.667 = 800 wide, 600 * 0.667 = 400 tall  
         (800 - 400) / 2 = 200px clipped each side
</code></pre>

<p><strong>注意与“Fit”的唯一区别：</strong> <code>min</code> 与 <code>max</code>。仅此而已。这就是全部区别。“Fit”使用 <code>min(scaleX, scaleY)</code>，“Crop”使用 <code>max(scaleX, scaleY)</code>。</p>

<p><strong>适用场景：</strong> 当你 希望容器完全填充且没有空白时。例如：背景图片、横幅广告、头像圆形图片、任何“封面图片”场景。</p>

<h3>3. <code>ContentScale.FillBounds</code></h3>

<p><strong>规则</strong>：拉伸或压缩图像以精确匹配容器。宽高比？什么宽高比？</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*oJwmnjQMTmPZLT84fe2A2g.png" alt="" /></p>

<p><strong>结果：</strong> 1200x600 的图像现在正好是 400x400。照片中的人物看起来更矮更宽。圆形变成了椭圆形。一片混乱。</p>

<p>计算过程：</p>

<pre><code class="bash">scaleX = containerWidth / imageWidth   = 400/1200 = 0.333  
scaleY = containerHeight / imageHeight = 400/600  = 0.667  
// Use BOTH scales independently. X and Y scale differently.  
Result: exactly 400 x 400. Aspect ratio destroyed.
</code></pre>

<p><strong>适用场景</strong>：几乎不用于照片。但适用于：纯色背景、可容忍变形的重复图案、渐变图像，或者图像和容器的宽高比始终保持一致的情况。</p>

<h3>4. <code>ContentScale.FillWidth</code></h3>

<p><strong>规则</strong>：缩放图片，使其宽度与容器宽度完全匹配。保持宽高比。高度则根据需要进行调整。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*-UFvjDKmw5TnmFHMNNC2MQ.png" alt="" /></p>

<p><strong>问题所在：</strong>宽度缩放至 400。由于宽高比为 2:1，高度变为 200。因为容器高度为 400，所以底部会留出 200 像素的空白区域。</p>

<p><strong>计算过程：</strong></p>

<pre><code class="bash">scale = containerWidth / imageWidth = 400/1200 = 0.333  
Result: 1200 * 0.333 = 400 wide, 600 * 0.333 = 200 tall
</code></pre>

<p><strong>但是等等——</strong>如果图片是竖屏/纵向的（例如 600x1200），<code>FillWidth</code> 会将宽度缩放至 400，高度变为 800，这将超出 400 像素的容器高度。在这种情况下，图片顶部和底部都会被裁剪。</p>

<p><strong>适用场景：</strong> 可滚动的垂直列表，需要保持宽度一致。例如：新闻推送，图片需要铺满整个宽度；或者卡片式的 LazyColumn。</p>

<h3>5. <code>ContentScale.FillHeight</code></h3>

<p><strong>规则：</strong> 缩放图片，使其高度与容器高度完全匹配。保持宽高比。宽度会根据需要进行调整。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*w1OL-tuZhOLyJvrZD1tG9Q.png" alt="" /></p>

<p><strong>实际情况：</strong> 高度缩放至 400。由于宽高比为 2:1，宽度变为 800。远超我们 400px 的容器宽度。两侧各被裁剪了 200px。</p>

<p>数学原理：</p>

<pre><code class="bash">scale = containerHeight / imageHeight = 400/600 = 0.667  
Result: 1200 * 0.667 = 800 wide, 600 * 0.667 = 400 tall
</code></pre>

<p><strong>适用场景：</strong> 水平滚动图库（LazyRow），以及需要保持项目高度一致的场景。</p>

<h3>6. <code>ContentScale.Inside</code></h3>

<p><strong>规则：</strong> 如果图片<strong>大于</strong>容器，则行为与 <code>Fit</code> 完全相同。如果图片<strong>小于</strong>容器，则不进行缩放，只需将其居中显示在原始大小即可。</p>

<p><strong>当图片大于容器时（例如，1200x600 -> 400x400 的盒子）：</strong></p>

<p>行为与 <code>Fit</code> 完全相同。</p>

<p><strong>当图片小于容器尺寸时（例如，100x50 -> 400x400 的盒子）：</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*RjAu6FyjeazaqI-TJpn9qA.png" alt="" /></p>

<p>与“Fit”属性的主要区别在于：“Fit”属性会将 100x50 的图片放大到 400x200。“Inside”属性则会说“不，它已经适合容器了，无需调整”。</p>

<p><strong>计算公式：</strong></p>

<pre><code class="bash">scaleX = containerWidth / imageWidth  
scaleY = containerHeight / imageHeight  
scale  = min(scaleX, scaleY)  
if (scale &gt;= 1.0) scale = 1.0  // DON'T upscale
</code></pre>

<p><strong>适用场景：</strong> 当你 希望在图片尺寸较小时以原始分辨率显示图片，但又不想让过大的图片超出容器范围时。</p>

<p>例如：聊天应用的图片消息、用户上传的内容，在这些场景下，你 不希望放大低分辨率图片并使其模糊。</p>

<h3>7. <code>ContentScale.None</code></h3>

<p><strong>规则：</strong> 完全不要缩放。将图片居中显示，并保持其原始像素大小。如果图片大于容器，则会被裁剪。如果图片小于容器，则会留出空白区域。</p>

<p>当图像较大时（1200x600 像素，位于 400x400 像素的方框内）：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*5_sNHX7zfxzOvtPY_vT9kA.png" alt="" /></p>

<p>当图像较小时（100x50 像素，位于 400x400 像素的方框内）：</p>

<p>与“内部”选项相同——居中显示，图像很小，不缩放。</p>

<p>适用场景：像素画、精灵图、必须保持精确像素大小的图标，或任何缩放会破坏内容的场景。</p>

<h2>速查表（请收藏）</h2>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*gtPkJfFTuQ75s-vQ0RaheQ.png" alt="" /></p>

<p>决策流程图：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*-_4_rSr8HlmLUsUT4KPD6w.png" alt="" /></p>

<h2>幕后揭秘：Compose 是如何实现的？</h2>

<p>当你 将 <code>contentScale</code> 传递给 <code>Image</code> 对象时，渲染管线深处实际发生的情况如下：</p>

<h3><strong>步骤 1：测量</strong></h3>

<p><code>Image</code> 可组合对象会根据其 <code>Modifier</code> 约束进行自身测量。如果你使用 <code>Modifier.size(300.dp, 400.dp)</code>，容器将变为 300x400 dp（使用密度转换为像素）。</p>

<h3>步骤 2：ContentScale.computeScaleFactor()</h3>

<p>每个 <code>ContentScale</code> 实际上都是一个接口，只有一个关键方法：</p>

<pre><code class="kotlin">// This is what ContentScale looks like under the hood  
fun interface ContentScale {  
    fun computeScaleFactor(srcSize: Size, dstSize: Size): ScaleFactor  
}
</code></pre>

<p><code>ScaleFactor</code> 只是一个二元组：<code>(scaleX: Float, scaleY: Float)</code>。</p>

<p>以下是每个变体的实现方式：</p>

<pre><code class="kotlin">// Fit  
ContentScale.Fit -&gt; {  
    val scale = min(dstWidth / srcWidth, dstHeight / srcHeight)  
    ScaleFactor(scale, scale)  // same scale for both axes = aspect ratio preserved  
}  
// Crop  
ContentScale.Crop -&gt; {  
    val scale = max(dstWidth / srcWidth, dstHeight / srcHeight)  
    ScaleFactor(scale, scale)  // same scale, but max instead of min  
}  
// FillBounds  
ContentScale.FillBounds -&gt; {  
    ScaleFactor(dstWidth / srcWidth, dstHeight / srcHeight)  
    // different scales for X and Y = aspect ratio broken  
}  
// FillWidth  
ContentScale.FillWidth -&gt; {  
    val scale = dstWidth / srcWidth  
    ScaleFactor(scale, scale)  
}  
// FillHeight  
ContentScale.FillHeight -&gt; {  
    val scale = dstHeight / srcHeight  
    ScaleFactor(scale, scale)  
}  
// Inside  
ContentScale.Inside -&gt; {  
    if (srcWidth &lt;= dstWidth &amp;&amp; srcHeight &lt;= dstHeight) {  
        ScaleFactor(1f, 1f)  // already fits, don't upscale  
    } else {  
        // same as Fit  
        val scale = min(dstWidth / srcWidth, dstHeight / srcHeight)  
        ScaleFactor(scale, scale)  
    }  
}  
// None  
ContentScale.None -&gt; {  
    ScaleFactor(1f, 1f)  // always 1:1, no scaling ever  
}
</code></pre>

<h3>步骤 3：平移（居中）</h3>

<p>缩放后，图像可能无法填满容器。系统使用 <code>Alignment</code> 将缩放后的图像居中（默认值为 <code>Alignment.Center</code>）：</p>

<pre><code class="kotlin">val offsetX = (containerWidth - scaledImageWidth) / 2  
val offsetY = (containerHeight - scaledImageHeight) / 2
</code></pre>

<p>你 可以使用 <code>Image()</code> 函数的 <code>alignment</code> 参数覆盖此默认行为：</p>

<pre><code class="kotlin">Image(  
    painter = painterResource(id = R.drawable.landscape),  
    contentDescription = null,  
    contentScale = ContentScale.Crop,  
    alignment = Alignment.TopStart  // crop from top-left instead of center  
)
</code></pre>

<p>这在 <code>Crop</code> 函数中非常有用——例如，如果你 有一张人像照片，并且想要确保人物的脸部（通常位于顶部附近）不会被裁剪掉，请使用 <code>Alignment.TopCenter</code>。</p>

<h3>步骤 4：画布绘制</h3>

<p>最后，Compose 函数会在绘制之前将变换矩阵应用于画布：</p>

<pre><code class="bash">Canvas -&gt; translate(offsetX, offsetY) -&gt; scale(scaleX, scaleY) -&gt; drawImage
</code></pre>

<p>如果缩放后​​的图像超出容器边界（例如在 <code>Crop</code> 函数中），则容器的裁剪边界会处理裁剪。<code>clipToBounds</code> 修饰符（由 <code>Image</code> 函数内部应用）确保溢出部分被隐藏。</p>

<h2>常见错误及专业技巧</h2>

<h3>错误 1：对照片使用 <code>FillBounds</code></h3>

<p>用户的脸部会像哈哈镜一样扭曲变形。请改用 <code>Crop</code>。</p>

<h3>错误 2：对背景图片使用 <code>Fit</code></h3>

<p>会出现难看的黑边。请对背景图片使用 <code>Crop</code>。</p>

<h3>错误 3：忘记使用 <code>Crop</code> 的 <code>Alignment</code> 属性</h3>

<p>默认情况下，<code>Crop</code> 会将可见区域居中。对于头像，请考虑使用 <code>Alignment.TopCenter</code>，以避免脸部顶部被裁剪。</p>

<h3>错误 4：混淆 <code>Fit</code> 和 <code>Inside</code></h3>

<p>当图片大于容器时，它们的效果相同。区别仅在于较小的图片：<code>Fit</code> 会放大图片，而 <code>Inside</code> 不会。</p>

<h3>专业技巧：ContentScale 适用于任何 Painter</h3>

<p>不仅仅是 <code>painterResource</code>。它适用于 <code>rememberAsyncImagePainter</code>（Coil）、<code>rememberImagePainter</code>、矢量图、自定义绘图器——几乎任何对象。</p>

<h3>专业提示：结合使用 <code>Modifier.aspectRatio()</code></h3>

<pre><code class="kotlin">Image(  
    painter = painterResource(R.drawable.hero),  
    contentDescription = null,  
    contentScale = ContentScale.Crop,  
    modifier = Modifier  
        .fillMaxWidth()  
        .aspectRatio(16f / 9f)  // forces a 16:9 container  
)
</code></pre>

<p>现在，你 可以获得形状一致的容器，并且图像可以通过 <code>Crop</code> 完美填充容器。这套组合非常适合用于主图、卡片和媒体播放器。</p>

<h2>快速参考代码片段</h2>

<pre><code class="kotlin">// Product thumbnail (show full image)  
ContentScale.Fit  

// Profile picture circle  
ContentScale.Crop + Modifier.clip(CircleShape)  

// Full-screen background  
ContentScale.Crop + Modifier.fillMaxSize()  

// News feed image (full width, flexible height)  
ContentScale.FillWidth + Modifier.fillMaxWidth()  

// Horizontal gallery  
ContentScale.FillHeight + Modifier.fillMaxHeight()  

// Chat image (don't upscale small images)  
ContentScale.Inside  

// Pixel art or sprites  
ContentScale.None
</code></pre>

<h2>TL;DR</h2>

<ul>
<li><p><strong>Fit</strong> = 包含所有区域，允许留白</p></li>
<li><p><strong>Crop</strong> = 填充所有区域，允许丢失边缘</p></li>
<li><p><strong>FillBounds</strong> = 填充所有区域，允许图像变形（很少需要）</p></li>
<li><p><strong>FillWidth</strong> = 锁定宽度，高度自动调整</p></li>
<li><p><strong>FillHeight</strong> = 锁定高度，宽度自动调整</p></li>
<li><p><strong>Inside</strong> = 填充图像，但拒绝放大小尺寸图像</p></li>
<li><p><strong>None</strong> = 不修改像素</p></li>
</ul>


<p>秘诀在于一个函数：<code>computeScaleFactor(src, dst)</code>，它返回 <code>(scaleX, scaleY)</code>。每个变体只是计算这两个数字的不同策略。就是这样。这就是全部。</p>

<p>现在去发布一些正确缩放的图像吧。</p>

<p>如果这篇文章让你免于陷入 StackOverflow 的无底洞，那真是太感谢了。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[深度解析Compose中的BoxWithConstraints]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/26/boxwithconstraints-in-jetpack-compose/"/>
    <updated>2026-03-26T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/26/boxwithconstraints-in-jetpack-compose</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「BoxWithConstraints in Jetpack Compose: The Complete Deep Dive」，原文链接<a href="https://proandroiddev.com/boxwithconstraints-in-jetpack-compose-the-complete-deep-dive-28b8f6516ef7">https://proandroiddev.com/boxwithconstraints-in-jetpack-compose-the-complete-deep-dive-28b8f6516ef7</a>，由Sehaj kahlon发布于2026年2月15日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/1*MLsQmsCtobQD5EhMRB1jFg.png" title="auto auto" ></a></p>

<!-- more -->


<h2>关键点：这个组件存在的意义是什么？</h2>

<p>让我来描述一下你可能经历过的场景。</p>

<p>你正在构建一个卡片组件。在手机上，它应该垂直堆叠：图片在上，文字在下。在平板电脑上，它应该水平拉伸：图片在左，文字在右。需求很简单，对吧？</p>

<p>你的第一反应可能是：</p>

<pre><code class="kotlin">@Composable
fun AdaptiveCard() {
    val configuration = LocalConfiguration.current
    if (configuration.screenWidthDp &lt; 600) {
        VerticalCard()
    } else {
        HorizontalCard()
    }
}
</code></pre>

<p><strong>这种方法存在缺陷。</strong> 原因如下。</p>

<p><code>LocalConfiguration.current.screenWidthDp</code> 返回的是屏幕宽度。但如果你的卡片位于 <code>NavigationRail</code> 中呢？或者在分屏应用中？或者在 ChromeOS 的可调整大小的窗口中呢？你的卡片并不关心屏幕大小——它只关心<strong>实际分配给它的空间</strong>。</p>

<p>这就是 <code>BoxWithConstraints</code> 解决的根本问题：<strong>它告诉你父元素分配给你的空间大小，而不是屏幕大小。</strong></p>

<p>可以这样理解：</p>

<ul>
<li><p><code>LocalConfiguration</code> = &ldquo;房间有多大？&rdquo;</p></li>
<li><p><code>BoxWithConstraints</code> = &ldquo;你把我放在的盒子有多大？&rdquo;</p></li>
</ul>


<p>相框并不关心你的房子有多大。它只关心分配给它的墙面空间。</p>

<h2>内部机制：底层工作原理</h2>

<h3>Compose 执行模型（以及 BoxWithConstraints 为何会破坏它）</h3>

<p>要理解 <code>BoxWithConstraints</code>，你必须首先了解 Compose 的执行流程：</p>

<pre><code class="bash">Composition → Measurement → Layout → Drawing
</code></pre>

<p>在标准的 Compose 代码中：</p>

<ol>
<li><p><strong>组合</strong>：你声明 UI 树（<code>Column { Text("Hello") }</code>）</p></li>
<li><p><strong>测量</strong>：系统测量每个节点</p></li>
<li><p><strong>布局</strong>：节点定位</p></li>
<li><p><strong>绘制</strong>：像素绘制到屏幕上</p></li>
</ol>


<p>这个顺序<strong>不可更改</strong>。组合先于测量。你无法测量尚未组合的内容。</p>

<p>但是 <code>BoxWithConstraints</code> 提出了一个不寻常的问题：<em>“我能否在决定组合内容之前就知道我的约束条件？”</em></p>

<p>这就像要求厨师在烹饪之前先摆盘一样。</p>

<h2>引入子组合：Compose 的延迟组合机制</h2>

<p><code>BoxWithConstraints</code> 通过<strong>子组合</strong>实现此功能，该机制将组合操作延迟到测量时执行。</p>

<p>以下是实际实现（为清晰起见已简化）：</p>

<pre><code class="kotlin">@Composable
fun BoxWithConstraints(
    modifier: Modifier = Modifier,
    contentAlignment: Alignment = Alignment.TopStart,
    propagateMinConstraints: Boolean = false,
    content: @Composable BoxWithConstraintsScope.() -&gt; Unit,
) {
    SubcomposeLayout(modifier) { constraints -&gt;
        // We're NOW in the measurement phase, but we can compose!
        val scope = BoxWithConstraintsScopeImpl(this, constraints)
        val measurables = subcompose(Unit) { scope.content() }

        // Standard Box measurement logic follows...
        with(measurePolicy) { measure(measurables, constraints) }
    }
}
</code></pre>

<p>关键在于 <code>SubcomposeLayout</code>。它反转了正常的流程：</p>

<pre><code class="bash">Normal:

Composition → Measurement
Subcompose: Measurement → Composition (of children) → Measurement (of children)
</code></pre>

<p>当父元素请求 <code>BoxWithConstraints</code> 测量自身时，它会：</p>

<ol>
<li><p>接收约束条件</p></li>
<li><p><strong>创建一个包含这些约束条件的作用域</strong></p></li>
<li><p><strong>此时组合子元素</strong></p></li>
<li><p>测量已组合的子元素</p></li>
<li><p>返回测量结果</p></li>
</ol>


<p>这就是为什么 <code>BoxWithConstraints</code> 被认为是“重量级”的原因。它在测量阶段执行组合操作。</p>

<h2>三层节点架构</h2>

<p>在内部，<code>SubcomposeLayout</code> 将子节点分为三个不同的区域进行管理：</p>

<pre><code class="bash">[Active nodes] [Reusable nodes] [Precomposed nodes]
     ↑

     ↑

     ↑
  Currently

  "Recycled"

  Pre-composed
    used

    for reuse

    ahead of time
</code></pre>

<p><strong>活动节点</strong>：当前测量阶段正在使用的插槽。</p>

<p><strong>可重用节点</strong>：之前已激活但仍保留的插槽（类似于 RecyclerView 的可回收视图）。当需要类似内容时，这些插槽会被“重新激活”，而不是创建新的组合。</p>

<p><strong>预组合节点</strong>：通过 <code>precompose()</code> 预先组合的插槽（惰性列表内部使用此功能在即将显示的项目可见之前进行组合）。</p>

<p>正是这种架构使得 <code>LazyColumn</code> 能够流畅滚动。它不会创建和销毁组合，而是在这三个节点池之间进行节点的轮换。</p>

<h2>约束：深入解析</h2>

<p>在 <code>BoxWithConstraintsScope</code> 中，你可以访问四个属性：</p>

<pre><code class="kotlin">interface BoxWithConstraintsScope : BoxScope {
    val minWidth: Dp
    val maxWidth: Dp
    val minHeight: Dp
    val maxHeight: Dp
    val constraints: Constraints  // The raw pixel-based constraints
}
</code></pre>

<h3>这些属性的实际含义是什么？</h3>

<p><code>**maxWidth**</code> <strong>/</strong> <code>**maxHeight**</code>：父元素提供的最大空间。如果为 <code>Dp.Infinity</code>，则表示父元素允许你“随意使用”（通常来自可滚动容器）。</p>

<p><code>**minWidth**</code> <strong>/</strong> <code>**minHeight**</code>：父元素期望的最小高度。通常为 <code>0.dp</code>，但如果父元素使用了 <code>propagateMinConstraints = true</code> 或你使用的是基于 <code>weight</code> 的布局，则该值可能不为零。</p>

<h2>屏幕尺寸陷阱</h2>

<pre><code class="kotlin">BoxWithConstraints {
    // These are NOT the screen dimensions!
    val width = maxWidth   // Space YOUR PARENT gave YOU
    val height = maxHeight // Space YOUR PARENT gave YOU
}
</code></pre>

<p>考虑以下层级结构：</p>

<pre><code class="kotlin">Row(Modifier.fillMaxSize()) {
    NavigationRail { /* 80.dp wide */ }
    BoxWithConstraints(Modifier.weight(1f)) {
        // maxWidth here is (screenWidth - 80.dp)
        // NOT screenWidth!
    }
}
</code></pre>

<p>这正是 <code>BoxWithConstraints</code> 存在的意义。<code>LocalConfiguration</code> 会给出错误的答案。</p>

<h2>有界约束与无界约束</h2>

<p>这里有一个容易引起混淆的细微差别：</p>

<pre><code class="kotlin">LazyColumn {
    item {
        BoxWithConstraints {
            // maxHeight == Dp.Infinity !!!
            // Because LazyColumn offers infinite vertical space
        }

        }
</code></pre>

<p><code>maxHeight</code> 的值为 <code>Infinity</code>，因为 <code>LazyColumn</code> 不会限制其子元素的高度，它会滚动。这是预期行为，但这意味着你不能在可滚动容器内使用 <code>maxHeight</code> 来进行响应式布局。</p>

<h2>实际用例</h2>

<h3>用例 1：响应式卡片布局</h3>

<p>此卡片会根据<strong>可用空间</strong>（而非屏幕方向）在垂直和水平布局之间切换：</p>

<pre><code class="kotlin">@Composable
fun ResponsiveProductCard(
    imageUrl: String,
    title: String,
    description: String,
    modifier: Modifier = Modifier
) {
    BoxWithConstraints(modifier = modifier) {
        val isWide = maxWidth &gt; 400.dp

        if (isWide) {
            // Horizontal layout: image left, text right
            Row(
                modifier = Modifier.fillMaxWidth(),
                horizontalArrangement = Arrangement.spacedBy(16.dp)
            ) {
                AsyncImage(
                    model = imageUrl,
                    contentDescription = null,
                    modifier = Modifier
                        .weight(0.4f)
                        .aspectRatio(1f)
                )
                Column(
                    modifier = Modifier.weight(0.6f),
                    verticalArrangement = Arrangement.Center
                ) {
                    Text(title, style = MaterialTheme.typography.headlineSmall)
                    Spacer(Modifier.height(8.dp))
                    Text(description, style = MaterialTheme.typography.bodyMedium)
                }
            }
        } else {
            // Vertical layout: image top, text bottom
            Column {
                AsyncImage(
                    model = imageUrl,
                    contentDescription = null,
                    modifier = Modifier
                        .fillMaxWidth()
                        .aspectRatio(16f / 9f)
                )
                Spacer(Modifier.height(12.dp))
                Text(title, style = MaterialTheme.typography.headlineSmall)
                Spacer(Modifier.height(4.dp))
                Text(description, style = MaterialTheme.typography.bodyMedium)
            }
        }
    }
}
</code></pre>

<p><strong>为什么这比检查屏幕尺寸更好</strong>：将此卡片放入双列网格中，即使在平板电脑上，它也会自动显示垂直布局。它真正实现了对<em>其</em>容器的响应式布局。</p>

<h3>用例 2：动态文本大小</h3>

<p>自动调整文本大小以适应可用宽度：</p>

<pre><code class="kotlin">@Composable
fun AutoSizeTitle(
    text: String,
    modifier: Modifier = Modifier
) {
    BoxWithConstraints(modifier = modifier) {
        val fontSize = when {
            maxWidth &lt; 200.dp -&gt; 14.sp
            maxWidth &lt; 300.dp -&gt; 18.sp
            maxWidth &lt; 400.dp -&gt; 24.sp
            else -&gt; 32.sp
        }

        Text(
            text = text,
            fontSize = fontSize,
            maxLines = 1,
            overflow = TextOverflow.Ellipsis,
            modifier = Modifier.fillMaxWidth()
        )
    }
}
</code></pre>

<h3>用例 3：基于宽度的网格项数</h3>

<p>动态计算网格列数：</p>

<pre><code class="kotlin">@Composable
fun AdaptiveGrid(
    items: List&lt;Item&gt;,
    modifier: Modifier = Modifier
) {
    BoxWithConstraints(modifier = modifier.fillMaxWidth()) {
        val columns = maxOf(1, (maxWidth / 160.dp).toInt())

        LazyVerticalGrid(
            columns = GridCells.Fixed(columns),
            horizontalArrangement = Arrangement.spacedBy(8.dp),
            verticalArrangement = Arrangement.spacedBy(8.dp)
        ) {
            items(items) { item -&gt;
                GridItem(item)
            }
        }
    }
}
</code></pre>

<h2>性能陷阱和最佳实践</h2>

<h3>何时不应使用 BoxWithConstraints</h3>

<p><strong>1. 当一个简单的修饰符就足够时</strong></p>

<pre><code class="kotlin">// ❌ Overkill
BoxWithConstraints {
    Box(Modifier.size(maxWidth * 0.5f, maxHeight * 0.5f))
}
</code></pre>

<pre><code class="kotlin">// ✅ Just use fillMaxSize with a fraction
Box(Modifier.fillMaxSize(0.5f))
</code></pre>

<p><strong>2. 当你只是检查屏幕方向时</strong></p>

<pre><code class="kotlin">// ❌ Using BoxWithConstraints for screen-level decisions
BoxWithConstraints {
    if (maxWidth &gt; maxHeight) LandscapeLayout() else PortraitLayout()
}
</code></pre>

<pre><code class="kotlin">// ✅ Use LocalConfiguration for screen-level decisions
val configuration = LocalConfiguration.current
if (configuration.orientation == Configuration.ORIENTATION_LANDSCAPE) {
    LandscapeLayout()
} else {
    PortraitLayout()
}
</code></pre>

<p><strong>3.在 LazyColumn/LazyRow 元素内部</strong></p>

<p>这是一个常见的错误：</p>

<pre><code class="kotlin">// ❌ Performance issue
LazyColumn {
    items(1000) { index -&gt;
        BoxWithConstraints {  // Subcomposition per item!
            ItemContent(index)
        }
    }
}
</code></pre>

<p>每个元素都会触发子组合。如果元素数量为 1000，则在滚动过程中会重复执行子组合。相反，将 <code>BoxWithConstraints</code> 提升到父级：</p>

<pre><code class="kotlin">// ✅ Single subcomposition
BoxWithConstraints {
    val itemHeight = if (maxWidth &gt; 400.dp) 120.dp else 80.dp
    LazyColumn {
        items(1000) { index -&gt;
            ItemContent(index, height = itemHeight)
        }
    }
}
</code></pre>

<h2>更轻量级的替代方案</h2>

<p><strong>基于百分比的尺寸：</strong></p>

<pre><code class="kotlin">// Use Modifier.fillMaxWidth(fraction)
Box(Modifier.fillMaxWidth(0.8f))
</code></pre>

<pre><code class="kotlin">// Use Modifier.weight in Row/Column
Row {
    Box(Modifier.weight(1f))  // 25%
    Box(Modifier.weight(3f))  // 75%
}For aspect ratio:
</code></pre>

<pre><code class="kotlin">Box(Modifier.aspectRatio(16f / 9f))
</code></pre>

<p><strong>不使用子组件的自定义尺寸逻辑：</strong></p>

<pre><code class="kotlin">// Custom Layout is lighter than SubcomposeLayout
Layout(
    content = { /* your content */ }
) { measurables, constraints -&gt;
    // Full control over measurement without subcomposition cost
    // But you can't make composition decisions here
}
</code></pre>

<p><strong>窗口尺寸类（屏幕级响应式）：</strong></p>

<pre><code class="kotlin">// Material3's adaptive APIs
val windowSizeClass = calculateWindowSizeClass(activity)
when (windowSizeClass.widthSizeClass) {
    WindowWidthSizeClass.Compact -&gt; CompactLayout()
    WindowWidthSizeClass.Medium -&gt; MediumLayout()
    WindowWidthSizeClass.Expanded -&gt; ExpandedLayout()
}
</code></pre>

<h2>重要陷阱和边界情况</h2>

<h3>1. 固有尺寸不起作用</h3>

<p>这将导致崩溃：</p>

<pre><code class="kotlin">Row {
    Text(
        "Hello",
        modifier = Modifier.height(IntrinsicSize.Min)
    )
    BoxWithConstraints {
        // CRASH: "Asking for intrinsic measurements of 
        // SubcomposeLayout layouts is not supported"
    }
}
</code></pre>

<p><code>SubcomposeLayout</code> 无法回答“你的固有尺寸是多少？”因为它在接收到实际约束之前无法确定要组合的内容。</p>

<p><strong>解决方法</strong>：为 <code>BoxWithConstraints</code> 添加显式的大小修饰符：</p>

<pre><code class="kotlin">BoxWithConstraints(Modifier.height(100.dp)) {
    // Now intrinsic measurement isn't needed
}
</code></pre>

<h3>2. 首次组合时序</h3>

<p>在首次组合时，<code>BoxWithConstraints</code> 还没有约束。子元素会先使用占位符值进行组合，然后再使用实际约束重新组合。这意味着：</p>

<ul>
<li><p>内部的 <code>LaunchedEffect</code> 等效果可能会触发两次</p></li>
<li><p>基于约束的初始状态计算可能需要保护</p></li>
</ul>


<p><strong>最佳实践</strong>：保护基于约束的逻辑：</p>

<pre><code class="kotlin">BoxWithConstraints {
    if (constraints.hasBoundedWidth) {
        val columns = (maxWidth / 160.dp).toInt()
        // Safe to use
    }
}
</code></pre>

<h3>3. 通常不需要嵌套 <code>BoxWithConstraints</code></h3>

<p>如果你发现自己需要嵌套它们：</p>

<pre><code class="kotlin">BoxWithConstraints {
    BoxWithConstraints {  // Usually unnecessary
        // ...
    }
}
</code></pre>

<p>内部的 <code>BoxWithConstraints</code> 具有相同的约束（除非被修改）。只需使用外部作用域即可。</p>

<h3>4. 状态提升的重要性（详见此处）</h3>

<p>由于测量期间内容会被子组合，因此在 <code>BoxWithConstraints</code> 内部创建的状态具有特殊的生命周期特征：</p>

<pre><code class="kotlin">BoxWithConstraints {
    // This state is created during MEASUREMENT, not composition
    var count by remember { mutableStateOf(0) }

    // If parent re-measures without recomposing, this state persists
    // But if the slot is "recycled", it might reset
}
</code></pre>

<p><strong>最佳实践</strong>：将重要状态提升到 <code>BoxWithConstraints</code> 之上：</p>

<pre><code class="kotlin">var count by remember { mutableStateOf(0) }
</code></pre>

<pre><code class="kotlin">BoxWithConstraints {
    // count is stable regardless of subcomposition lifecycle
    Text("Count: $count")
}
</code></pre>

<h3>5. propagateMinConstraints 参数</h3>

<pre><code class="kotlin">BoxWithConstraints(propagateMinConstraints = true) {
    // Children now MUST be at least minWidth x minHeight
    // This can cause unexpected overflow if children 
    // have their own size requirements
}
</code></pre>

<p>仅当你明确希望强制子元素具有最小尺寸时才使用此参数。</p>

<h2>总结：何时使用 BoxWithConstraints</h2>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*ReLZsAjntRlcLXMJlYEUbw.png" alt="" /></p>

<h2>结语</h2>

<p><code>BoxWithConstraints</code> 是一个表面看似简单，但当你理解其底层机制后，会发现它蕴含着丰富的内涵。它不仅仅是一个<strong>“告诉你自身大小的盒子”</strong>，而是一种允许元素组合决策依赖于布局信息的机制。</p>

<p>当你真正需要<strong>容器感知响应式布局</strong>时，请使用它。如果更简单的修饰符就能满足需求，则应避免使用它。永远记住：<strong>它会带来额外的开销，因为它本质上与常规代码合成不同。</strong></p>

<p><strong>最好的代码并非使用最复杂的 API，而是使用最适合当前任务的 API。</strong></p>

<p><strong>如果这篇深度解析对你有所帮助，请考虑与你的团队分享。知识共享才能促进 Android 社​​区的发展。</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Compose中的IntrinsicSize实战指南]]></title>
    <link href="https://alexhilton.github.io/blog/2026/02/02/mastering-intrisnicsize/"/>
    <updated>2026-02-02T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/02/02/mastering-intrisnicsize</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Mastering IntrinsicSize in Jetpack Compose: A Real-World Guide」，原文链接<a href="https://proandroiddev.com/mastering-intrinsicsize-in-jetpack-compose-a-real-world-guide-a57ab90c6556">https://proandroiddev.com/mastering-intrinsicsize-in-jetpack-compose-a-real-world-guide-a57ab90c6556</a>，由Sehaj kahlon发布于2026年1月24日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/1*5575GBQlUI38hH2ThTnGBQ.png" title="auto auto" ></a></p>

<!-- more -->


<p>如果你一直在使用 Jetpack Compose，你可能遇到过布局行为与预期不符的令人沮丧的情况。也许是 <code>Box</code> 中的 <code>weight()</code> 修饰符导致了奇怪的行为，或者你的分层 UI 元素大小不正确。</p>

<p>这时 <strong><code>IntrinsicSize</code></strong> 就派上用场了——相信我，一旦你理解了它，它将成为你 Compose 工具箱中最强大的工具之一。</p>

<h2>问题：一个真实的生产场景</h2>

<p>最近，我正在为一个应用程序构建详情页面。设计要求：</p>

<ol>
<li><p>顶部放置一张<strong>标题图片</strong></p></li>
<li><p>一张<strong>卡片</strong>，与标题图片重叠 56dp</p></li>
<li><p>从标题图片到柔和灰色背景的<strong>平滑过渡</strong></p></li>
</ol>


<p>我们想要实现的效果如下：</p>

<blockquote><p><strong><em>💡 设计目标：</em></strong> <em>创建一个卡片，使其优雅地与标题图片重叠，并实现无缝的背景过渡。</em></p></blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:904/1*7wqxO7TLyV_oaH9wQxz6pw.png" alt="" /></p>

<h2>挑战</h2>

<p>我需要创建一个包含两层的“盒子Box”：</p>

<ul>
<li><p><strong>图层 1（背景）：</strong> 顶部 56dp 的透明区域，然后是柔和的灰色背景</p></li>
<li><p><strong>图层 2（卡片）：</strong> 绘制在顶部的实际卡片内容</p></li>
</ul>


<p>这是我的第一个作品尝试：</p>

<pre><code class="kotlin">@Composable
fun OverlappingCardContent(overlapHeight: Dp) {
    Box(modifier = Modifier.fillMaxWidth()) {
        // Layer 1: Background
        Column(modifier = Modifier.fillMaxWidth()) {
            Spacer(modifier = Modifier.height(overlapHeight)) // 56dp transparent
            Box(
                modifier = Modifier
                    .fillMaxWidth()
                    .weight(1f)  // Fill remaining space with soft background
                    .background(color = Color.LightGray)
            )
        }

        // Layer 2: Card
        Card(modifier = Modifier.fillMaxWidth()) {
            // Card content...
        }
    }
}
</code></pre>

<p><strong>但是这行不通！</strong> 😩</p>

<p><code>weight(1f)</code>修饰符导致了问题。布局要么意外地折叠，要么意外地展开。</p>

<h2>了解根本原因</h2>

<p>为了理解为什么会失败，我们先回顾一下 Compose 布局的工作原理。</p>

<h3>Compose 布局：单遍系统</h3>

<p>为了提高性能，Compose 布局采用<strong>单次遍历</strong>方式测量布局。每个可组合元素：</p>

<ol>
<li><p>从父元素接收约束</p></li>
<li><p>测量其子元素</p></li>
<li><p>确定自身大小</p></li>
<li><p>放置其子元素</p></li>
</ol>


<p>问题在于：当像 <code>Box</code> 这样的父元素测量其子元素时，每个子元素都不知道其他子元素的大小。它们是独立测量的。</p>

<h2>为什么 <code>weight(1f)</code> 会失败</h2>

<p><code>weight()</code> 修饰符表示：“占据剩余空间的 X 倍。”</p>

<p>但是剩余空间指的是<strong>什么</strong>？<code>Box</code> 元素尚未确定自身高度——它正在等待子元素告知其自身大小。这会造成循环依赖：</p>

<pre><code class="bash">Box: "Children, how tall are you?"
  ├─ Column: "I need 56dp + whatever weight(1f) gives me"
  │

  "But weight depends on your height, Box!"
  └─ Card: "I need ~180dp for my content"  Box: "I'm confused..." 🤯
</code></pre>

<h2>引入 IntrinsicSize：打破循环依赖</h2>

<p><code>IntrinsicSize</code> 是 Compose 在实际测量之前向子元素询问一个<strong>假设性问题</strong> 的方式：</p>

<blockquote><p><strong><em>“如果我给你无限的空间，你需要的最小（或最大）高度是多少？”</em></strong></p></blockquote>

<h2>IntrinsicSize.Min 与 IntrinsicSize.Max</h2>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*IUoEVdIal11Nwn3WFG6w0A.png" alt="" /></p>

<h2>修复</h2>

<pre><code class="kotlin">Box(
    modifier = Modifier
        .fillMaxWidth()
        .height(IntrinsicSize.Min)  // ← The magic line!
) {
    // Layer 1: Background
    Column(modifier = Modifier.fillMaxWidth()) {
        Spacer(modifier = Modifier.height(56.dp))
        Box(
            modifier = Modifier
                .fillMaxWidth()
                .weight(1f)
                .background(color = Color.LightGray)
        )
    }

    // Layer 2: Card
    Card(modifier = Modifier.fillMaxWidth()) {
        // Card content...
    }
}
</code></pre>

<p>现在布局正常了！原因如下：</p>

<h2>IntrinsicSize.Min 如何解决冲突</h2>

<p>当 <code>Box</code> 使用 <code>height(IntrinsicSize.Min)</code> 时，它会询问每个子元素：</p>

<p><strong>列的最小固有高度：</strong></p>

<pre><code class="bash">Spacer: 56dp (fixed)
Weighted Box: 0dp (minimum, can shrink to nothing)
Total: 56dp
</code></pre>

<p><strong>卡片的最小固有高度：</strong></p>

<pre><code class="bash">Status row + Title + Subtitle + Button + Padding = ~180dp
Total: ~180dp
</code></pre>

<p><strong>Box 选择：</strong> <code>max(56dp, 180dp)</code> = <strong>180dp</strong></p>

<p>为什么是 <code>max()</code>？因为 Box 的高度需要足以容纳<strong>所有</strong>子元素！</p>

<p>现在布局完全知道如何高度要合适，<code>weight(1f)</code> 可以正常工作：</p>

<ul>
<li><p>盒子高度：180dp</p></li>
<li><p>间距：56dp</p></li>
<li><p>加权背景盒子：180dp — 56dp = <strong>124dp</strong></p></li>
</ul>


<h2>完整解决方案：重叠卡片式 UI</h2>

<p>以下是支持此 UI 的最终代码：</p>

<pre><code class="kotlin">private val OVERLAP_HEIGHT = 56.dp
</code></pre>

<pre><code class="kotlin">@Composable
fun HeaderWithOverlappingCard(imageHeight: Dp) {
    LazyColumn {
        // Create overlap: position content before header ends
        item { 
            Spacer(modifier = Modifier.height(imageHeight - OVERLAP_HEIGHT)) 
        }

        item {
            OverlappingCardContent(overlapHeight = OVERLAP_HEIGHT)
        }
    }
}
</code></pre>

<pre><code class="kotlin">@Composable
fun OverlappingCardContent(overlapHeight: Dp) {
    Box(
        modifier = Modifier
            .fillMaxWidth()
            .height(IntrinsicSize.Min)
    ) {
        // Layer 1: Background transition
        Column(modifier = Modifier.fillMaxWidth()) {
            Spacer(modifier = Modifier.height(overlapHeight)) // Transparent over header
            Box(
                modifier = Modifier
                    .fillMaxWidth()
                    .weight(1f)
                    .background(color = SoftGrayBackground)
            )
        }

        // Layer 2: Card content (drawn on top)
        Card(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 8.dp),
            shape = RoundedCornerShape(12.dp)
        ) {
            Column(modifier = Modifier.padding(24.dp)) {
                Text("✓ Success", color = Color.Green)
                Text("₹100 Cashback", style = MaterialTheme.typography.h4)
                Text("Transaction complete")
                Spacer(modifier = Modifier.height(16.dp))
                Button(onClick = {}) { Text("View Details") }
            }
        }
    }
}
</code></pre>

<h2>可视化分解</h2>

<pre><code class="kotlin">imageHeight = 200dp
overlapHeight = 56dp
</code></pre>

<pre><code class="bash">LazyColumn positions:
├─ Spacer: 144dp (200 - 56)
└─ OverlappingCardContent starts at Y = 144dp
</code></pre>

<pre><code class="bash">Inside OverlappingCardContent (height = 180dp via IntrinsicSize.Min):
├─ Column Layer:
│   ├─ Spacer: 56dp (transparent, header shows through)
│   └─ Background Box: 124dp (soft gray)
└─ Card Layer: 180dp (drawn from top, overlaps header)
</code></pre>

<h2>IntrinsicSize 的常见用例</h2>

<h3>1. 一行中高度相同的按钮</h3>

<p><strong>问题：</strong> 文本长度不同的按钮最终高度也不同。</p>

<p><strong>解决方案：</strong></p>

<pre><code class="kotlin">Row(modifier = Modifier.height(IntrinsicSize.Min)) {
    Button(
        modifier = Modifier.weight(1f).fillMaxHeight(),
        onClick = {}
    ) { Text("Short") }

    Button(
        modifier = Modifier.weight(1f).fillMaxHeight(),
        onClick = {}
    ) { Text("This is a much\nlonger button\nwith more text") }
}
</code></pre>

<p><strong>结果：</strong> 两个按钮的高度都与最高的按钮高度相同。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*IubiRLP4sFk4rl-uvLwwKA.png" alt="" /></p>

<h3>2. 分隔线与父元素高度匹配</h3>

<p><strong>问题：</strong> 分隔线无法拉伸以匹配多行内容。</p>

<p><strong>解决方案：</strong></p>

<pre><code class="kotlin">Row(modifier = Modifier.height(IntrinsicSize.Min)) {
    Text("Left content\nwith multiple\nlines")

    Divider(
        modifier = Modifier
            .fillMaxHeight()
            .width(1.dp)
    )

    Text("Right")
}
</code></pre>

<p><strong>结果：</strong> 分隔线可以拉伸以匹配多行文本的高度。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*nFJrnQVIaT7Kfh70JDofOA.png" alt="" /></p>

<h3>3. 带有动态内容和固定覆盖层的卡片</h3>

<p><strong>问题：</strong> 覆盖层需要与卡片宽度匹配，但卡片宽度是动态的。</p>

<p><strong>解决方案：</strong></p>

<pre><code class="kotlin">Box(modifier = Modifier.width(IntrinsicSize.Max)) {
    Card {
        Column {
            Text("Dynamic content here...")
            // More content
        }
    }

    // Badge that should match card width
    Badge(
        modifier = Modifier
            .align(Alignment.TopEnd)
            .fillMaxWidth()
    )
}
</code></pre>

<h2>性能相关注意事项 ⚠️</h2>

<p><code>IntrinsicSize</code> 会触发<strong>两遍测量</strong>：</p>

<ol>
<li><p>第一遍：查询固有尺寸</p></li>
<li><p>第二遍：实际测量和布局</p></li>
</ol>


<p>这会带来一定的性能开销。谨慎使用：</p>

<h2>✅ 适用场景</h2>

<ul>
<li><p>复杂的分层 UI（例如我们的重叠卡片）</p></li>
<li><p>使同级元素大小一致</p></li>
<li><p>使分隔符/分隔线与内容匹配</p></li>
<li><p>需要子元素协调大小时</p></li>
</ul>


<h2>❌ 避免使用场景</h2>

<ul>
<li><p>在可能会重复数百次的 <code>LazyColumn</code>/<code>LazyRow</code> 元素中</p></li>
<li><p>深度嵌套的固有尺寸</p></li>
<li><p>当固定尺寸或 <code>wrapContentHeight()</code> 可以满足需求时</p></li>
<li><p>在性能关键路径中</p></li>
</ul>


<h2>要点</h2>

<ol>
<li><p><strong><code>IntrinsicSize.Min</code></strong> = &ldquo;使用子元素所需的最小高度&rdquo;</p></li>
<li><p><strong><code>IntrinsicSize.Max</code></strong> = &ldquo;使用子元素可能需要的最大高度&rdquo;</p></li>
<li><p>它解决了父元素和子元素尺寸之间的循环依赖关系</p></li>
<li><p>对于子元素使用 <code>weight()</code> 或 <code>fillMaxHeight()</code> 的分层 UI 至关重要</p></li>
<li><p>会带来性能开销——请谨慎使用</p></li>
</ol>


<h2>结论</h2>

<p><code>IntrinsicSize</code> 看似是一个小众的 API，但它对于在 Compose 中构建复杂的 UI 至关重要。我们构建的重叠卡片模式只是其中一个例子——当你需要协调同级元素的大小，或者当分层布局需要“统一”尺寸时，你会发现它非常实用。</p>

<p>下次当你的 Compose 布局在使用 <code>weight()</code> 或 <code>fillMaxHeight()</code> 时出现异常行为时，请记住：<strong>IntrinsicSize 或许能帮你解决问题</strong>。</p>

<p><em>如果你有任何疑问或发现了 IntrinsicSize 的其他创意用法，请在下方留言！</em></p>

<p><strong>关注我，获取更多 Android 开发技巧。</strong></p>
]]></content>
  </entry>
  
</feed>
