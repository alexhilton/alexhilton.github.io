<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Effectiveandroid | 稀有猿诉]]></title>
  <link href="https://alexhilton.github.io/blog/categories/effectiveandroid/atom.xml" rel="self"/>
  <link href="https://alexhilton.github.io/"/>
  <updated>2026-06-06T11:46:16+00:00</updated>
  <id>https://alexhilton.github.io/</id>
  <author>
    <name><![CDATA[Alex Hilton]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[车载系统中的可扩展UI：从UI嵌入到系统窗口编排]]></title>
    <link href="https://alexhilton.github.io/blog/2026/06/06/scalable-ui-in-android/"/>
    <updated>2026-06-06T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/06/06/scalable-ui-in-android</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Scalable UI in Android Automotive OS: From UI Embedding to System Window Orchestration」，原文链接<a href="https://medium.com/proandroiddev/scalable-ui-in-aaos-from-ui-embedding-to-system-window-orchestration-dae03b335eee">https://medium.com/proandroiddev/scalable-ui-in-aaos-from-ui-embedding-to-system-window-orchestration-dae03b335eee</a>，由Daniel Georg发布于20264月23日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*9Xu2aaQTu6ecj9iVhBWkfA.png" title="auto auto" ></a></p>

<!-- more -->


<p>在 Android Automotive 的原生主屏幕上，所有内容都位于同一个 Activity 中。CarLauncher 会同时绘制地图、媒体卡片和状态小部件。切换应用意味着需要替换整个 UI。 <a href="https://source.android.com/docs/automotive/scalableui">Scalable UI</a> 框架采用了不同的方法：每个面板都拥有自己独立的 Activity，所有 Activity 并排运行，完全由 RRO 覆盖层中的声明式 XML 驱动。</p>

<p>这源于另一个问题。在我之前的<a href="https://medium.com/@passenger6/remote-compose-in-android-automotive-os-embed-rich-ui-without-the-usual-pain-90b857d59d8c">文章</a>中，我探讨了远程组合 (Remote Compose) 作为在 AAOS 中跨应用边界共享 UI 的新方法。它提供了一种解决方案，避免了通常的跨进程开销和耦合。然而，来自 <a href="https://www.linkedin.com/in/ralph-thomas"><strong>Ralph Thomas</strong></a> <strong>(Google)</strong> 的一条<a href="https://www.linkedin.com/feed/update/urn:li:activity:7444753922419761152?commentUrn=urn%3Ali%3Acomment%3A%28activity%3A7444753922419761152%2C7444763415753060352%29&amp;dashCommentUrn=urn%3Ali%3Afsd_comment%3A%287444763415753060352%2Curn%3Ali%3Aactivity%3A7444753922419761152%29">评论</a> 改变了我的想法更广阔的视野引领我深入探索，彻底改变了我的视角。</p>

<p>我一直密切关注着可扩展用户界面（Scalable UI），但那条评论让我停下来思考另一个问题。远程配置（Remote Compose）解决了嵌入外部用户界面而不耦合的问题，但如果<strong>嵌入本身</strong>是一种错误的抽象方式呢？</p>

<blockquote><p>我们不再需要在启动器内部构建组件，因为在可扩展用户界面时代，应用程序<strong>本身就是组件</strong>。</p></blockquote>

<p>OEM厂商现在不再使用启动器来协调嵌入的内容，而是通过RRO覆盖层在系统UI配置中声明布局。传统意义上的“主页”Activity已不复存在，因为系统本身就成为了启动器。每个应用程序都拥有自己的域，独立运行，无需共享界面、进程间通信（IPC）或嵌入。</p>

<p>在这种模式下，包括面板大小、Activity位置和过渡效果在内的所有配置都用XML声明。从 OEM 的角度来看，这意味着无需编写任何 Java/Kotlin 代码，只需 XML 覆盖层，即可实现复杂的窗口行为。</p>

<p>可扩展 UI 背后的完整愿景有一个名称，至少我个人认为它应该叫：<strong>状态驱动的驾驶舱编排</strong>。整个驾驶舱，包括应用程序、系统栏、HUN 和覆盖层，都变成了一个协调一致的界面，在同步状态之间切换，而不是一系列独立管理的窗口。系统 UI 在 XML 中声明状态；窗口管理器驱动每个应用程序、每个系统栏和每个覆盖层同步完成状态转换。</p>

<p>本文的其余部分将介绍这在实践中是如何实现的：构建模块、完全使用 XML 构建的概念验证仪表板，以及这种架构带来的权衡取舍。我还会介绍我开发的用于创建可扩展 UI 的可视化编辑器，无需手动编写 XML。</p>

<p>沉重的遗留问题：启动器为何沦为噩梦</p>

<p>当标准的嵌入技术无法满足复杂的多窗口布局需求时，下一步就是深入系统内部，例如，创建一个 CustomDisplayAreaProvider 并直接编排 TaskDisplayArea (TDA)。</p>

<p>我花了几个月的时间来实现这种自定义的底层窗口逻辑。要实际放置和动画化这些显示区域，你必须在系统 UI 中注册一个自定义的 TaskDisplayAreaOrganizer。设置布局需要一个 WindowContainerTransaction 来告知 WindowManager 新的 TDA 边界，而动画则必须逐帧驱动，直接使用 SurfaceControl.Transaction 控制显示区域的边界。</p>

<p>每个过渡效果都是一个手动编写的 ValueAnimator，它插值边界并将其应用到 SurfaceFlinger，可以直接应用，也可以通过扩展 WMShell 过渡框架并添加自定义的 TransitionHandler 来实现。到了这一步，你不再是在编写应用程序代码，而是在编写窗口管理管道的一部分。</p>

<p>WinScope 成了你在这个过程中唯一的帮手。它是唯一能让你直观地理解窗口管理器中层、焦点转移和可见性标志等复杂机制的工具。没有它，你基本上就是在盲目摸索。</p>

<p>经过数月的工程开发，你终于运行了兼容性测试套件 (CTS)。几个小时后，结果显示你的信息娱乐系统不再符合 CTS 标准。这标志着一场痛苦的 bug 修复马拉松的开始，每次修复 Google 强制测试中的 bug 都会破坏你自定义的 UX。自定义窗口管理逻辑是一把双刃剑，很容易导致认证进度被打乱。</p>

<p>在分析了旧方法失效的原因之后，下一个问题显而易见：是否存在更好的抽象方案？</p>

<p>编排 vs. 嵌入</p>

<p>具体来说，可扩展 UI 由四个基本元素构成，全部以 XML 声明。“驾驶舱状态”是唯一的数据源——你不再需要编写动画代码，而是定义：</p>

<ul>
<li><strong>任务面板：</strong> 矩形容器，映射到专用的根任务栈，用于承载完整的应用程序（导航或媒体），作为功能性组件。</li>
</ul>


<pre><code class="xml">&lt;TaskPanel id="navigation" defaultVariant="@id/base" displayId="0"&gt;
&lt;/TaskPanel&gt;
</code></pre>

<ul>
<li><strong>装饰面板：</strong> 用于注入自定义 UI/覆盖层的通用容器，可与应用程序任务完美同步。</li>
</ul>


<pre><code class="xml">&lt;DecorPanel id="navigation_decor" defaultVariant="@id/base" displayId="0"&gt;
&lt;/DecorPanel&gt;
</code></pre>

<ul>
<li><strong>变体：</strong> 管理精确边界、Z 轴顺序和可见性的视觉状态。</li>
</ul>


<pre><code class="xml">&lt;Variant id="@+id/base"&gt; &lt;/Variant&gt;
</code></pre>

<p><strong>过渡：</strong> 由窗口管理器协调的同步动画，用于根据系统事件在变体之间切换面板。 OEM 厂商还可以定义自己的自定义事件，并通过面板控制器触发这些事件，或将其绑定到意图，从而将系统扩展到内置的 <em>System</em>* 事件之外。</p>

<pre><code class="xml">&lt;Transitions&gt;
        &lt;Transition onEvent="_System_TaskOpenEvent" fromVariant="@id/base" toVariant="@id/open" duration="300" interpolator="@android:anim/accelerate_decelerate_interpolator"/&gt;
 &lt;/Transitions&gt;
</code></pre>

<p>以下是一个导航面板的实际示例，该面板定义了两种视觉状态（变体）和一个动画过渡：</p>

<pre><code class="xml">&lt;TaskPanel id="navigation" defaultVariant="@id/base" displayId="0"&gt;
    &lt;Variant id="@+id/base"&gt;
        &lt;Layer layer="2"/&gt;
        &lt;Visibility isVisible="true"/&gt;
        &lt;Alpha alpha="1.0"/&gt;
        &lt;Bounds left="0dp" top="0dp" right="850dp" bottom="1600dp"/&gt;
    &lt;/Variant&gt;
    &lt;Variant id="@+id/open"&gt;
        &lt;Layer layer="2"/&gt;
        &lt;Visibility isVisible="true"/&gt;
        &lt;Alpha alpha="1.0"/&gt;
        &lt;Bounds left="0dp" top="0dp" right="2480dp" bottom="1600dp"/&gt;
    &lt;/Variant&gt;
    &lt;Transitions&gt;
        &lt;Transition onEvent="_System_TaskOpenEvent" fromVariant="@id/base" toVariant="@id/open" duration="300" interpolator="@android:anim/accelerate_decelerate_interpolator"/&gt;
    &lt;/Transitions&gt;
&lt;/TaskPanel&gt;
</code></pre>

<p><strong>重要性：</strong> 请注意，计算 850dp 和 2480dp 之间的插值无需任何代码。<strong>可扩展 UI</strong> 框架基于此 XML 协调过渡，确保窗口管理器能够与容器完美同步地缩放底层任务。</p>

<p>这就是 <strong>状态驱动的驾驶舱编排</strong> 的实际应用——系统栏和 HUN 也是可扩展 UI 面板，由与应用程序相同的驾驶舱状态驱动。</p>

<p>这直接解决了之前提到的 CTS 问题。由于该框架已预先通过认证并符合 CTS 标准，因此消除了后期测试失败的主要风险。过去需要数月时间进行自定义窗口逻辑设置以及可能出现的认证回归才能实现的功能，现在<strong>开箱即用</strong>。</p>

<p>以上是核心组件。我不会在这里重写手册——如果你需要完整的技术规格、库源代码，或者想了解它是如何集成到 SystemUI 中的，请查看以下资源：</p>

<ul>
<li><p><strong>官方文档：</strong> <a href="https://source.android.com/docs/automotive/scalableui">带有可扩展 UI 的高级窗口</a></p></li>
<li><p><strong>库：</strong> <a href="https://cs.android.com/android/platform/superproject/+/android-latest-release:packages/apps/Car/systemlibs/car-scalable-ui-lib/">car-scalable-ui-lib</a>（声明式核心）</p></li>
<li><p><strong>窗口 Shell：</strong> <a href="https://cs.android.com/android/platform/superproject/+/android-latest-release:packages/services/Car/libs/car-wm-shell-lib/">car-wm-shell-lib</a>（核心任务布局和内边距管理）</p></li>
<li><p><strong>SystemUI 集成：</strong> <a href="https://cs.android.com/android/platform/superproject/+/android-latest-release:packages/apps/Car/SystemUI/src/com/android/systemui/car/wm/scalableui/">可缩放 UI 窗口管理器逻辑</a></p></li>
<li><p><strong>代码实验室和示例：</strong> <a href="https://cs.android.com/android/platform/superproject/+/android-latest-release:packages/apps/Car/References/scalable-ui/">参考实现</a></p></li>
</ul>


<p>验证：无需 Kotlin/Java 代码即可实现的 3 列仪表盘</p>

<p>一旦所有三个部分都就位，框架就会启动，发现你的面板清单，并开始进行编排。你现在可以构建自己的自定义多窗口信息娱乐系统了。</p>

<p>完整的 PoC，包括配置叠加层和所有面板 XML，也可以在我的 <a href="https://github.com/passenger6/MultiPanelLandscapeRRO">GitHub</a> 上找到。</p>

<p>超越 XML：面板控制器</p>

<p>任何设计都存在权衡取舍，而我想在此深入探讨的，是你在决定使用 Scalable UI 构建生产级控制面板之前，务必了解的一点。我通过自身实验观察到的行为，可能是出于有意设计，也可能是集中式编排带来的副作用。我并不总是能确定究竟是哪一种，而且在实践中，区分二者远不如理解它如何影响实际部署重要。</p>

<p><strong>系统 UI 成为单点故障</strong></p>

<p>这种架构转变从根本上改变了控制面板的故障模型。由于系统本身充当了编排器的角色，系统 UI 实际上成为了整个 UI 的单点故障。</p>

<p>在 Scalable UI 出现之前，启动器崩溃可能会导致主屏幕消失，但状态栏、HVAC 控制、通知和自定义 OEM SystemUIOverlayWindows 仍会继续独立运行。相反，系统 UI 崩溃会导致这些系统界面瘫痪，而启动器和托管应用程序则“幸存”下来。</p>

<p>使用 Scalable UI 时，当系统 UI 崩溃时，框架会移除系统 UI 作为 TaskOrganizer 创建的所有面板根任务。这些面板内的所有应用，无论是导航、电话、设置还是其他正在运行的活动，都会连同其任务一起被终止。系统 UI 会快速重启，但由于 Scalable UI 不会保存上次的活动状态，它会从初始配置而非用户离开时的状态重建面板布局。只有配置为自动启动的应用才会自动重启，并且只会重启到其默认的面板位置。所有应用状态、导航历史记录和滚动位置都会丢失。例如，在我的概念验证中，无论用户重新排列了哪些面板或哪个面板处于活动状态，系统始终以三列布局重启。</p>

<p>对于 OEM 厂商而言，这改变了系统 UI 故障时的风险。添加到系统 UI 的每个新功能、每个面板控制器、过渡动画器和事件分发器都会扩大单次崩溃的影响范围。面板控制器中一个未被捕获的异常，不仅会导致驾驶员失去状态栏，还会导致整个信息娱乐体验的丧失。</p>

<p><strong>我希望添加的功能</strong></p>

<p>除了上述权衡之外，我还希望 Scalable UI 能添加以下几个功能：</p>

<ul>
<li><p><strong>Scalable UI 配置的热重载。</strong> 目前，应用更新后的面板布局需要终止 System UI，这会销毁所有面板根任务并终止其中托管的所有应用程序。如果有一种热重载机制，能够在不重启 System UI 的情况下获取新的面板定义、变体更改和过渡更新，那么 OEM 厂商就可以在运行时更新其仪表板布局，而无需终止正在运行的会话。</p></li>
<li><p><strong>面板动画的缩放和矩阵变换。</strong> <em>SurfaceControl.Transaction</em> 已经在框架级别支持 <em>setScale()</em> 和 <em>setMatrix()</em>，但 Scalable UI 框架并未公开这些方法。如果在 Variant 模型和 Panel 接口中添加缩放和旋转属性，就可以在过渡期间实现缩放、缩小和倾斜效果，而无需自定义窗口管理技巧。</p></li>
<li><p><strong>每个属性的动画时序。</strong> 目前，所有动画属性共享同一个持续时间和插值器。框架的自定义动画路径虽然存在，但由于缺少属性设置器、缺少起始值/目标值注入以及任务预定位，因此对边界动画不起作用。修复这三个问题将允许每个属性的交错过渡，例如边界滑动时带有过冲，而透明度则线性淡入淡出。</p></li>
<li><p><strong>同步应用过渡。</strong> Scalable UI 在内部处理所有任务过渡，没有为外部动画参与者提供钩子。集成 <em>RemoteAnimationAdapter</em> 支持将允许 OEM 启动器在应用进入面板时为其表面添加动画，例如将应用从控件图标变形到目标面板，而不是简单地出现在目标边界处。</p></li>
<li><p><strong>重启后的状态持久性。</strong> Scalable UI 不会持久化上次活动的变体状态。每次重启后，每个面板都会恢复到其默认配置。为每个面板保留当前变体可以让信息娱乐系统从用户离开的地方继续运行。</p></li>
</ul>


<p><strong>隆重推出 Scalable UI 编辑器</strong></p>

<p>在过去几个月使用 Scalable UI 的过程中，我发现一个问题反复出现：手动编写 Scalable UI 意味着需要在多个 XML 文件中处理变体、Z 图层和过渡效果，而且只有在刷入 RRO 并重启 SystemUI 后才能看到效果。工作流程并没有跟上框架的强大功能。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*TBlXAvuYmRG4dFD2E-02iA.png" alt="captionless image" /></p>

<p>因此，我开始开发自己的工具——一个 Scalable UI 可视化编辑器，可以直接从布局设计导出 RRO 项目。你可以将 TaskPanel 放置在画布上，切换变体来定义状态，并调整过渡效果，所有操作均可实时预览。只需单击一下，即可将 RRO 直接写入正在运行的模拟器或设备。输出为标准的 RRO 文件，与手动工作流程完全兼容。</p>

<p>如果你的团队正在基于 Scalable UI 进行开发，欢迎与我们联系。</p>

<p><strong>结语</strong></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[面向Android开发者的Google I/O 2026]]></title>
    <link href="https://alexhilton.github.io/blog/2026/05/24/google-io-2026/"/>
    <updated>2026-05-24T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/05/24/google-io-2026</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Google I/O 2026 for Android Developers」，原文链接<a href="https://medium.com/proandroiddev/google-i-o-2026-for-android-developers-3866ae50ddb1">https://medium.com/proandroiddev/google-i-o-2026-for-android-developers-3866ae50ddb1</a>，由Anand Gaur发布于2026年5月20日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*T51U_MIjkcvJMpQVTbUlSg.png" title="auto auto" ></a></p>

<!-- more -->


<p>本次大会为真正开发 Android 应用的开发者带来了诸多变革——Android CLI、Compose 优先、Android 17、Google Book、XR DP4、AppFunctions 以及全新的 Play 管理中心。</p>

<p>如果你在 5 月 19 日观看了 Google I/O 2026 大会的主题演讲，并期待看到 Android 方面的新消息，那么你可能会感到失望。这场主题演讲更像是一场 Gemini 的盛会。真正会影响你日常工作的 Android 发布会内容被分散在 <strong>Android Show: I/O Edition</strong>（5 月 12 日）、一场单独的<strong>开发者主题演讲</strong>以及一篇题为“Google I/O 大会上 Android 开发者需要了解的 17 件事”的官方 Android 开发者博客文章中。</p>

<p>我已经仔细阅读了每一场会议、开发者博客和新文档，所以你不用再费心了。以下是为所有在 2026 年编写或发布 Android 应用的开发者准备的完整简报，绝无废话。</p>

<p>让我们开始吧。</p>

<p>第一部分：智能编码时代</p>

<p>谷歌今年正式宣布：<strong>Compose 优先</strong>。未来所有新库、新 API 和架构指南都将仅支持 Compose。</p>

<p>XML View 系统不会被删除。你现有的应用将继续运行。但它已进入<strong>维护模式</strong>——不再添加新功能，不再提供新指南，仅修复 bug 和安全补丁。如果你仍在使用 Views，那么迁移问题已从“我应该迁移吗？”转变为“我能多快迁移？”</p>

<h3>2026 年 4 月 Compose 版本更新内容</h3>

<p>当​​前的 Compose BOM（版本号 2026.04.01，核心模块版本为 v1.11）包含一些真正有用的新增功能：</p>

<pre><code class="kotlin">implementation(platform("androidx.compose:compose-bom:2026.04.01"))
</code></pre>

<ul>
<li><p><strong>Grid API</strong>——终于有了真正的 2D 布局原语。可以把它想象成 Android 版的 CSS Grid。你可以使用轨道、间隙和单元格来定义布局。尺寸调整支持 <code>Dp</code>、百分比、固有内容大小和灵活的 <code>Fr</code> 单位（与 CSS Grid 使用的分数单位相同）。你可以自动放置项目，也可以显式地将它们跨越多行和多列。最棒的是：它是自适应的——你可以根据设备状态重新配置轨道和跨度，因此同一个 Grid 布局可以自然地适应手机、平板电脑和桌面折叠模式。</p></li>
<li><p><strong>共享元素调试工具</strong> — 如果你曾经遇到过共享元素过渡效果不理想却又不知其原因的情况，那么这个工具正是你需要的。</p></li>
<li><p><strong>触控板事件支持</strong> — 这对于新的 Google Book 笔记本电脑至关重要（详见下文）。</p></li>
<li><p><strong>v2 测试 API 默认启用</strong> — 这可能会对现有的测试套件造成一些影响。默认的测试调度器已从 <code>UnconfinedTestDispatcher</code> 更改为 <code>StandardTestDispatcher</code>。简单来说：测试中启动的协程不再立即执行。它们会排队，只有在虚拟时钟推进时才会运行。你的测试会变得更加可预测，更接近生产环境的行为，但你可能需要在之前运行正常的地方添加 <code>advanceUntilIdle()</code> 调用。</p></li>
</ul>


<h3>展望未来</h3>

<p>除了 v1.11 版本之外，I/O 大会的公告重点介绍了<strong>更成熟的样式 API</strong>、改进的共享元素过渡效果以及对键盘、鼠标、触控板和手写笔等输入方式的增强支持——所有这些都为 Android 迈向多设备形态的世界奠定了基础。</p>

<p>第三部分：自适应 UI 已成为必备</p>

<p>Android Show 上最重磅的硬件发布是 <strong>Googlebook</strong>——谷歌全新的高端 AI 笔记本电脑产品线。你可以将其视为 Chromebook 的继任者，但它运行的并非 ChromeOS，而是 <strong>Aluminium OS</strong>（基于 Android 17 的桌面版）。</p>

<p>首发合作伙伴：宏碁、华硕、戴尔、惠普和联想。首批设备将于今年秋季上市。</p>

<p>对于 Android 开发者而言，有三件事至关重要：</p>

<ol>
<li><p><strong>你的 Android 应用将在 Googlebook 上运行。</strong> 如果你现在就构建自适应应用，那么你已经做好了准备。</p></li>
<li><p><strong>Android Studio Canary 中新增了一个桌面模拟器</strong>，专门用于测试大屏幕布局。你可以立即获取该模拟器，并测试你的应用在桌面窗口中的实际显示效果。</p></li>
<li><p><strong>桌面体验的设计和开发者指南</strong>现已发布，请访问 <code>developer.android.com/design/ui/desktop</code>。</p></li>
</ol>


<p>如果你的路线图上原本写着“桌面布局以后再说”，那么现在是时候加快进度了。Google Book 的发布意味着到今年年底，数百万台支持 Android 应用的笔记本电脑将送到用户手中。</p>

<p>第五部分：将生成式 AI 集成到你的应用中</p>

<h3>Jetpack Glance + RemoteCompose</h3>

<p>Android 17 通过 Jetpack Glance 将<strong>移动设备、Wear OS 和 Android Auto</strong> 上的组件开发统一到一个基于 Compose 的单一模型中。</p>

<p>这项重大的技术突破在于<strong>RemoteCompose 集成</strong>。在移动设备和车载系统上，它支持高保真动画。在 Wear OS 上，它允许组件在远程界面上原生渲染复杂的 UI 逻辑（无需加载整个 UI 堆栈），即使在低功耗手表硬件上也能保持流畅的性能。</p>

<p>实际好处：用户在车载仪表盘上查看航班状态组件，即可在手腕上收到登机口变更更新——相同的 Glance 组件代码，三个界面。</p>

<p><strong>注意：</strong> 作为此次统一的一部分，Wear OS 的“磁贴”已更名为<strong>Wear Widgets</strong>。</p>

<h3>车载安卓系统</h3>

<p>车载应用库迎来重大更新：</p>

<ul>
<li><p>一次构建，即可同时适配 Android Auto 和 Android Automotive 操作系统。</p></li>
<li><p>新增媒体应用组件和模板功能。</p></li>
<li><p>更灵活的布局，优化用户界面，减少驾驶时的注意力分散。</p></li>
<li><p><strong>沉浸式视频播放</strong>即将登陆运行 Android 17 的手机——仅限停车状态下使用。EAP 已开放给所有停车体验应用。</p></li>
<li><p>更新了 <strong>适用于 Android Auto 的 Google 地图</strong>，新增 3D 沉浸式导航功能。</p></li>
</ul>


<p>如果你之前因为模板功能有限而放弃开发车载应用，那么这些新组件值得你重新考虑。</p>

<p>第七部分：Android XR 开发者预览版 4</p>

<p>如果你的应用涉及相机、视频或音频，Android 17 将为你带来重大升级。谷歌的宣传语是：Android 现在是你打造专业级媒体体验的理想之选。</p>

<p>以下是新增功能：</p>

<ul>
<li><p><strong>CameraXViewfinder Composable</strong> — 一款即插即用的取景器，可自动处理折叠屏设备和平板电脑上的复杂缩放和响应。它修复了许多你以前需要手动处理的特殊情况。</p></li>
<li><p><strong>Media3 AI 特效库</strong> — 一个统一的界面，提供诸如 <strong>Magic Eraser</strong> 和 <strong>Studio Sound</strong> 等高级功能，并可自动针对设备硬件进行优化。</p></li>
<li><p><strong>CodecDB</strong> — 针对特定芯片组的编码建议。这解决了“视频在 Pixel 上完美导出，但在某些小米设备上却无法播放”的问题。</p></li>
<li><p><strong>ExoPlayer 中的进度条模式</strong> — 超流畅的进度条拖动。如果你开发任何视频播放器，这项功能都至关重要。</p></li>
<li><p><strong>Media3 Transformer</strong> — 多资源合成和编辑流程。</p></li>
<li><p><strong>CastPlayer API</strong> — 简化的投屏集成。</p></li>
</ul>


<p>总而言之，这些功能减少了你需要自行编写和维护的媒体管道工作量。</p>

<p>第九部分：性能工具的重要性日益凸显</p>

<p>Android 17（代号 <strong>Cinnamon Bun</strong>）稳定版将于 6 月发布。<strong>Android 17 QPR1 Beta 3</strong> 在主题演讲后立即发布，为更多 UI 界面引入了背景模糊效果。</p>

<p>当你的目标 API 版本为 <strong>API 37</strong> 时，以下是需要测试的内容。 ### 强制性行为</p>

<p>无论你是否愿意，一旦你的应用目标 API 版本达到 37，以下行为都将对其产生影响：</p>

<ul>
<li><p><strong>应用内存限制</strong> — 全平台强制执行。请在目标版本前审核你的内存使用情况。</p></li>
<li><p><strong>强制支持大屏幕可调整大小</strong> — 你的应用必须能够正确调整大小。不再允许使用 <code>resizeableActivity=false</code> 来规避此限制。</p></li>
<li><p><strong>默认证书透明性</strong> — 你的网络协议栈和任何自定义 TLS 代码都必须准备就绪。</p></li>
<li><p><strong>受限本地网络访问</strong> — 应用需要显式声明才能与局域网设备通信。这对物联网、投屏和 Chromecast 相关应用影响巨大。</p></li>
<li><p><strong>后台音频强化</strong> — 规则更加严格。请检查你的后台音频行为。</p></li>
<li><p><strong>短信 OTP 保护</strong> — 系统级保护机制改变了 OTP 向应用传输的方式。</p></li>
</ul>


<h3>系统内部机制</h3>

<p>这些改动通常不会造成任何问题，但如果你的代码对时间节点有假设，则可能会出现问题：</p>

<ul>
<li><p><strong>无锁消息队列</strong> — 更流畅的用户界面，更少的主线程争用。请重新测试任何对时间节点敏感的功能。</p></li>
<li><p><strong>垃圾回收器使用更频繁、消耗更少的新生代垃圾回收器</strong> — 整体帧速率更流畅。</p></li>
</ul>


<h3>新 API（使用这些 API 减少权限需求）</h3>

<p>Google 专门添加了两个新 API，以便你无需为一次性需求请求广泛的权限：</p>

<ul>
<li><p><strong>联系人选择器</strong> — 允许用户选择联系人，而无需你的应用请求完整的联系人权限。</p></li>
<li><p><strong>吸管 API</strong> (<code>ACTION_OPEN_EYE_DROPPER</code>) — 无需请求屏幕截图权限即可使用系统颜色选择器。</p></li>
</ul>


<p>如果你的应用目前需要请求完整的联系人权限或屏幕截图权限才能实现某个特定功能，请切换到这些 API。用户会注意到权限请求减少了，Play 商店也会给予相应的奖励。</p>

<p><strong>操作事项：</strong> 打开 <a href="https://developer.android.com/about/versions/17/behavior-changes-all">Android 17 行为变更页面</a> 并逐行阅读，然后再将 <code>targetSdk</code> 改为 37。</p>

<p>第 11 部分：Google TV — Engage SDK 和指针遥控器</p>

<p>Play 管理中心获得了本次 I/O 大会上最具体、最值得关注的开发者更新。</p>

<h3>发现：Play Shorts + Gemini Surface</h3>

<ul>
<li><p><strong>Play Shorts</strong> — Play 商店内新增的短视频发现格式。你以往的截图和功能图展示策略现在有了视频优先的版本。不妨考虑制作一个 15-30 秒的循环视频，突出你应用的核心亮点。</p></li>
<li><p><strong>Gemini 应用内的应用发现</strong> — Gemini 现在可以在用户通过 Android 和网页搜索应用时推荐你的应用。Play 商店应用商店优化 (ASO) 的范围现在扩展到“Gemini 是否了解我的应用并能正确展示它？” 值得思考的是，你的应用商店列表对应用生命周期管理 (LLM) 的呈现效果如何，而不仅仅是对用户。</p></li>
</ul>


<h3>订阅留存（API 重大变更）</h3>

<p>以下是影响最大的 API 变更：</p>

<ul>
<li><p><strong>默认帐户恢复期从 30 天延长至 60 天。</strong> Google 的数据显示，此举可为顶级开发者带来高达 <strong>18% 的非自愿流失率</strong> 和 <strong>9% 的总流失率</strong>。请确认你的计费逻辑在更长的窗口期内仍能正确处理 <code>SUBSCRIPTION_STATE_ON_HOLD</code> 状态。</p></li>
<li><p><strong>应用内订阅管理 API（即将推出）</strong> — 当订阅者在你的应用内点击“取消”按钮时，新的 API 允许他们<strong>立即更改套餐或接受降级优惠</strong>，而不是完全流失。结合自动按比例退款的替换模式，这是 Play 多年来推出的最强大的用户留存工具包。</p></li>
</ul>


<h3>商品目录和商店列表自动化</h3>

<p>本地化和商品目录管理迎来了一次彻底的 AI 升级：</p>

<ul>
<li><p><strong>基于关键词推荐的 Gemini 驱动的自定义商店列表</strong> — 在你的 Grow 控制面板中点击一个关键词，Gemini 就会根据该搜索趋势自动生成并预填充本地化的商店列表。一键部署。</p></li>
<li><p><strong>基于导入文档的 AI 预填充商店列表</strong> — 上传 CSV 或 Google 表格，Play 管理中心就会自动填充各种语言的列表以供审核。手动复制粘贴本地化已成为历史。</p></li>
<li><p><strong>AI 翻译的订阅权益</strong> — 无需单独的本地化工作流程，即可在全球范围内扩展订阅权益文案。</p></li>
<li><p><strong>代理目录管理</strong> — 通过代理批量更新价格和 SKU。如果你在各个市场拥有数百个应用内购买项目 (IAP)，这将是 I/O 大会带来的最大运营优势。</p></li>
</ul>


<h3>分析功能全面升级</h3>

<p>Play 管理中心的分析功能终于全面升级：</p>

<ul>
<li><p><strong>购物车转化率</strong> — 终于添加到核心绩效指标中。精准定位购买流程中结账失败的环节。</p></li>
<li><p><strong>覆盖率指标</strong> — 追踪 Play 商店的整体可见度，包括下游间接价值。</p></li>
<li><p><strong>订阅用户时长和流失原因数据</strong> — 提供关于哪些用户群体流失以及流失原因的详细数据。无需再猜测 D30、D90 和 D365 的留存率下降原因。</p></li>
<li><p><strong>Gemini 驱动的图表描述</strong> — 从“统计信息”页面扩展到“覆盖率和设备”以及“商店绩效”页面。AI 摘要可帮助你发现趋势，无需手动解读图表。</p></li>
<li><p><strong>互动问答 + 主动变现洞察</strong> — 询问“为什么上周 D7 在巴西的留存率下降了？”，即可获得量身定制的建议，而不仅仅是数字。</p></li>
</ul>


<p>结语</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Android上的ZeroMQ：用发布/订阅模式连接Linux服务]]></title>
    <link href="https://alexhilton.github.io/blog/2026/05/23/android-using-zeromq/"/>
    <updated>2026-05-23T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/05/23/android-using-zeromq</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「ZeroMQ on Android: Bridging a Linux Service with PUB SUB」，原文链接<a href="https://levelup.gitconnected.com/zeromq-on-android-bridging-a-linux-service-with-pub-sub-43ecf52209fa">https://levelup.gitconnected.com/zeromq-on-android-bridging-a-linux-service-with-pub-sub-43ecf52209fa</a>，由James Cullimore发布于2026年3月9日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/1*jKF0CYHnFn0_HY4YdXRiuQ.png" title="auto auto" ></a></p>

<!-- more -->


<p>在最近的一个项目中，我们遇到了一个看似简单却又充满挑战的需求。我们需要在一个基于 Linux 的小型服务和一个 Android 应用之间建立可靠、低延迟的通信。没有云组件，没有 HTTP API，也没有长期运行的后端基础设施。只有同一台机器上的两个进程需要高效且持续地进行通信。</p>

<p>传统的 REST 或 gRPC 等方法对于我们面临的问题来说显得过于繁琐。我们不需要请求/响应语义，也不想承担序列化格式、连接管理或重试逻辑带来的额外开销。我们真正需要的是一个轻量级的消息传递层，能够在数据可用时推送消息，并允许 Android 应用在需要时发布消息。</p>

<p>这时，ZeroMQ 就派上了用场。</p>

<p>ZeroMQ 不是消息代理，也不是框架。它是一个底层消息库，提供 PUB/SUB、PUSH/PULL 和 REQ/REP 等构建模块，并允许你根据系统需求将它们组合起来。它完全在进程内运行，使用纯 TCP 协议，并且可以在包括 Linux 和 Android 在内的不同平台上良好运行。</p>

<p>在本文中，我将详细介绍我们如何使用 ZeroMQ 通过简单的 PUB/SUB 设置将 Linux 服务连接到 Android 应用。我们将探讨我们实际交付的客户端实现、其结构设计的原因以及我们在此过程中遇到的权衡取舍。</p>

<h2>架构概述和消息模型</h2>

<p>在编写任何代码之前，我们花时间确定了 Linux 服务和 Android 应用应该如何通信。这两个进程将在同一设备上运行，并通过 localhost 进行通信。不需要加密、身份验证或消息持久化。最重要的是简单性、可预测性和低延迟。</p>

<p>ZeroMQ 的消息传递模式使这一决策变得非常简单。</p>

<p>我们为主要数据流选择了发布/订阅（PUB/SUB）模型。Linux 服务充当发布者，在新数据可用时广播消息。Android 应用充当订阅者，持续监听并响应更新。这种模式非常适合传输状态变化、传感器数据或状态更新，而无需轮询。</p>

<p>同时，Android 应用需要一种向 Linux 服务发送消息的方式。我们没有引入第二个协议或不同的传输方式，而是使用了反向的第二个 ZeroMQ 套接字。在 Android 端，该套接字发布消息，Linux 服务订阅这些消息。虽然这不是严格的请求/响应机制，但它保持了通信模型的对称性，并且易于理解。</p>

<p>从 Android 应用的角度来看，这会产生两个独立的连接：</p>

<ul>
<li><p>一个连接到 Linux 服务发布端口的订阅套接字，用于接收消息</p></li>
<li><p>一个连接到 Linux 服务订阅端口的发布套接字，用于发送消息</p></li>
</ul>


<p>两个套接字都运行在本地主机上的 TCP 协议下，每个套接字都绑定到一个专用端口。这种分离方式保持了消息流的清晰性，避免了将不相关的功能耦合到同一个通道中。</p>

<p>其余的实现工作都围绕着在 Android 上安全地管理这些套接字展开。这意味着要以一种不会泄漏资源或阻塞主线程的方式来处理线程、生命周期、连接状态和清理工作。然而，核心思想依然很简单：连接、订阅、接收消息，并在需要时发布响应。</p>

<h2>Android 设置和 ZeroMQ 注意事项</h2>

<p>在 Android 上使用 ZeroMQ 有一些值得提前注意的实际问题。虽然 ZeroMQ 本身是平台无关的，但 Android 的运行时、线程模型和生命周期限制会影响你将其集成到实际应用程序中的方式。</p>

<p>在我们的案例中，我们依赖于 Java ZeroMQ 绑定，它公开了熟悉的 ZContext 和 ZMQ.Socket API。这些绑定在 Android 上运行良好，但它们不具备生命周期感知能力。一旦创建了上下文或套接字，你就需要显式地管理其生命周期。否则，很容易导致线程泄漏或本地资源的生命周期超过创建它们的组件。</p>

<p>线程是第二个主要问题。ZeroMQ 套接字默认是阻塞的。在主线程上调用 recv 或 send 是不可行之选，即使是短暂的操作，如果处理不当，也可能导致卡顿或 ANR（应用程序无响应）。因此，所有套接字创建和消息处理都被推到后台线程进行。</p>

<p>Kotlin 协程非常适合这里。它们允许我们将工作卸载到 Dispatchers.IO，保持代码的可读性，并在需要时将状态更改协调回主线程。稍后你将看到的客户端实现使用协程来管理连接建立、消息接收和清理，而无需将线程问题暴露给应用程序的其他部分。</p>

<p>另一个重要的细节是，如果出现问题，ZeroMQ 不会自动重新连接套接字。如果连接断开或套接字发生故障，你需要决定如何应对。在我们的设置中，我们有意保持简单。客户端通过回调报告连接状态更改，并将重新连接策略留给调用者。这使得 ZeroMQ 客户端专注于通信，而不是策略。</p>

<p>考虑到这些限制，我们将所有与 ZeroMQ 相关的逻辑封装在一个类中。该类拥有上下文、套接字和协程，并向应用程序的其他部分公开了一个简洁明了的 API。</p>

<h2>将 ZeroMQ 添加到 Android 项目</h2>

<p>要在 Android 上使用 ZeroMQ，我们依赖于<strong>JeroMQ</strong>，它是 ZeroMQ 的纯 Java 实现。这是一个重要的区别。JeroMQ 不需要原生二进制文件或 NDK 集成，这使得设置过程非常简单，并避免了 ABI 相关问题。</p>

<p>在这个项目中，依赖项使用版本目录进行管理。ZeroMQ 依赖项只需定义一次，即可在所有模块中保持一致的引用。</p>

<p>首先，我们声明版本：</p>

<pre><code class="toml">jeromq = "0.6.0"
</code></pre>

<p>接下来，我们定义实际的库入口：</p>

<pre><code class="toml">jeromq = { group = "org.zeromq", name = "jeromq", version.ref = "jeromq" }
</code></pre>

<p>完成这些步骤后，就可以像添加其他库一样，将依赖项添加到 Android 模块中：</p>

<pre><code class="kotlin">implementation(libs.jeromq)
</code></pre>

<p>使用 JeroMQ 意味着 ZeroMQ 上下文和套接字完全运行在 JVM 中。这与 Android 的进程模型非常契合，并使资源管理更加可预测。这样做的缺点是无法使用 ZeroMQ 的原生实现，但对于本地低延迟通信而言，这种方法已经绰绰有余。</p>

<p>添加依赖项后，无需其他配置。你可以创建 <code>ZContext</code>，打开套接字，并立即开始发送或接收消息。</p>

<h2>ZeroMqClient 类及其职责</h2>

<p>Android 端实现的核心是一个名为 <code>ZeroMqClient</code> 的类。它的作用是将所有 ZeroMQ 特有的细节隐藏在一个简洁、可预测的 API 之后，以便应用程序的其他部分可以安全地与之交互。</p>

<p>该类负责：</p>

<ul>
<li><p>创建并拥有 ZeroMQ 上下文</p></li>
<li><p>管理一个用于接收消息的 SUB 套接字</p></li>
<li><p>管理一个用于发送消息的 PUB 套接字</p></li>
<li><p>在主线程之外运行阻塞式套接字操作</p></li>
<li><p>公开消息和连接状态的回调函数</p></li>
<li><p>确定性地清理所有资源</p></li>
</ul>


<p>该类没有直接公开套接字，而是采用了一种配置驱动的方法。订阅和发布端口是可选的，这使得客户端可以根据使用场景以仅接收、仅发送或双向模式运行。</p>

<p>以下是项目中完整的类定义：</p>

<pre><code class="kotlin">import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.Job
import kotlinx.coroutines.launch
import org.zeromq.SocketType
import org.zeromq.ZContext
import org.zeromq.ZMQ
import org.zeromq.ZMQException
import java.io.Closeable

class ZeroMqClient(
   private val subPort: Int? = null, // eg. 54054,
   private val pubPort: Int? = null, // eg. 54055,
   private val messageCallback: ((String) -&gt; Unit)? = null,
   private val onConnectionChanged: ((Boolean) -&gt; Unit)? = null
) : Closeable {
   private var context: ZContext? = null
   private var subSocket: ZMQ.Socket? = null
   private var pubSocket: ZMQ.Socket? = null
   private var isRunning = false
   private var receiveJob: Job? = null
</code></pre>

<p>该类实现了 <code>Closeable</code> 接口，使其生命周期清晰可见。调用 <code>close</code> 函数时，预期所有后台工作都会停止，所有原生资源都会被释放。这一点在之后将客户端与 Android 组件生命周期绑定时尤为重要。</p>

<p><code>subPort</code> 和 <code>pubPort</code> 参数可以为空。这使得客户端可以根据调用者的需求有条件地创建套接字，而无需引入多个实现或标志。</p>

<p>回调函数也以传入的方式传递，而不是作为流或可观察对象公开。这是为了保持客户端轻量级且不依赖框架而特意做出的选择。应用程序中的更高层可以根据需要将这些回调函数适配到 StateFlow、LiveData 或任何其他抽象层。</p>

<p>最后，ZeroMQ 上下文、套接字和协程作业等内部状态都是私有的。应用程序的其他部分无需了解消息是如何接收或发送的，只需知道消息正在接收或发送即可。</p>

<h2>连接和初始化套接字</h2>

<p><code>connect()</code> 函数用于将客户端从一个普通对象转换为一个活跃的 ZeroMQ 参与者。目标很简单：创建一个上下文，创建所需的套接字，将它们连接到本地主机，并在配置了 SUB 端口后开始接收消息。</p>

<p>实现过程中受到以下几个限制：</p>

<ul>
<li><p>我们绝不阻塞主线程。</p></li>
<li><p>我们希望 SUB 和 PUB 套接字是可选的。</p></li>
<li><p>我们希望应用程序的其他部分能够知道连接是否成功。</p></li>
</ul>


<p>以下是 <code>connect()</code> 函数的实现：</p>

<pre><code class="kotlin">fun connect() {
  if (isRunning) return
  CoroutineScope(Dispatchers.IO).launch {
    try {
      context = ZContext()
      subPort?.let {
        subSocket = context?.createSocket(SocketType.SUB)
        subSocket?.subscribe("".toByteArray())
        subSocket?.connect("tcp://127.0.0.1:$subPort")
        Log.d(TAG, "Subbing to $subPort")
        startReceiving()
      }
      pubPort?.let {
        pubSocket = context?.createSocket(SocketType.PUB)
        pubSocket?.connect("tcp://127.0.0.1:$pubPort")
        Log.d(TAG, "Pubbing to $pubPort")
      }
      isRunning = subSocket != null || pubSocket != null
      onConnectionChanged?.let {
        CoroutineScope(Dispatchers.Main).launch { it.invoke(true) }
      }
    } catch (e: Exception) {
      Log.e(TAG, "Error connecting to server: ${e.message}", e)
      onConnectionChanged?.let {
        CoroutineScope(Dispatchers.Main).launch { it.invoke(false) }
      }
      close()
    } 
  }  
}
</code></pre>

<p>提前返回的 <code>if (isRunning) return</code> 可以防止重复连接。如果没有这个保护机制，很容易创建多个上下文和套接字，这很快就会导致混乱的行为和资源泄漏。</p>

<p>所有设置都在 <code>Dispatchers.IO</code> 中启动。套接字创建和 <code>connect()</code> 调用都不是应该在主线程上运行的操作。这也让我们可以在以后添加重试或超时机制，而无需更改调用代码。</p>

<p>当客户端配置了 <code>subPort</code> 时，它会创建一个 <code>SocketType.SUB</code> 套接字并立即进行订阅：</p>

<pre><code class="kotlin">subSocket?.subscribe("".toByteArray())
</code></pre>

<p>使用空前缀订阅意味着：订阅所有内容。这保持了客户端的灵活性。如果你之后引入了基于主题的过滤，你可以将其替换为特定的前缀，并保持其余代码不变。</p>

<p>两个套接字都连接到 <code>tcp://127.0.0.1:&lt;port&gt;</code>。这是最初需求的关键部分：Linux 服务和 Android 应用之间的本地通信。</p>

<p>最后，连接回调函数在主调度器上被调用。即使回调函数只是切换状态，从后台线程更新 UI 状态也常常会导致一些难以察觉的问题，因此需要显式地处理。</p>

<p>如果在设置过程中出现任何错误，代码会记录错误，报告连接失败，并调用 <code>close()</code> 以确保部分创建的资源不会残留。</p>

<h2>使用协程接收消息</h2>

<p>SUB 套接字连接成功后，下一个问题虽然简单却至关重要：如何在不阻塞应用程序的情况下持续监听，并将接收到的消息传递给代码的其他部分。</p>

<p>这就是 <code>startReceiving()</code> 的作用。它会在后台调度器上运行一个循环，阻塞在 <code>recvStr()</code> 上，并将接收到的每条消息通过回调函数转发出去。</p>

<p>以下是具体的实现：</p>

<pre><code class="kotlin">private fun startReceiving() {
  receiveJob = CoroutineScope(Dispatchers.IO).launch {
    try {
      while (isRunning &amp;&amp; subSocket != null) {
        val message = subSocket?.recvStr()
        if (message != null) {
          Log.d(TAG, "Server received message: $message")
          messageCallback?.invoke(message)
        }
      }
    } catch (e: Exception) {
      Log.e(TAG, "Error receiving message: ${e.message}", e)
    }
  }
}
</code></pre>

<p><code>recvStr()</code> 是一个阻塞调用。在这里没问题，因为这个协程运行在 <code>Dispatchers.IO</code> 上。线程可以等待消息而不会影响 UI 的响应速度。</p>

<p>循环条件会检查 <code>isRunning</code> 和 <code>subSocket != null</code>。这样，当客户端关闭或套接字意外被清理时，就可以快速、清晰地停止接收消息。结合 <code>close()</code> 中的 <code>receiveJob?.cancel()</code>，它提供了两层停止行为：基于状态的退出和协程取消。</p>

<p>消息通过 <code>messageCallback</code> 向上传递。这使得该类专注于传输问题，而不是消息的解析方式。实际上，应用程序可以在回调中解析 JSON、更新 <code>StateFlow</code> 或将事件路由到应用程序的更深层。</p>

<p>一个关于日志的小提示：日志标签显示“服务器已收到消息”，但这段代码位于客户端，接收来自服务器的消息。如果保持现状，它仍然可以工作，但在调试时可能会产生误导。是否要调整这一点取决于代码风格，但值得注意。</p>

<p>错误会被捕获并记录，但协程不会自动重启。这符合客户端的轻量级设计。如有需要，可以在上一层实现连接策略（重试、重新连接、退避）。</p>

<h2>使用 <code>sendMessage()</code> 发送消息</h2>

<p>接收消息只是成功的一半。Android 应用还需要一种简单的方法将消息发布回 Linux 服务。</p>

<p><code>sendMessage()</code> 函数特意设计得非常简洁。它会验证客户端是否正在运行以及 PUB 套接字是否存在，然后发送消息并记录结果。</p>

<p>以下是实际使用的实现：</p>

<pre><code class="kotlin">fun sendMessage(message: String) {
  if (!isRunning || pubSocket == null) {
    Log.e(TAG, "Cannot send message, client not connected")
    return
   }
  try {
    pubSocket?.send(message)
    Log.d(TAG, "Sent message to server: $message")
  } catch (e: ZMQException) {
    Log.e(TAG, "Error sending message or receiving response: ${e.message}", e)
  }
}
</code></pre>

<p>首先，该方法预先处理了常见的误用情况。如果客户端创建时没有指定 <code>pubPort</code>，或者 <code>connect()</code> 尚未建立 PUB 套接字，我们会快速失败并记录清晰的日志消息。这避免了应用“以为”已发送消息但实际上并未发送任何消息的静默丢失。</p>

<p>其次，该方法直接使用 <code>pubSocket?.send(message)</code>。在 PUB/SUB 架构中，不会立即收到确认或响应。send 调用要么在本地成功，要么抛出异常。实际的数据传输语义取决于对方是否已订阅并已连接。</p>

<p>第三，错误被限定为 <code>ZMQException</code>。这样可以确保捕获的异常只针对套接字发送操作的实际预期结果，而不是吞掉所有异常。</p>

<p>如果之后将其扩展为请求响应模式，API 的结构就会发生改变。在 Android 端，你可能需要将 PUB 替换为 REQ，并引入阻塞式接收或返回值的挂起函数。对于当前的设计而言，即发即弃的发送方式最为合适。</p>

<h2>清理、生命周期以及安全地关闭客户端</h2>

<p>在 Android 上，资源管理至关重要。ZeroMQ 上下文及其套接字可能在底层持有原生资源，除非你主动关闭，否则接收循环可能会在创建它的 UI 组件消失后继续运行很长时间。</p>

<p>因此，此客户端实现了 <code>Closeable</code> 接口，并将所有清理逻辑放在 <code>close()</code> 函数中。</p>

<p>以下是实际使用的实现：</p>

<pre><code class="kotlin">override fun close() {
       try {
           isRunning = false
           receiveJob?.cancel()
           subSocket?.close()
           pubSocket?.close()
           context?.close()
           Log.d(TAG, "Disconnected from server")
           CoroutineScope(Dispatchers.Main).launch { onConnectionChanged?.invoke(false) }
       } catch (e: Exception) {
           Log.e(TAG, "Error disconnecting from server: ${e.message}", e)
       } finally {
           receiveJob = null
           subSocket = null
           pubSocket = null
           context = null
       }
   }
</code></pre>

<h3>停止接收循环</h3>

<p>第一行代码设置了 <code>isRunning = false</code>。这足以使 <code>startReceiving()</code> 中的 <code>while (isRunning &amp;&amp; subSocket != null)</code> 循环自然退出。</p>

<p>然后，<code>receiveJob?.cancel()</code> 添加了第二个关闭机制。取消操作至关重要，因为 <code>recvStr()</code> 是阻塞的。实际上，取消操作和关闭套接字同时进行，有助于协程展开，而不是无限期地等待。</p>

<h3>释放套接字和上下文</h3>

<p>套接字会先关闭，然后是上下文：</p>

<ul>
<li><p><code>subSocket?.close()</code></p></li>
<li><p><code>pubSocket?.close()</code></p></li>
<li><p><code>context?.close()</code></p></li>
</ul>


<p>这样的顺序保证了清理过程的可预测性。在这样的系统中，需要避免在上下文已经消失后，套接字仍然处于活动状态。</p>

<h3>在主线程上报告状态变更</h3>

<p>断开连接后，客户端通过以下方式报告连接状态：</p>

<pre><code class="kotlin">CoroutineScope(Dispatchers.Main).launch { onConnectionChanged?.invoke(false) }
</code></pre>

<p>这与 <code>connect()</code> 中使用的方法一致：状态更新会路由回主调度器，以便调用者可以安全地更新 UI 状态。</p>

<p>最后，<code>finally</code> 代码块会将引用设置为 null。这与其说是为了确保正确性，不如说是为了明确地“关闭”对象，使其可以被垃圾回收，而不会留下残留引用。</p>

<h2>结论</h2>

<p>这种设置之所以有效，是因为它缩小了问题的范围，并将消息传递视为基础设施，而不是架构声明。我们有一个需要广播更新的 Linux 服务，以及一个需要快速响应并偶尔发布消息的 Android 应用。ZeroMQ 以极低的开销满足了我们的需求，无需引入代理、HTTP 层或更复杂的 RPC 栈。</p>

<p><code>ZeroMqClient</code> 的设计非常精简。它拥有 ZeroMQ 上下文和套接字，所有阻塞操作都在 <code>Dispatchers.IO</code> 上完成，并且只暴露应用其他部分所需的功能：<code>connect()</code>、<code>sendMessage()</code>、消息回调和连接状态回调。生命周期通过 <code>Closeable</code> 显式控制，清理工作也集中进行，因此套接字不会在创建它们的组件之后仍然存在。</p>

<p>如果你正在考虑类似的方案，需要记住的主要权衡是，发布/订阅模式并非请求/响应。你优化的是流式更新和松耦合，而不是确认和保证交付。这非常适合本地低延迟通信，但这仍然是一个需要你有意做出的设计选择。</p>

<p>在我们的案例中，这个选择是值得的。我们在 Linux 服务和 Android 应用之间建立了一个简洁、轻量级的通道，并且我们保持了实现的简洁性，使其易于调试和维护。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[一个实用的Android Perfetto分析器]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/09/pefetto-analyzer/"/>
    <updated>2026-03-09T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/09/pefetto-analyzer</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Building a Deterministic Perfetto Analyzer for Android」，原文链接<a href="https://proandroiddev.com/building-a-deterministic-perfetto-analyzer-for-android-95877b3e083e">https://proandroiddev.com/building-a-deterministic-perfetto-analyzer-for-android-95877b3e083e</a>，由Sumeet Singh Arora发布于2026年1月6日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/1*qr6R9rois09aHO5R-yZW6A.png" title="auto auto" ></a></p>

<!-- more -->


<h2>每个 Android 性能工程师都面临的问题</h2>

<p>当你打开一个 <a href="https://perfetto.dev/">Perfetto</a> trace后。</p>

<p>会有成千上万个切片（slice）。数十个线程。数百万个数据点。一条看起来像抽象艺术的时间线。</p>

<p>而在这其中的某个地方——被层层系统噪声、框架回调和线程交错所掩盖——隐藏着一个简单问题的答案：<em>为什么我的应用运行缓慢？</em></p>

<p>我花了数年时间大规模调试 Android 性能。模式总是千篇一律：</p>

<ol>
<li><p>用户抱怨启动卡顿/缓慢</p></li>
<li><p>工程师捕获跟踪信息</p></li>
<li><p>工程师打开 Perfetto 用户界面</p></li>
<li><p>工程师花费 2 小时滚动、缩放、眯眼查看</p></li>
<li><p>工程师<em>可能</em>找到了根本原因</p></li>
</ol>


<p>跟踪信息无法讲述故事。它们只是罗列事实——带有时间戳、层层嵌套、令人眼花缭乱。“拥有跟踪信息”和“理解性能”之间存在着巨大的鸿沟。</p>

<p><strong>本文旨在通过编程方式弥合这一鸿沟。</strong></p>

<p>在过去的几周里，我一直在构建一个<strong>Perfetto 跟踪分析器</strong>，它可以将原始跟踪信息转换为结构化的、易于理解的洞察。没有仪表盘。只有确定性分析，它反映了经验丰富的工程师实际思考性能问题的方式。</p>

<p>读完本文，你将了解如何：</p>

<ol>
<li><p>使跟踪信息<strong>可读</strong>——启动时间、长时间任务、帧健康状况</p></li>
<li><p>使跟踪信息<strong>可归属</strong>——进程、线程、组件所有权</p></li>
<li><p>分离<strong>职责</strong>——应用程序、框架和系统</p></li>
<li><p>使用<strong>时间窗口</strong>查找关键事件发生的时间点</p></li>
<li><p>生成<strong>确定性嫌疑对象</strong>——用于调查的优先级信号</p></li>
</ol>


<h2>为什么 Perfetto 跟踪难以阅读</h2>

<p>Perfetto 功能极其强大。它可以捕获：</p>

<ul>
<li><p>CPU 调度</p></li>
<li><p>帧渲染</p></li>
<li><p>Binder 事务</p></li>
<li><p>应用自定义跟踪部分</p></li>
<li><p>系统服务活动</p></li>
</ul>


<p>但强大的功能也带​​来了复杂性。一个典型的跟踪可能包含：</p>

<ul>
<li><p>超过 50,000 个切片</p></li>
<li><p>超过 100 个不同的线程名称</p></li>
<li><p>超过 10 层的嵌套层级结构</p></li>
</ul>


<p>当你调试卡顿的滚动或缓慢的启动问题时，你并不需要所有这些信息。你需要的是：</p>

<ol>
<li><p><strong>发生了什么</strong> — 启动时间、耗时较长的任务、帧健康状况</p></li>
<li><p><strong>谁做的</strong> — 哪个进程、哪个线程</p></li>
<li><p><strong>什么类型的工作</strong> — 应用代码、框架还是系统代码</p></li>
</ol>


<p>让我们逐一解决这些问题。</p>

<h2>第一步：使跟踪更易读</h2>

<p>首要目标是提取人们真正关心的高级指标。</p>

<h3>启动时间</h3>

<p><a href="https://github.com/singhsume123/perfetto-agent">分析器</a> 使用尽力而为的启发式方法来估算启动时间：<strong>从最早的切片到第一个 <code>Choreographer#doFrame</code></strong> 的时间。</p>

<p>这种方法并不完美——它无法涵盖所有​​启动场景——但它提供了一个有用的基准进行比较。</p>

<p>以下是 <a href="http://medium.com/r?url=https%3A%2F%2Fgithub.com%2Fsinghsume123%2FTraceToy">TraceToy</a> 测试跟踪的示例（TraceToy 是我专门构建的一个示例应用程序，用于生成有趣的测试跟踪——稍后会详细介绍）：</p>

<pre><code class="json">{
  "startup_ms": 2910.15
}
</code></pre>

<p>在生产环境中，对于优化良好的冷启动，你通常会看到 500–1500 毫秒的启动时间；而对于初始化繁重的应用程序，启动时间则可能达到 2000–5000 毫秒甚至更长。具体的数值并不重要，重要的是跟踪其随时间的变化——无论基线是 800 毫秒还是 3000 毫秒，20% 的性能下降就是 20% 的性能下降。</p>

<p>生产环境的应用通常会更精确地检测启动过程——使用 <code>reportFullyDrawn()</code>、自定义跟踪标记（例如 <code>Startup#complete</code>）或 Jetpack App Startup 等库。当这些方法不可用时，分析器的启发式方法是一个合理的备选方案，但你可以（也应该）对其进行调整，使其与应用对“启动完成”的实际定义相匹配。</p>

<p>一个数值。可立即用于性能下降检测。</p>

<h3>UI 线程上的长任务</h3>

<p>长任务是指超过阈值并阻塞主线程的任务片段。这些是导致卡顿和无响应的主要原因。</p>

<p>分析器默认使用 <strong>50 毫秒的阈值</strong>（可通过 <code>--long-task-ms</code> 配置）：</p>

<pre><code class="json">{
  "ui_thread_long_tasks": {
    "threshold_ms": 50,
    "count": 12,
    "top": [
      {"name": "&lt;internal slice&gt;", "dur_ms": 16572.1},
      {"name": "216", "dur_ms": 4103.2},
      {"name": "Handler: android.view.View", "dur_ms": 312.5}
    ]
  }
}
</code></pre>

<p>看出问题了吗？是数字 ID。内部切片。无上下文。无归属信息。</p>

<p>技术上正确。实际应用价值有限。</p>

<p>我们知道<em>某些东西</em>阻塞了主线程 16 秒。但我们不知道<em>是什么</em>，也不知道<em>是谁</em>造成的。这就是原始分析失效的地方——也是为什么步骤 2（归属信息）至关重要的原因。</p>

<h3>帧健康状况</h3>

<p>丢帧会导致明显的卡顿。分析器从 <code>Choreographer#doFrame</code> 切片中提取帧时间：</p>

<pre><code class="json">{
  "frame_features": {
    "total_frames": 888,
    "janky_frames": 37,
    "p95_frame_ms": 14.35
  }
}
</code></pre>

<p>在这里，百分位数比平均值更重要——即使大多数帧都很快，少数几个坏帧也会破坏流畅度。</p>

<h3>局限性</h3>

<p>此时，我们可以回答：<em>发生了什么？</em></p>

<p>但我们无法回答：<em>谁该负责？</em></p>

<h2>步骤 2：使跟踪数据可归因</h2>

<p>归因是指将性能数据与所有权关联起来。在调试实际问题时，工程师会问：</p>

<ul>
<li><p><em>哪个</em> <strong><em>进程</em></strong> <em>执行了此操作？</em></p></li>
<li><p><em>哪个</em> <strong><em>线程</em></strong><em>？</em></p></li>
<li><p><em>是我的</em> <strong><em>应用</em></strong> <em>还是其他程序</em>？_</p></li>
</ul>


<h3>聚焦进程过滤</h3>

<p>首先，我们需要过滤掉噪声。跟踪数据会捕获设备上的<em>所有</em>活动——系统服务、其他应用、内核活动。其中大部分都无关紧要。</p>

<p>分析器支持聚焦模式：</p>

<pre><code class="bash">python3 -m perfetto_agent.cli analyze \
  --trace tracetoy_trace.perfetto-trace \
  --focus-process com.example.tracetoy \
  --out analysis.json
</code></pre>

<p>这会将输出从“设备上发生的一切”转移到“与<em>此</em>应用相关的内容”。这是一个小小的标志，却能极大地提高信噪比。</p>

<h3>进程和线程识别</h3>

<p>设置聚焦进程后，每个切片都会包含所有权数据。分析器会将 <code>pid</code> 和 <code>tid</code> 连接到进程名和线程名：</p>

<pre><code class="json">{
  "name": "BG#churn",
  "dur_ms": 267.29,
  "pid": 5664,
  "tid": 5664,
  "thread_name": "xample.tracetoy",
  "process_name": "com.example.tracetoy"
}
</code></pre>

<p>将其与步骤 1 中无用的 <code>"name": "216"</code> 进行比较。现在我们知道：</p>

<ul>
<li><p>✅ 它来自我的应用程序 (<code>com.example.tracetoy</code>)</p></li>
<li><p>✅ 它运行在主线程上 (tid == pid)</p></li>
<li><p>✅ 这是一个应用程序定义的段（<code>BG#</code> 前缀）</p></li>
</ul>


<h3>主线程检测</h3>

<p>这里有一个微妙但重要的细节：<strong>主线程在跟踪信息中并不总是标记为“main”</strong>。</p>

<p>分析器采用尽力而为的启发式方法：</p>

<ol>
<li><p>如果应用进程存在名为 <code>"main"</code> 的线程 → 使用该线程</p></li>
<li><p>否则，回退到 <code>tid == pid</code>（在 Android 中通常适用于主线程）</p></li>
</ol>


<p>这模拟了经验丰富的工程师在元数据不完整时的推理方式——为了透明起见，分析器会记录所使用的启发式方法。</p>

<h3>应用自定义节成为一等公民</h3>

<p>Android 的 <code>Trace.beginSection()</code> API 允许开发者标记自定义 span：</p>

<pre><code class="kotlin">Trace.beginSection("UI#loadUserProfile")
// ... work ...
Trace.endSection()
</code></pre>

<p>分析器会显式地提取并聚合这些信息：</p>

<pre><code class="json">{
  "app_sections": {
    "top_by_total_ms": [
      {"name": "BG#churn", "total_ms": 11811.05, "count": 194},
      {"name": "UI#stall_button_click", "total_ms": 818.05, "count": 4}
    ]
  }
}
</code></pre>

<p>这回答了以下问题：<em>我的代码的哪些部分</em>主导了执行？_</p>

<p>至此，我们已将匿名切片转换为具有属性的、可操作的数据。但我们仍然没有回答最重要的问题：<strong>究竟是谁的错——我的应用、框架还是系统？</strong></p>

<h2>第三步：职责分离</h2>

<p>关于移动性能，有一个令人不安的真相：</p>

<p><strong>当你的应用运行缓慢时，并非所有问题都出在你身上。</strong></p>

<p>各种数据混杂在一起：</p>

<ul>
<li><p>你的应用代码</p></li>
<li><p>Android 框架内部代码</p></li>
<li><p>系统和内核活动</p></li>
</ul>


<p>如果不将这些数据分离，很容易得出错误的结论，并优化错误的地方。</p>

<h3>确定性分类</h3>

<p>每个切片现在都被标记为以下类别之一：</p>

<ul>
<li><p>🟢 <strong>app</strong> — 焦点进程上的应用程序标记（例如，<code>UI#stall_button_click</code>）</p></li>
<li><p>🔵 <strong>framework</strong> — 焦点进程上的框架标记（例如，<code>Choreographer#doFrame</code>）</p></li>
<li><p>🔴 <strong>system</strong> — 非焦点进程 ID 或系统标记（例如，<code>binder transaction</code>）</p></li>
<li><p>⚪ <strong>unknown</strong> — 无法确定分类（例如，数字 ID）</p></li>
</ul>


<p>分类器有意采用<strong>保守</strong>的分类方式：</p>

<ul>
<li><p>基于进程 ID 的所有权</p></li>
<li><p>基于线程的所有权</p></li>
<li><p>基于明确的名称标记规则</p></li>
</ul>


<p>如果一个切片无法明确归类，则会被标记为“unknown”。这并非错误，而是诚实。</p>

<h3>带有责任标签的长任务</h3>

<p>以下是一个已分类的长任务切片示例：</p>

<pre><code class="json">{
  "name": "BG#churn",
  "dur_ms": 267.29,
  "pid": 5664,
  "tid": 5664,
  "thread_name": "xample.tracetoy",
  "process_name": "com.example.tracetoy",
  "category": "app"
}
</code></pre>

<p>以及一个框架密集型任务切片示例：</p>

<pre><code class="json">{
  "name": "dequeueBuffer - VRI[MainActivity]#0(BLAST Consumer)0",
  "dur_ms": 180.4,
  "pid": 5664,
  "tid": 5688,
  "thread_name": "RenderThread",
  "category": "framework"
}
</code></pre>

<p>相同的跟踪记录，但对优化方向的影响却截然不同。</p>

<h3>聚合分解</h3>

<p>分析器无需手动查看数百个切片，而是计算以下内容：</p>

<pre><code class="json">{
  "work_breakdown": {
    "by_category_ms": {
      "app": 12603.72,
      "framework": 305.77,
      "system": 0.0,
      "unknown": 5167.50
    }
  }
}
</code></pre>

<p>这一个代码块回答了一个重要的问题：</p>

<blockquote><p>此性能问题主要是应用程序本身的问题吗？</p></blockquote>

<p>有时答案令人不安，有时却令人欣慰。无论如何，它都基于数据。</p>

<h3>主线程阻塞：究竟是谁的责任？</h3>

<p>分析器按类别分解主线程阻塞时间：</p>

<pre><code class="json">{
  "main_thread_blocking": {
    "app_ms": 12603.72,
    "framework_ms": 305.77,
    "system_ms": 0.0,
    "unknown_ms": 2840.88
  }
}
</code></pre>

<p>责任归属在此变得不可回避。</p>

<p>如果主线程主要被应用程序工作阻塞（就像这个跟踪记录中显示的那样），<strong>你就能准确地知道应该把优化工作集中在哪些方面</strong>。</p>

<h3>摘要块</h3>

<p>所有内容都汇总成一个最简洁的摘要：</p>

<pre><code class="json">{
  "summary": {
    "main_thread_found": true,
    "top_app_sections": ["BG#churn", "dequeueBuffer - VRI[MainActivity]#0...", "UI#stall_button_click"],
    "top_long_slice_name": "BG#churn",
    "dominant_work_category": "app",
    "main_thread_blocked_by": "app"
  }
}
</code></pre>

<p>这不是解释，而是一个<strong>信号</strong>——告诉你应该关注哪里，以及哪些地方不应该关注。</p>

<h2>第四步：时间窗口和嫌疑对象</h2>

<p>知道是谁执行了操作固然重要，但性能下降很少是全局性的——它们往往是<strong>暂时性的</strong>。</p>

<p>典型的问题如下：</p>

<ul>
<li><p><em>为什么启动这么慢？</em></p></li>
<li><p><em>为什么卡顿只在第一个屏幕之后才出现？</em></p></li>
<li><p><em>为什么 UI 线程在交互过程中被阻塞，但在启动时却没有？</em></p></li>
</ul>


<p>缺乏时间上下文的归因分析仍然需要手动梳理时间线。第四步可以减轻这种认知负担。</p>

<h3>时间窗口：明确“何时”⏱️</h3>

<p>分析器不再将跟踪信息视为一条长长的时间线，而是将其划分为粗略的、确定性的窗口：</p>

<ul>
<li><p><strong>启动窗口</strong> — 从进程启动到启动结束</p></li>
<li><p><strong>稳定状态窗口</strong> — 启动后的时间段（默认值：5 秒）</p></li>
</ul>


<pre><code class="json">{
  "time_windows": {
    "startup": {
      "start_ms": 0.0,
      "end_ms": 2910.15,
      "method": "startup_ms"
    },
    "steady_state": {
      "start_ms": 2910.15,
      "end_ms": 7910.15,
      "method": "startup_end + 5000ms"
    }
  }
}
</code></pre>

<p>仅此一项就改变了你对跟踪信息的推理方式。</p>

<h3>窗口分解：上下文中的责任归属 📊</h3>

<p>对于每个窗口，分析器计算：</p>

<ul>
<li><p>按类别划分的总工作量（<code>app</code>、<code>framework</code>、<code>system</code>、<code>unknown</code>）</p></li>
<li><p>按类别划分的主线程阻塞</p></li>
</ul>


<p>来自 TraceToy 跟踪：</p>

<pre><code class="json">{
  "window_breakdown": {
    "startup": {
      "by_category_ms": {"app": 0.0, "framework": 0.0, "system": 378.43, "unknown": 101.34},
      "main_thread_blocking_ms": {"app_ms": 0.0, "framework_ms": 0.0, "system_ms": 0.0, "unknown_ms": 101.34}
    },
    "steady_state": { /* ... */ }
  }
}
</code></pre>

<p>这可以确定性地回答以下问题：</p>

<blockquote><p>启动阶段主要由系统工作主导。稳定阶段有大量未知工作阻塞了主线程。</p></blockquote>

<p>无需猜测。</p>

<h3>从数据到嫌疑人 🕵️</h3>

<p>一旦掌握了归因、责任和时间窗口，就可以找出<strong>嫌疑人</strong>。</p>

<p>嫌疑人<strong>并非解释</strong>，而是一个<em>优先级信号</em>。</p>

<p>分析器生成一个确定性列表：</p>

<pre><code class="json">{
  "suspects": [
    {"label": "Startup main thread dominated by unknown work", "window": "startup", "evidence": {"unknown_ms": 101.34}},
    {"label": "Startup dominated by system work", "window": "startup", "evidence": {"system_ms": 378.43}}
    // ... more suspects for steady_state
  ]
}
</code></pre>

<p>这些标签是模板化的、可复现的，并且有证据支持。</p>

<p>它们不解释问题，而是指明方向。</p>

<h3>摘要现在更有意义了🧭</h3>

<p>摘要块变得可操作：</p>

<pre><code class="json">{
  "summary": {
    "startup_dominant_category": "system",
    "steady_state_dominant_category": "unknown",
    "top_suspect": "Startup main thread dominated by unknown work"
  }
}
</code></pre>

<p>如果你只阅读此摘要，你就已经知道：</p>

<ul>
<li><p>从哪里开始调查</p></li>
<li><p>应该查看哪种类型的工作</p></li>
<li><p>哪个阶段最重要</p></li>
</ul>


<p>这与原始跟踪信息相比是一个巨大的转变。</p>

<h2>整合所有步骤</h2>

<p>以下是完整的工作流程：</p>

<h3>1. 捕获跟踪</h3>

<p>使用 Android Studio 的性能分析器或命令行：</p>

<pre><code class="bash">adb shell perfetto -o /data/misc/perfetto-traces/trace.perfetto-trace -t 10s \
  sched freq idle am wm gfx view binder_driver hal dalvik camera input res memory
</code></pre>

<h3>2. 运行分析器</h3>

<pre><code class="bash">python3 -m perfetto_agent.cli analyze \
  --trace trace.perfetto-trace \
  --focus-process com.example.myapp \
  --out analysis.json \
  --long-task-ms 50 \
  --top-n 10
</code></pre>

<h3>3.阅读输出</h3>

<p>首先查看 <code>summary</code> 块，了解主要类别和主要嫌疑人，然后深入查看具体信息：</p>

<ul>
<li><p><code>work_breakdown</code> 用于查看类别分布</p></li>
<li><p><code>features.long_slices_attributed</code> 用于查看单个嫌疑人</p></li>
<li><p><code>features.app_sections</code> 用于查看你的插桩代码</p></li>
</ul>


<h2>此功能无法实现的功能</h2>

<p>让我们明确一下它的局限性：</p>

<ul>
<li><p>❌ <strong>不追究责任</strong> — 仅显示证据</p></li>
<li><p>❌ <strong>不生成建议</strong> — 这需要理解意图</p></li>
<li><p>❌ <strong>不声明因果关系</strong> — 相关性不等于因果关系</p></li>
</ul>


<p>该系统使责任<strong>可见</strong>。这就足够了。</p>

<h2>使用 TraceToy 进行测试</h2>

<p>我构建了一个名为 <strong>TraceToy</strong> 的示例应用程序，用于生成有趣的跟踪信息，以便测试分析器。</p>

<p>它包含：</p>

<ul>
<li><p><strong>启动初始化</strong>（使用 <code>StartupInit</code> 标记）</p></li>
<li><p><strong>UI 阻塞按钮</strong> — 使用 <code>UI#stall_button_click</code> 故意阻塞 UI 线程 200 毫秒</p></li>
<li><p><strong>后台加载切换</strong> — 生成 <code>BG#churn</code> 标记</p></li>
<li><p><strong>可滚动列表</strong> — 生成帧渲染事件</p></li>
</ul>


<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Kkwdpn2Wvc8hadZlx56COQ.png" alt="" /></p>

<p>截图由作者从 Android TraceToys 示例应用中截取。</p>

<h3>快速入门</h3>

<pre><code class="bash"># Record a 20-second trace
adb shell perfetto -o /data/misc/perfetto-traces/trace -t 20s sched gfx view dalvik
</code></pre>

<pre><code class="bash"># Interact with the app (tap UI Stall, scroll, toggle background load)# Pull and analyze
adb pull /data/misc/perfetto-traces/trace tracetoy_trace.perfetto-trace
python3 -m perfetto_agent.cli analyze \
  --trace tracetoy_trace.perfetto-trace \
  --focus-process com.example.tracetoy \
  --out analysis.json
</code></pre>

<p>输出应包含：</p>

<ul>
<li><p>已识别 TraceToy 进程（<code>com.example.tracetoy</code>，进程 ID 5664）</p></li>
<li><p>应用部分中出现 <code>BG#churn</code>（194 次，总计 11.8 秒）和 <code>UI#stall_button_click</code>（4 次，总计 818 毫秒）</p></li>
<li><p>199 个耗时超过 50 毫秒阈值的长任务</p></li>
<li><p>888 帧，其中 37 帧卡顿（p95：14.35 毫秒）</p></li>
</ul>


<h2>实现亮点：切片分类</h2>

<p>分析器中最有趣的部分是分类器。它使用显式标记列表，并且有意采用保守的分类方法：</p>

<pre><code class="python">def classify_slice_name(name: str | None, pid: int | None, focus_pid: int | None) -&gt; str:
    """Classify a slice into app/framework/system/unknown using pid + name tokens."""
    # If not from focus process, it's system
    if focus_pid is not None and pid is not None and pid != focus_pid:
        return "system"    
    if not name:
        return "unknown"    

    lower_name = name.lower()        
    app_tokens = ["ui#", "bg#", "startupinit"]
    framework_tokens = [
        "choreographer", "doframe", "renderthread", 
        "viewrootimpl", "dequeuebuffer", "blast", "hwui"
    ]
    system_tokens = ["binder", "surfaceflinger", "sched", "kworker", "irq", "futex"]    
    # Check app tokens first (highest confidence)
    if focus_pid is not None and pid == focus_pid:
        if any(token in lower_name for token in app_tokens):
            return "app"
        if any(token in lower_name for token in framework_tokens):
            return "framework"    
    if any(token in lower_name for token in system_tokens):
        return "system"    
    return "unknown"
</code></pre>

<p>关键见解：<strong>保守的分类方法优于错误的置信度</strong>。如果无法准确分类，则“未知”是诚实的答案。</p>

<p>请参阅<a href="https://github.com/singhsume123/perfetto-agent">完整实现</a>，了解启动检测、主线程解析和帧健康计算。</p>

<h2>为你的应用添加插桩</h2>

<p>为了最大限度地利用此分析，请对你的代码进行插桩：</p>

<pre><code class="kotlin">import android.os.Trace
class UserProfileLoader {
    fun loadProfile(userId: String): UserProfile {
        Trace.beginSection("UI#loadUserProfile")
        try {
            Trace.beginSection("UI#fetchFromCache")
            val cached = cache.get(userId)
            Trace.endSection()                        
            if (cached != null) return cached                        

            Trace.beginSection("IO#fetchFromNetwork")
            val profile = api.fetchProfile(userId)
            Trace.endSection()                        
            return profile
        } finally {
            Trace.endSection()
        }
    }
}
</code></pre>

<p>使用一致的命名约定：</p>

<ul>
<li><p><code>UI#</code> — 主线程/UI 操作</p></li>
<li><p><code>BG#</code> — 后台线程操作</p></li>
<li><p><code>IO#</code> — I/O 操作</p></li>
<li><p><code>DB#</code> — 数据库操作</p></li>
</ul>


<p>这使得分析器的输出能够立即转化为实际操作。</p>

<h2>亲自尝试</h2>

<p>该分析器是开源的：</p>

<p>🔗 <strong>Perfetto 分析器</strong>：<a href="https://github.com/singhsume123/perfetto-agent">github.com/singhsume123/perfetto-agent</a></p>

<p>🔗 <strong>TraceToy 测试应用</strong>：<a href="https://github.com/singhsume123/TraceToy">github.com/singhsume123/TraceToy</a></p>

<p>该仓库包含：</p>

<ul>
<li><p>Python 命令行分析器</p></li>
<li><p>示例跟踪和输出 (<code>analysis.json</code>)</p></li>
<li><p>设置说明</p></li>
</ul>


<h2>要点总结</h2>

<ol>
<li><p><strong>Perfetto 跟踪功能强大但信息量庞大</strong> — 程序化分析能够从噪声中提取有效信息</p></li>
<li><p><strong>归属至关重要</strong> — 了解谁执行了工作（进程、线程、组件）至关重要</p></li>
<li><p><strong>明确职责分离</strong> — 应用、框架和系统之间的职责划分最重要的区别</p></li>
<li><p><strong>时间窗口至关重要</strong> — 启动阶段和稳定阶段通常具有截然不同的特征</p></li>
<li><p><strong>嫌疑人指出问题所在，而非解释问题原因</strong> — 确定性信号告诉你应该首先从哪里入手</p></li>
<li><p><strong>保守的分类胜过错误的自信</strong> — “未知”总比错误好</p></li>
<li><p><strong>为你的代码添加插桩</strong> — <code>Trace.beginSection()</code> 是你的好帮手</p></li>
</ol>


<p>性能调试并非要找出<em>所有</em>发生的事情。而是要找出：</p>

<ul>
<li><p><strong>什么才是关键的</strong></p></li>
<li><p><strong>何时才是关键的</strong></p></li>
<li><p><strong>谁负责</strong></p></li>
</ul>


<p>此分析器将这个模型清晰地呈现出来，并且机器可读。</p>

<p>_有任何问题或反馈？请在 <a href="https://www.linkedin.com/in/sumeet-singh-arora-50605153/"><em>LinkedIn</em></a> 上联系我。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[务实的模块化：连接模块(wiring Modules)的妙用]]></title>
    <link href="https://alexhilton.github.io/blog/2026/01/03/pragmatic-modularization/"/>
    <updated>2026-01-03T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/01/03/pragmatic-modularization</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Pragmatic Modularization: The Case for Wiring Modules」，原文链接<a href="https://proandroiddev.com/pragmatic-modularization-the-case-for-wiring-modules-c936d3af3611">https://proandroiddev.com/pragmatic-modularization-the-case-for-wiring-modules-c936d3af3611</a>，由Alex Krafts发布于2025年11月22日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/0*9waVavO74e0cqQg6" title="auto auto" ></a></p>

<!-- more -->


<p>如果你正在经历漫长的模块化之旅，你应该已经阅读过官方的<a href="https://developer.android.com/topic/modularization">Google 开发者指南</a>。你可能已经见过关于提供依赖项的这种具体建议：</p>

<blockquote><p>“……app 模块通常是添加依赖项的好地方。要提供实现，请将其指定为所选构建变体或测试源集的依赖项。”</p></blockquote>

<p>下图展示了这种模式的一个常见示例，其中 <code>:app</code> 模块使用 <code>:database:impl:room</code> 作为其 <code>main</code> 源集，并使用 <code>:database:impl:mock</code> 作为 <code>androidTest</code> 源集。这是一种强大且正确的测试依赖项管理方法。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1304/0*Q3MP_6C6i4qhNhQr.png" alt="图 1. App 模块提供实际实现。" /></p>

<p>但这种模式存在一个<strong>微妙之处</strong>。当这种逻辑应用于大型项目中所有功能实现时，可能会<strong>无意中造成构建速度瓶颈</strong>，尤其是在处理遗留应用模块时。</p>

<pre><code class="kotlin">// :app/build.gradle.kts
dependencies {
    implementation(project(":database:api"))
    implementation(project(":database:impl:room")) // &lt;--- This is the trap for incremental builds
}
</code></pre>

<p>代码是模块化的，但你的 <code>:app</code> 模块现在与 <code>:database:impl:room</code> 模块<strong>构建耦合</strong>。这意味着任何<strong>资源更改</strong>（例如添加字符串）或对<strong>实现自身的公共接口</strong>的任何更改（例如在 <code>:impl</code> 内部添加一个不属于官方 <code>:api</code> 模块的新的 <code>public</code> 辅助类）都会破坏编译避免机制，并强制整个 <code>:app</code> 模块重新编译，从而抵消模块化带来的主要速度优势。</p>

<blockquote><p>即使启用了非传递 R 类等优化，直接依赖关系通常也会强制构建系统验证使用者是否与生产者的新资源符号匹配。布线模块充当防火墙，防止频繁的资源检查影响到庞大的 App 模块。</p></blockquote>

<h2>“精简应用”的理想状态与“臃肿应用”的现实</h2>

<p>官方建议让 <code>:app</code> 模块提供实现，这非常有效，尤其是在 Google 指南中展示的场景下：为不同的构建版本（例如 <code>main</code> 和 <code>androidTest</code>）替换依赖项。</p>

<p>但是，如果将此模式误用作所有功能实现的<em>通用规则</em>，尤其是在包含<strong>“臃肿应用”模块</strong>的项目中，就会出现一个微妙的陷阱。</p>

<p>官方指南通常会隐含地假设一种架构理想：<strong>“精简应用”模块</strong>。“精简应用”只是一个轻量级的汇编器。它几乎不包含任何代码或资源。它<em>唯一</em>的任务是应用 <code>com.android.application</code> 插件并将所有功能模块打包成一个 APK 文件。如果你的 <code>:app</code> 模块很精简，重新编译它既快速又便宜，因此它依赖于 <code>:impl</code> 模块并无大碍。</p>

<p>但对我们大多数人来说，现实情况是<strong>“厚应用”模块</strong>。它是最初的单体应用，仍然充斥着遗留代码、资源和半模块化的功能。</p>

<p>当你的“厚应用”模块直接依赖于某个功能的 <code>:impl</code> 时，例如 <code>:app</code> -> &ldquo;:database:impl:room&rdquo;<code>，你就创建了一个会拖慢构建速度的瓶颈。你最大、最复杂的模块现在与该功能的内部细节**构建耦合**了。任何对</code>:impl<code>中资源或实现自身公共接口的更改都会破坏编译规避机制，并强制整个</code>:app` 模块重新编译。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1304/1*su8uLnwQsJMVWsUxrlVBSg.png" alt="图 2. “厚应用”瓶颈。由于庞大的 :app 模块直接依赖于实现，因此底层任何 ABI 更改都会触发顶层的完全重新编译。红色箭头展示了功能模块中的一个微小改动如何迫使资源密集型的 `:app` 模块重新构建，从而破坏了避免编译的策略。" /></p>

<p>“现在，一个纯粹主义者可能会说：‘你应该更加自律，永远不要让你的 <code>:app</code> 模块从 <code>:impl</code> 导入任何东西，即使是公共的。’</p>

<p>他们的说法没错。但是，在一个拥有数十位贡献者的‘厚应用’单体应用中，‘自律’是一种脆弱的防御手段。而连接模块模式<strong>在构建系统层面强制执行了这种自律。</strong>它使得开发者<em>不可能</em>意外地将 <code>:app</code> 模块与某个功能的实现细节耦合在一起，从而默认地保证了构建速度。”</p>

<h2>“理想”方案 vs. “赋能”方案</h2>

<p>这让团队面临两条道路：</p>

<ol>
<li>“理想”（但缓慢）方案：纯粹主义者的解决方案是将“臃肿的应用”模块重构为真正“精简”的模块……这才是正确的长期目标，但这需要数年时间，循序渐进。</li>
<li>“赋能”（但快速）方案：这是“连接模块”模式。这种模式并非理想方案的替代方案，而是实现理想方案的催化剂。它提供了团队所需的构建速度和稳定性，使团队有时间和信心真正执行长期的增量重构。</li>
</ol>


<p>虽然这会增加“模块蔓延”，但它以架构的纯粹性换取了工程速度。在大型团队中，速度通常是更关键的指标。</p>

<p>但这种观点将问题简单地二元化了。实际上，这种模式是一种<strong>务实的权衡</strong>。你是在<strong>有意识地做出权衡</strong>。你接受了新的、可控的成本（增加模块数量和配置），以解决一个令人头疼的日常问题（缓慢的增量构建）。这种新的、明确的“连接”债务通常远比“臃肿应用”的单体架构债务成本低得多，后者正在扼杀团队的开发速度。这种构建速度往往正是团队能够拥有时间和稳定性来进行更大规模、更长期的重构的关键所在。</p>

<p>像 Slack、Spotify 和 Uber 这样的大型 Android 代码库都公开讨论过一些模式，这些模式大量依赖于将实现细节隐藏在稳定的 API 之后。这并非权宜之计；这是大型团队保持构建速度快和代码库可维护性的根本方法。</p>

<h2>解决方案：“连接模块”模式</h2>

<blockquote><p>这种模式有时被称为聚合模块，但我们更倾向于使用“连接”，因为它更能体现其在连接架构中的积极作用。</p></blockquote>

<p>此解决方案引入了一个新的轻量级<strong>“连接模块”</strong>（例如，<code>:database:wiring</code>）。该模块充当“守门人”或该功能的稳定外观。</p>

<p>其原理很简单：</p>

<ol>
<li><code>:app</code> 模块<em>仅</em>依赖于新的、稳定的 <code>:database:wiring</code> 模块。</li>
<li>连接模块<em>是唯一</em>了解其自身内部 <code>:impl</code>（以及 <code>:ui</code>、<code>:domain</code> 等）模块的模块。</li>
</ol>


<pre><code class="kotlin">// :app/build.gradle.kts (Corrected)
dependencies {
    // ...
    implementation(project(":database:wiring")) // NOW it's decoupled!
    implementation(project(":feature:home"))
}
</code></pre>

<pre><code class="kotlin">// :database:wiring/build.gradle.kts (The new "Wiring Module")
plugins {
    id("com.android.library")
}
android {
    namespace = "com.example.database.wiring"
}
dependencies {
    // Link the bindings internally without exposing the API transitively
    implementation(project(":database:api"))

    // Hide the implementation details from the :app module
    implementation(project(":database:impl:room"))
}
</code></pre>

<p>这种模式并非免费。这是一个务实的步骤，它能实现你承诺的快速增量构建，赋予你的团队速度和稳定性，从而支持那些规模更大、周期更长的重构。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1304/1*H2gD-qUVVI2r7CsTtMSt6Q.png" alt="图 3. 使用 :impl:room 模块 ABI 更改后的增量构建仅触发轻量级 :wiring 模块重建" /></p>

<p>现在，<code>:app</code> 模块仅依赖于稳定的 wiring 模块。</p>

<p><strong>关于依赖注入的说明：</strong></p>

<p>这个“wiring 模块”不仅仅是一个提升构建速度的技巧；它是你功能<strong>依赖注入绑定</strong>（例如 Dagger 或 Hilt 的 <code>@Module</code>）的理想存放位置。它的唯一职责就是将抽象的 <code>:api</code> 连接到具体的 <code>:impl</code>，这正是清晰依赖注入 (DI) 的精髓所在。</p>

<p>这种模式能够确保良好的架构，而构建速度的提升则是这种清晰分离带来的一个绝佳附加效果。</p>

<h2>为什么速度如此之快：理解编译避免</h2>

<p>这种结构并非权宜之计；它是启用 Gradle 最强大的构建速度提升功能——<strong>编译避免</strong>——的<em>正确</em>方法。</p>

<p>Gradle 团队在其精彩博文“<a href="https://blog.gradle.org/our-approach-to-faster-compilation">我们实现更快编译的方法</a>”中对此进行了解释。以下是关键要点：</p>

<ol>
<li><strong>Gradle 检查的是 ABI，而不是实现：</strong> 正如博文所述，Gradle 检查的是依赖项的<strong>应用程序二进制接口 (ABI)</strong>。ABI 是模块的“公共契约”或“公共结构”——即其公共类和方法签名。它<strong>不</strong>包含方法体等私有实现细节。</li>
<li><strong>旧方法会破坏这一点：</strong> 当你的 <code>:app</code> 模块直接依赖于 <code>:database:impl:room</code> 时，你实际上是在强制 Gradle 将整个实现都视为应用构建的一部分。正如我们之前提到的，<code>:database:impl:room</code> 中任何<strong>资源更改</strong>或对实现本身的<strong>公共接口</strong>（例如新增的 <code>public</code> 辅助函数）的更改，对于 <code>:app</code> 来说都是 ABI 不兼容的更改，从而导致需要完全重新编译。</li>
<li><p><strong>新方法可以解决这个问题：</strong> 使用我们的 <code>:database:wiring</code> 模块后：</p></li>
<li><p>开发人员更改了 <code>:database:impl:room</code> 中的一个文件。</p></li>
<li><code>:database:wiring</code> 模块重新编译（由于它很小，所以速度很快）。</li>
<li><strong>至关重要的是</strong>，正如 Gradle 博客文章中所解释的，这是一个 <strong>ABI 兼容的更改</strong>。连接模块的<em>公共结构</em>（即其 <code>api</code> 依赖项）并未改变。</li>
<li>Gradle 会检查 <code>:app</code>，发现其依赖项的 ABI 完全相同，因此<strong>完全跳过重新编译</strong> <code>:app</code>。</li>
</ol>


<p>这篇博文将此称为<strong>编译避免(Compilation Avoidance)</strong>（完全跳过模块的编译），并将其与<em>增量编译</em>（重新编译模块中的<em>某些</em>文件）区分开来。这种模式是实现主应用程序模块真正避免编译的最有效方法之一。</p>

<h2>实际应用：效果如何</h2>

<p>这并非纸上谈兵。我们在生产应用中使用 Gradle Profiler 来测试这种模式旨在解决的具体场景：在某个功能的 <code>:impl</code> 模块中进行破坏 ABI 的更改（例如，添加一个新的公共方法），然后运行增量 <code>:app:assembleDebug</code> 构建。</p>

<p>我们对八个不同的功能模块重复了此操作，并在引入连接模块前后分别对每个功能模块进行了平均 10 次运行。在旧配置中，<code>:app</code> 直接依赖于 <code>:feature:impl</code>，这些增量构建平均耗时约 <strong>99 秒</strong>。引入连接模块后，<code>:app</code> 仅依赖于 <code>:feature:wiring</code>，相同场景下的平均耗时降至约 <strong>63 秒</strong>。</p>

<p>在这八个功能模块中，速度提升幅度约为 <strong>29%</strong> 到 <strong>45%</strong>，在这种特定的增量构建场景下，平均提升幅度约为 <strong>36%</strong>。</p>

<p>在进行此次重构时，我恰好在研究是否值得将我的 MacBook 升级到更新的 Apple Silicon 机型。这个对比让我恍然大悟：对于这种工作负载，此次连接模块的更改所带来的构建速度提升，与升级到性能更强劲的高端 MacBook 机型**的效果不相上下。区别在于，这种重构能够有效地让参与项目的每个开发者都感受到“新机器”般的体验——而这仅仅是通过一个小的、有针对性的 Gradle 修改实现的，而非硬件升级。</p>

<h2>要点</h2>

<p>官方文档的建议在理论上是正确的，但理解实践中的权衡取舍至关重要。</p>

<p>对于任何实际的<strong>“厚应用”模块</strong>而言，直接依赖 <code>:impl</code> 模块通常会导致构建速度瓶颈，因为它将最大的模块与某个功能的内部变更耦合在一起。</p>

<p>保护构建速度至关重要。“模块连接”模式是一种非常实用且低成本的重构方式，它能显著提升日常工作流程的构建速度，而成本通常仅为完整 <code>:app</code> 模块重构的一小部分。</p>
]]></content>
  </entry>
  
</feed>
