<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Ai | 稀有猿诉]]></title>
  <link href="https://alexhilton.github.io/blog/categories/ai/atom.xml" rel="self"/>
  <link href="https://alexhilton.github.io/"/>
  <updated>2026-03-16T13:05:59+00:00</updated>
  <id>https://alexhilton.github.io/</id>
  <author>
    <name><![CDATA[Alex Hilton]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[把离线AI代理装进口袋里]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/16/offline-agent/"/>
    <updated>2026-03-16T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/16/offline-agent</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「The Dawn of Offline AI Agents in Your Pocket」，原文链接<a href="https://medium.com/@denisov.shureg/the-dawn-of-offline-ai-agents-in-your-pocket-55fff80d794b">https://medium.com/@denisov.shureg/the-dawn-of-offline-ai-agents-in-your-pocket-55fff80d794b</a>，由Sasha Denisov发布于2025年8月21日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/1*Ty7uTMMWx6UkF6xX-rng2Q.jpeg" title="auto auto" ></a></p>

<!-- more -->


<h3>前言</h3>

<p>在我之前的文章<a href="https://medium.com/@denisov.shureg/fine-tuning-gemma-with-lora-for-on-device-inference-android-ios-web-with-separate-lora-weights-f05d1db30d86">使用LoRA对Gemma进行微调，以实现设备端推理（Android、iOS、Web）并使用独立的LoRA权重</a>中，我详细介绍了如何使用LoRA对Gemma模型进行微调，以实现设备端推理。我们涵盖了理论基础，并准备好了用于移动部署的模型。现在，激动人心的时刻到了——真正开始构建利用这些强大AI功能的移动或Web应用程序。</p>

<p>经过数月对flutter_gemma插件的开发和完善，我已经了解了在移动应用中实现设备端AI的哪些方法有效（哪些无效）。今天，我将分享我关于如何创建用户真正想要使用的、可用于生产环境的AI功能的全部心得。</p>

<p>我们正在进入移动开发的新时代。有了flutter_gemma，你不再只是构建连接到AI服务的应用程序——你创建的是完全自主、多模态的AI代理，它们完全驻留在用户的设备上。</p>

<p>想想这意味着什么。你的应用现在可以：</p>

<ul>
<li><p>通过摄像头<strong>观察和理解</strong>世界，分析图像和文档，无需向云端发送任何数据</p></li>
<li><p><strong>推理和思考</strong>复杂问题，并将思考过程透明地展示给用户</p></li>
<li><p><strong>执行操作</strong>通过调用函数和集成设备功能，成为真正的数字助理</p></li>
<li><p><strong>随时随地使用</strong>——无论是在飞机上、偏远地区，还是仅仅为了保护用户隐私</p></li>
</ul>


<p>这不仅仅是为应用添加人工智能功能，而是从根本上重新定义移动应用。教育类应用可以成为理解手写作业的私人辅导老师；辅助功能应用可以成为与视障用户一同观察世界的AI伙伴；效率类应用可以成为不仅理解请求还能执行请求的智能代理。</p>

<h3>你将学到什么</h3>

<p>读完本文，你将掌握以下技能：</p>

<ul>
<li><p>flutter_gemma 是什么，以及如何在现有的 Flutter 项目中进行设置</p></li>
<li><p>为你的特定用例选择合适的模型</p></li>
<li><p>构建能够理解文本和图像的多模态应用</p></li>
<li><p>创建可以调用外部函数的 AI 助手</p></li>
<li><p>实现“思考模式”来展示 AI 的推理过程</p></li>
<li><p>应对移动 AI 特有的挑战（内存、性能、用户体验）</p></li>
<li><p>将 AI 应用部署到生产环境</p></li>
</ul>


<h2>入门：你的第一个 AI 驱动的 Flutter 应用</h2>

<p>我的旅程始于一个简单的目标：在 Flutter 应用中直接运行现代 AI 模型，完全离线。我探索了各种方案。有一些强大的工具，例如 <code>llama.cpp</code>，我的朋友 <a href="https://medium.com/@farmaker47">Georgios Soloupis</a> 在他的文章 <a href="https://medium.com/@farmaker47/run-gemma-and-vlms-on-mobile-with-llama-cpp-dbb6e1b19a93">使用 llama.cpp 在移动设备上运行 Gemma 和 VLM</a> 中出色地演示了它在 Android 上的应用。然而，就我使用 Flutter 的特定需求而言，我发现许多现有的解决方案，包括 <code>llama.cpp</code> 的封装，通常缺乏生产就绪的跨平台应用所需的稳定性和无缝集成。</p>

<p>突破性的进展来自于我发现了<a href="https://ai.google.dev/edge/mediapipe/solutions/guide">Google 的 MediaPipe</a>。它功能强大，并针对设备端任务进行了优化，但有一个主要问题：它实际上并没有对 Flutter 提供官方支持。意识到这一缺陷后，我决定自己来弥补。这就是 <a href="https://pub.dev/packages/flutter_gemma">flutter_gemma</a> 的由来。</p>

<p><code>flutter_gemma</code> 是一个 Flutter 插件，它通过 MediaPipe 将 Google 的 Gemma 系列和其他小型语言模型 (SLM) 的强大功能引入到你的应用中，使你能够在 iOS、Android 和 Web 上本地运行它们。自创建以来，它已被许多寻求稳定高效的方式来构建设备端 AI 功能的开发者所采用。</p>

<p>第一次运行它时，你会感觉像魔法一样，但这需要对细节的仔细关注。</p>

<p>完整的最新安装说明始终位于项目的 <a href="https://github.com/DenisovAV/flutter_gemma/blob/main/README.md">README.md</a> 中。<strong>务必严格按照 iOS、Android 和 Web 平台的具体步骤进行操作。</strong> iOS 的 <code>Podfile</code> 文件中哪怕出现一个小小的错误，或者缺少授权，都可能导致应用无法运行。</p>

<p>如果你遇到任何问题，仓库中包含一个完整的 <a href="https://github.com/DenisovAV/flutter_gemma/tree/main/example">示例应用</a>，保证可以正常运行。你可以将其作为参考，与自己的配置进行比较。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:536/1*4qmYed9S-kSjPgW9sN1Z-Q.gif" alt="flutter_gemma 示例app" /></p>

<h2>模型分发挑战</h2>

<p>在深入代码之前，我们先来解决一个显而易见的问题：如何将 1~6GB 的 AI 模型部署到用户的设备上？</p>

<p>我最初接触 flutter_gemma 时，以为这很简单。但我错了。每个人在看完演示后都会问的第一个问题是：“这很酷，但我该如何将模型交付给生产环境的用户呢？”</p>

<h3><strong>手动部署方法</strong></h3>

<p>从技术上讲，你可以手动将模型部署到设备上进行测试：</p>

<ul>
<li><p><strong>Android</strong>：使用 ADB 推送模型文件：<code>adb push model.task /sdcard/Android/data/your.app.id/files/</code></p></li>
<li><p><strong>iOS</strong>：使用 Xcode 的设备窗口或 iTunes 文件共享将模型复制到应用的文档目录</p></li>
<li><p><strong>Web</strong>：托管模型文件并提供直接下载链接</p></li>
</ul>


<p>但是，让用户在使用你的应用之前手动下载并安装 AI 模型，这难道不是一件好事吗？这可不是我心目中可以投入生产使用的用户体验。想象一下，你跟别人说：“嘿，下载我们的聊天应用！哦，对了，请手动下载这个 3GB 的文件，然后把它放到手机上正确的文件夹里。” :)</p>

<p>嗯，这肯定不会发生。</p>

<h3><strong>引入 ModelManager：解决方案</strong></h3>

<p>幸运的是，flutter_gemma 插件包含一个强大的 <strong>ModelManager</strong> 类，它抽象化了模型分发的所有复杂性。它可以自动处理平台特定的文件操作、进度跟踪和错误恢复。</p>

<p><strong>模型管理器</strong>提供了三种将模型部署到设备上的主要方法：</p>

<ol>
<li><p><strong>资源打包</strong> — 用于小型模型和开发</p></li>
<li><p><strong>网络下载</strong> — 用于生产环境应用（推荐）</p></li>
<li><p><strong>自定义路径</strong> — 用于高级集成场景</p></li>
</ol>


<p>下面我将逐一介绍每种方法及其适用场景：</p>

<h3><strong>资源打包方法（仅限有限使用）</strong></h3>

<p>Flutter 允许你将资源与应用打包在一起，最初，我认为这就是解决方案：</p>

<pre><code class="dart">// Don't do this for large models
await modelManager.installModelFromAsset('assets/models/gemma_1b.task');
</code></pre>

<p>这种方法对于不太大的模型非常有效，但存在<strong>平台限制</strong>：iOS 和 Android 对资源包大小都有严格的限制。 iOS 应用如果大于 4GB，在 App Store 提交时将无法正常处理。Android 也有自己的限制：应用包总大小上限为 4GB，但任何单一设备配置的压缩下载大小限制为 200MB，而传统 APK 的大小上限仅为 100MB。</p>

<p>其实有“正确”的方法！</p>

<h3><strong>生产环境方案：智能模型加载</strong></h3>

<p>我经过数十个应用的实践，最终完善的解决方案就是我所说的“渐进式模型加载”。工作原理如下：</p>

<pre><code class="dart">class ProductionModelManager {
  static const String MODEL_URL = 'https://your-cdn.com/models/qwen25_1_5b.task';
  static const String MODEL_FILENAME = 'qwen25_1_5b.task';
  Future&lt;void&gt; ensureModelReady() async {
    final modelManager = FlutterGemmaPlugin.instance.modelManager;        
    // Check if model is already downloaded
    if (await modelManager.isModelInstalled) {
      return; // We're good to go
    }        
    // Show download UI to user
    await _downloadModelWithProgress();
  }    

  Future&lt;void&gt; _downloadModelWithProgress() async {
    final modelManager = FlutterGemmaPlugin.instance.modelManager;        
    // This is where the magic happens - with real-time progress updates
    await modelManager.downloadModelFromNetwork(
      MODEL_URL,
      onProgress: (progress) {
        // progress is a double from 0.0 to 1.0
        final percentage = (progress * 100).toStringAsFixed(1);
        print('Download progress: $percentage%');                

        // Update UI with download progress
        setState(() {
          _downloadProgress = progress;
          _downloadStatus = 'Downloading AI model... $percentage%';
        });
      },
    );
  }    

  // UI method to show download progress
  void _updateDownloadProgress(double progress) {
    setState(() {
      _downloadProgress = progress;
      if (progress &gt;= 1.0) {
        _downloadStatus = 'AI model ready!';
      } else {
        final percentage = (progress * 100).toStringAsFixed(1);
        _downloadStatus = 'Downloading AI model... $percentage%';
      }
    });
  }
}
</code></pre>

<h3><strong>为什么网络下载是生产标准</strong></h3>

<p>这种方法的主要优势如下：</p>

<ol>
<li><p><strong>应用体积小</strong>：你的应用只需几秒即可下载，无需几分钟</p></li>
<li><p><strong>模型更新</strong>：无需在应用商店发布即可更新 AI 模型</p></li>
<li><p><strong>基于 LoRA 的动态个性化</strong>：通过动态下载小型 LoRA 适配器文件（几兆字节），即可将不同的微调行为立即应用到基础模型</p></li>
<li><p><strong>设备特定优化</strong>：根据设备功能提供不同的模型变体</p></li>
<li><p><strong>成本控制</strong>：用户只需在需要时下载所需内容</p></li>
<li><p><strong>A/B 测试</strong>：针对不同的用户群体测试不同的模型</p></li>
</ol>


<p>为确保下载快速可靠，该插件使用了 <a href="https://pub.dev/packages/background_downloader">background_downloader</a> 包以优化性能。</p>

<h3><strong>首次运行体验</strong></h3>

<p>以下是我发现效果最佳的用户流程：</p>

<ol>
<li><p>用户下载你的应用（体积小，下载速度快）</p></li>
<li><p>应用打开后显示引导页面，介绍 AI 功能</p></li>
<li><p>用户点击“启用 AI 功能”</p></li>
<li><p>应用下载模型并显示进度指示器</p></li>
<li><p>用户下载完成后即可立即开始使用 AI 功能</p></li>
</ol>


<blockquote><p><strong>我总结的关键用户体验洞察：始终显示进度</strong> — 用户需要知道正在发生什么，<strong>显示文件大小信息</strong> — “1.2GB 中的 3.2MB” 可以帮助用户理解等待时间，<strong>妥善处理错误</strong> — 提供重试选项，而不仅仅是错误消息</p></blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*LlZE9jp5CZZx04ad9c7qTQ.png" alt="应用下载模型示例页面" /></p>

<p>好了，我们已经找到了将模型交付到设备的方法。接下来我们来讨论推理。</p>

<h2>理解推理类型：聊天模式 vs. 单次响应模式</h2>

<p><code>flutter_gemma</code> 插件提供了两种不同的 AI 交互模式，每种模式都针对不同的用途进行了优化。选择合适的模式是构建高效应用的关键：<strong>单次推理</strong>适用于一次性、无状态的请求，而<strong>聊天界面</strong>适用于有状态的、持续的对话。</p>

<h3>单次推理：适用于无状态的单次任务</h3>

<p>可以将单次推理理解为每次都向模型的一个全新实例提出问题。模型不会记住过去的交互。这种模式非常适合事务性任务，因为在事务性任务中，不需要从一次请求到下一次请求的上下文信息。</p>

<p><strong>用途：</strong></p>

<ul>
<li><p><strong>文本摘要：</strong> 向模型提供一篇长文章，并请求其生成简洁的摘要。</p></li>
<li><p><strong>数据提取：</strong> 分析一段文本，提取特定信息，例如姓名、日期或情感倾向。</p></li>
<li><p><strong>简单问答：</strong> 回答一个独立的问题，例如“法国的首都是哪里？”</p></li>
<li><p><strong>图像分析：</strong> 使用具备视觉能力的模型描述单张图像的内容。</p></li>
</ul>


<p>以下是如何使用它来概括一段文本：</p>

<pre><code class="dart">// 1. Create a model instance
final model = await gemma.createModel(modelType: ModelType.general);

// 2. Create a stateless session
final session = await model.createSession();

// 3. Generate a single, streaming response
final article = "Your long article text goes here...";
final prompt = "Summarize the following article in three sentences: $article";
String summary = '';
await for (final token in session.generateResponseAsync(prompt)) {
  summary += token;
  // Update your UI with the streaming summary in real-time
}

print(summary);

// 4. Clean up the session
await session.close();
</code></pre>

<h3>聊天界面：用于有状态的对话式 AI</h3>

<p>聊天界面旨在构建对话体验。它会自动管理对话历史记录，因此每条新消息都能在之前所有对话的上下文中被理解。你可以使用此模式来构建聊天机器人、助手以及任何需要来回对话的功能。</p>

<p><strong>用途：</strong></p>

<ul>
<li><p><strong>客户支持聊天机器人：</strong> 在多个回合中协助用户解决问题。</p></li>
<li><p><strong>AI 导师：</strong> 通过持续的、不断发展的对话来帮助用户学习。</p></li>
<li><p><strong>多步骤任务执行：</strong>引导用户完成一个流程，其中 AI 需要记住之前的回答。</p></li>
</ul>


<p>以下是一个简单的多轮对话示例：</p>

<pre><code class="dart">// 1. Create a model and a stateful chat instance
final model = await gemma.createModel(modelType: ModelType.general);
final chat = await model.createChat();

// 2. Send the first user message
await chat.addQuery(Message.text(text: "I'm planning a trip to Japan. What's a good city for a first-time visitor?", isUser: true));

// 3. Stream the AI's response
String aiResponse1 = '';
await for (final response in chat.generateChatResponseAsync()) {
  if (response is TextResponse) {
    aiResponse1 += response.token;
  }
}
print("AI: $aiResponse1"); // e.g., "Tokyo is a great choice..."

// 4. Ask a follow-up question. The AI remembers the context (Japan, Tokyo).
await chat.addQuery(Message.text(text: "What's the best way to get around there?", isUser: true));

// 5. Stream the second response
String aiResponse2 = '';
await for (final response in chat.generateChatResponseAsync()) {
  if (response is TextResponse) {
    aiResponse2 += response.token;
  }
}
print("AI: $aiResponse2"); // e.g., "The subway system in Tokyo is fantastic..."
</code></pre>

<p>关键区别很简单：<strong>单次推理</strong>用于无记忆的事务性请求，而<strong>聊天界面</strong>用于随着时间的推移构建上下文和关系。</p>

<h3>你的第一个 AI 聊天</h3>

<p>既然我们已经了解了核心概念，那么了解它们实际应用的最佳方法是查看一个完整的、可运行的示例。这里只提供一个简单的代码片段，我鼓励你探索代码库中包含的示例应用程序。</p>

<p>它包含一个完全实现的简单聊天界面，演示了如何管理模型状态、处理用户输入以及显示来自离线 AI 机器人的流式响应。它是完美的起点和可靠的参考资料。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:640/1*jxN5jrA3uwcXGqH5uuiCNg.gif" alt="Gemma 3 1B 聊天功能" /></p>

<p>你可以在<a href="https://github.com/DenisovAV/flutter_gemma/tree/main/example">示例应用程序文件夹</a>中找到完整的实现。</p>

<blockquote><p>我学到的关键经验：始终显示加载状态。模型初始化在移动设备上可能需要 10~30 秒，用户需要知道正在发生什么。</p></blockquote>

<p>现在你已经了解如何实现聊天功能，接下来需要做出一个关键决定：为你的应用程序选择合适的 AI 模型。</p>

<h2>了解模型功能：选择你的 AI 合作伙伴</h2>

<p>并非所有模型都一样。经过数十种组合的测试，以下是我对不同应用场景下最佳模型的真实评估：</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*p5E8SapFpWBSC7BZkxM5Ow.png" alt="示例应用中可用的模型" /></p>

<h3>实际模型选择示例</h3>

<p>当我们构建 <a href="https://livecaptionsxr.com/">LiveCaptionsXR</a>（一个用于实时字幕的 AI 辅助功能平台）时，我需要快速推理、具备处理屏幕内容的视觉能力以及合理的内存占用。我选择了 <strong>Gemma 3n 2B</strong>，因为它是满足所有标准（尤其是视觉支持）的最小模型。</p>

<p>关键不在于是否存在单一的“最佳”模型，而在于根据你的主要应用场景进行选择。以下是简要指南：</p>

<ul>
<li><p><strong>纯函数调用：</strong> 如果你的应用的主要目标是将用户命令转换为操作（例如智能助手），<strong>Hammer 2.1</strong> 正是为此而设计的。它以极低的开销出色地使用各种工具。</p></li>
<li><p><strong>包含函数调用的通用聊天：</strong> 如果你需要一个优秀的对话者，并且能够可靠地使用各种工具，<strong>Qwen 2.5</strong> 或 <strong>Gemma 2</strong> 都是绝佳的选择。</p></li>
<li><p><strong>简单轻量级聊天：</strong> 如果你只需要一个功能强大的对话式 AI，而不需要高级功能，<strong>Gemma 3 1B</strong> 是我的首选。它只有 500MB，下载和初始化速度都非常快。</p></li>
<li><p><strong>查看 AI 的推理过程：</strong> 如果你想了解模型的思考过程，<strong>DeepSeek R1</strong> 是唯一支持“思考模式”的选项。</p></li>
<li><p><strong>针对高度特定、精细化的任务：</strong> 当你的任务非常具体且需要最高效率时，超紧凑的 <strong>Gemma 3 270M</strong> 是使用 LoRA 进行精细化调整的理想选择。你可以利用其极小的资源占用，创建高度专业化的专家模型。</p></li>
</ul>


<p>稍后我会详细讲解一些具体示例的实现。但在那之前，我们需要了解 <code>flutter_gemma</code> 的一些核心概念，这些概念将在每个实现中用到。</p>

<h2>核心概念：消息、响应和流式传输</h2>

<p><code>flutter_gemma</code> API 构建于几个核心概念之上，在构建 UI 之前，理解这些概念至关重要。从简单的文本字符串过渡到结构化数据，是解锁插件最强大功能的关键。</p>

<h3>消息类型：不仅仅是文本</h3>

<p>首先，你发送给模型的每一条信息都会被封装在一个 <code>Message</code> 对象中。这是对话历史记录的基本构建模块。你不再直接发送原始字符串，而是创建一个 <code>Message</code> 对象来描述输入的性质。这种面向对象的方法支持多模态输入和函数调用等高级功能。</p>

<p>以下代码展示了你可以构建的不同类型的消息：</p>

<pre><code class="dart">// Text-only message - the most common type
final textMsg = Message.text(text: "Hello AI!", isUser: true);

// Image + text (multimodal) - for vision-capable models
final imageMsg = Message.withImage(
  text: "What's in this image?",
  imageBytes: await getImageBytes(),
  isUser: true,
);

// Tool response - to feed the result of a function call back to the model
final toolMsg = Message.toolResponse(
  toolName: 'get_weather',
  response: {'temperature': 72, 'condition': 'sunny'},
);

// System information - for displaying info in the UI that isn't sent to the model
final systemMsg = Message.systemInfo(text: "Function completed");

// Thinking content - for displaying the model's reasoning process (DeepSeek only)
final thinkingMsg = Message.thinking(text: "Let me analyze this...");
</code></pre>

<h3>响应类型：理解 AI 输出</h3>

<p>正如你的输入是结构化的一样，模型的输出也是结构化的。模型不会仅仅返回一个最终的文本块。相反，它会返回一个 <code>ModelResponse</code> 对象流，其中每个对象代表一种不同的输出类型。模型可能生成纯文本，也可能决定调用你的某个函数。你的应用程序逻辑必须准备好处理来自模型的这些不同的“意图”。</p>

<p>这通常在响应流的 <code>listen</code> 代码块中处理，你可以在其中检查每个传入响应对象的类型并采取相应的操作：</p>

<pre><code class="dart">chat.generateChatResponseAsync().listen((response) {
  if (response is TextResponse) {
    // This is a regular text token. Append it to your chat bubble.
    _updateChatBubble(response.token);      
  } else if (response is FunctionCallResponse) {
    // The AI wants to call a function. Execute it now.
    await _handleFunctionCall(response);      
  } else if (response is ThinkingResponse) {
    // The AI is "thinking" (DeepSeek only). Show this in the UI.
    _updateThinkingBubble(response.content);
  }
});
</code></pre>

<h3>流式处理 vs. 批处理：用户体验差异</h3>

<p>这是构建优秀 AI 应用最重要的概念之一。你应该<strong>始终流式处理模型的响应</strong>，而不是等待生成完整的文本（批处理）。</p>

<p>原因完全在于用户体验。等待完整的响应可能需要几秒钟，这会让应用感觉缓慢、卡顿或“冻结”。相比之下，逐个 token 地流式处理响应会让应用感觉非常快速且交互性强，因为用户可以实时看到 AI 的“输入”。即使总生成时间相同，流式版本的“感知性能”也明显更好。</p>

<p>以下是标准实现方式，你需要累积流式令牌，并在每个新事件发生时更新 UI：</p>

<pre><code class="dart">String _accumulatedText = '';void _processStreamingResponse() async {
  await for (final response in chat!.generateChatResponseAsync()) {
    if (response is TextResponse) {
      setState(() {
        _accumulatedText += response.token;
        // Update the last message in your message list with the new accumulated text
        _messages.last = Message.text(text: _accumulatedText);
      });
    }
  }
}
</code></pre>

<p>现在我们已经了解了基本概念，接下来让我们通过详细示例来学习如何处理除文本之外的其他数据。</p>

<h2>高级功能 #1：多模态 AI（让你的应用拥有视觉能力）</h2>

<p>当我的 Flutter 应用实现图像理解功能时，我就知道移动 AI 已经跨越了一个门槛。以下是如何构建能够真正“看”和理解图像的应用。</p>

<h3>设置支持视觉的模型</h3>

<p>目前只有 Gemma 3 Nano 模型支持视觉功能。设置略有不同：</p>

<pre><code class="dart">final model = await _gemma.createModel(
  modelType: ModelType.gemmaIt,
  supportImage: true,

  // Enable vision
  maxNumImages: 1,

  // How many images per message
  maxTokens: 4096,

  // Vision models need more tokens
);

final chat = await model.createChat(
  supportImage: true,
  tokenBuffer: 512,

  // Larger buffer for image processing
);
</code></pre>

<h3>构建图像分析功能</h3>

<p>以下是我用于分析图像的完整实现：​​</p>

<pre><code class="dart">class ImageAnalyzer extends StatefulWidget {
  @override
  _ImageAnalyzerState createState() =&gt; _ImageAnalyzerState();
}

class _ImageAnalyzerState extends State&lt;ImageAnalyzer&gt; {
  Uint8List? _selectedImage;
  String _analysis = '';
  bool _analyzing = false;  
  Future&lt;void&gt; _pickAndAnalyzeImage() async {
    final picker = ImagePicker();
    final image = await picker.pickImage(source: ImageSource.gallery);        
    if (image != null) {
      final bytes = await image.readAsBytes();
      setState(() {
        _selectedImage = bytes;
        _analyzing = true;
      });            
      await _analyzeImage(bytes);
    }
  }  

  Future&lt;void&gt; _analyzeImage(Uint8List imageBytes) async {
    final message = Message.withImage(
      text: "Analyze this image in detail. What do you see?",
      imageBytes: imageBytes,
      isUser: true,
    );        
    await chat!.addQuery(message);        
    String analysis = '';
    await for (final response in chat!.generateChatResponseAsync()) {
      if (response is TextResponse) {
        setState(() {
          analysis += response.token;
          _analysis = analysis;
        });
      }
    }        
    setState(() =&gt; _analyzing = false);
  }    

  // ... UI implementation
}
</code></pre>

<p><img src="https://miro.medium.com/v2/resize:fit:644/1*R57tH8OGLcMoBG9q3TbQBQ.jpeg" alt="Gemma 3n Multimodality" /></p>

<h2>高级功能 #2：函数调用（当 AI 接触现实世界）</h2>

<p>函数调用是设备端 AI 真正强大的地方。AI 不仅可以生成文本，还可以执行实际操作——调用 API、更新数据库、控制设备功能。</p>

<h3>理解工具和函数</h3>

<p>可以将工具视为你向 AI 公开的 API。 AI 会根据用户请求决定何时调用函数：</p>

<pre><code class="dart">final List&lt;Tool&gt; appTools = [
  Tool(
    name: 'get_weather',
    description: 'Get current weather for a location',
    parameters: {
      'type': 'object',
      'properties': {
        'location': {
          'type': 'string',
          'description': 'City name or address'
        }
      },
      'required': ['location']
    }
  ),

  Tool(
    name: 'set_reminder',
    description: 'Create a reminder for the user',
    parameters: {
      'type': 'object', 
      'properties': {
        'title': {'type': 'string'},
        'datetime': {'type': 'string', 'format': 'datetime'},
        'priority': {'type': 'string', 'enum': ['low', 'medium', 'high']}
      },
      'required': ['title', 'datetime']
    }
  )
];
</code></pre>

<h3>实现函数执行</h3>

<p>当 AI 需要调用函数时，你需要执行该函数并返回结果：</p>

<pre><code class="dart">Future&lt;Map&lt;String, dynamic&gt;&gt; _executeTool(FunctionCallResponse functionCall) async {
  switch (functionCall.name) {
    case 'get_weather':
      final location = functionCall.args['location'] as String;
      return await _getWeatherData(location);          
    case 'set_reminder':
      final title = functionCall.args['title'] as String;
      final datetime = DateTime.parse(functionCall.args['datetime']);
      await _createReminder(title, datetime);
      return {'status': 'success', 'reminder_id': '12345'};          
    default:
      return {'error': 'Unknown function: ${functionCall.name}'};
  }
}

Future&lt;Map&lt;String, dynamic&gt;&gt; _getWeatherData(String location) async {
  // Call actual weather API
  final response = await http.get(
    Uri.parse('https://api.weather.com/v1/current?location=$location')
  );    

  if (response.statusCode == 200) {
    final data = jsonDecode(response.body);
    return {
      'temperature': data['temperature'],
      'condition': data['condition'],
      'humidity': data['humidity']
    };
  } else {
    return {'error': 'Failed to get weather data'};
  }
}
</code></pre>

<p><img src="https://miro.medium.com/v2/resize:fit:640/1*3YiIG1m_TCMzG5KYiCbqtQ.gif" alt="Gemma 3n 和函数调用" /></p>

<h2>高级功能 #3：思考模式（观察 AI 的推理过程）</h2>

<p>某些模型型号提供的“思考模式”非常吸引人——你可以亲眼目睹 AI 一步步解决问题的过程。这就像拥有一个透明的 AI，可以清晰地展示它的工作原理。</p>

<h3>理解思考模式</h3>

<p>启用思考模式后，AI 的推理过程与其最终结果将分开记录：</p>

<pre><code class="dart">final chat = await model.createChat(
  isThinking: true,
  modelType: ModelType.deepSeek,
  temperature: 0.7,
);

chat.generateChatResponseAsync().listen((response) {
  if (response is ThinkingResponse) {
    // AI is thinking - show reasoning process
    _showThinkingBubble(response.content);      
  } else if (response is TextResponse) {
    // Final answer - show normal response
    _showChatMessage(response.token);
  }
});
</code></pre>

<h3>构建思考模式 UI 组件</h3>

<p>我创建了可展开的“Thinking气泡”，让用户可以窥探 AI 的推理过程：</p>

<pre><code class="dart">class ThinkingBubble extends StatefulWidget {
  final String thinkingContent;
  final bool isComplete;    

  @override
  _ThinkingBubbleState createState() =&gt; _ThinkingBubbleState();
}

class _ThinkingBubbleState extends State&lt;ThinkingBubble&gt; {
  bool _isExpanded = false;  

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(vertical: 4),
      decoration: BoxDecoration(
        color: Colors.blue.withOpacity(0.1),
        borderRadius: BorderRadius.circular(12),
        border: Border.all(color: Colors.blue.withOpacity(0.3)),
      ),
      child: Column(
        children: [
          ListTile(
            leading: Icon(Icons.psychology, color: Colors.blue),
            title: Text(
              'AI is thinking...',
              style: TextStyle(color: Colors.blue, fontWeight: FontWeight.w500),
            ),
            trailing: widget.isComplete 
              ? IconButton(
                  icon: Icon(_isExpanded ? Icons.expand_less : Icons.expand_more),
                  onPressed: () =&gt; setState(() =&gt; _isExpanded = !_isExpanded),
                )
              : SizedBox(
                  width: 16,
                  height: 16,
                  child: CircularProgressIndicator(strokeWidth: 2),
                ),
          ),                    

          if (_isExpanded &amp;&amp; widget.thinkingContent.isNotEmpty)
            Container(
              padding: EdgeInsets.all(16),
              child: Text(
                widget.thinkingContent,
                style: TextStyle(
                  fontFamily: 'monospace',
                  fontSize: 12,
                  color: Colors.grey[700],
                ),
              ),
            ),
        ],
      ),
    );
  }
}
</code></pre>

<p><img src="https://miro.medium.com/v2/resize:fit:640/1*mv58b97DBd5NAUyFaYgKSQ.gif" alt="Deepseek R1，思考模式下的函数调用" /></p>

<h3>何时使用思考模式</h3>

<p>Thinking模式非常适合：</p>

<ul>
<li><p><strong>教育类应用</strong> — 学生可以了解 AI 如何解决问题</p></li>
<li><p><strong>调试工具</strong> — 开发者可以了解 AI 的问题解决方法</p></li>
<li><p><strong>复杂推理任务</strong> — 用户可以在信任结果之前验证 AI 的逻辑</p></li>
</ul>


<p>但它也带来了一些问题延迟较高且会消耗更多Token，因此请谨慎使用。</p>

<h2>实际应用案例</h2>

<h3>LiveCaptionsXR：AI 无障碍平台</h3>

<p><a href="https://livecaptionsxr.com/">LiveCaptionsXR</a> 就是一个强大的实际应用案例，它是一个基于 AI 的无障碍平台。该项目源于加州聋人开发者 <a href="https://www.linkedin.com/in/craigmerry/">Craig Merry</a> 的一个想法，我们合作开发了该项目，并参加了 <a href="https://www.kaggle.com/competitions/google-gemma-3n-hackathon">Gemma 3n Challenge</a>。我们的目标是解决听力障碍人士面临的一个关键问题：不仅转录语音，还能通过在 3D 空间渲染字幕来显示说话者的身份和位置。该项目目前正处于积极开发阶段，初始 MVP 版本现已发布。</p>

<p>该应用程序完全在设备端运行，以保护隐私并支持离线使用，它使用多模态 Gemma 模型来处理音频和视觉数据。它还利用 <code>flutter_gemma</code> 的函数调用功能，使用户能够通过自然语言语音命令控制辅助功能，例如字幕大小。你可以在 <a href="https://www.youtube.com/watch?v=Oz8nzt2cc3Q">YouTube</a> 上观看我们的宣传视频。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*aXATKiNVJ59-0IPuB645rg.png" alt="口袋里的离线增强现实" /></p>

<h3>离线菜单翻译器</h3>

<p>我们都遇到过这样的挑战：身处异国他乡，渴望品尝当地美食，却发现餐厅菜单上的语言你看不懂，而且没有 Wi-Fi。这正是设备端多模态人工智能的完美应用场景，正如 Csongor Vogel 在一篇精彩的文章 <a href="https://www.linkedin.com/in/csongorvogel/">Using Gemma for Flutter apps</a> 中展示的那样。</p>

<p>借助 <code>flutter_gemma</code>，你可以构建一个使用视觉模型作为个人翻译器的应用。用户只需将摄像头对准菜单即可。模型会处理图像，识别外语文本（例如日语），并将其翻译成用户的语言。关键在于，所有操作都在设备端即时完成，确保应用在任何地方都能正常运行，并且图像始终保存在用户的手机中。</p>

<p>其核心逻辑是将图像连同翻译请求一起发送给模型，如下方简化示例所示。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*7v6Lsv96P20mstLfy4KQCA.png" alt="从左到右：应用加载 Gemma 3N 模型。然后，我们发送一张日文菜单图片进行翻译。最后，应用显示翻译后的菜单" /></p>

<h3>MenuMind：你的智能营养指南</h3>

<p>MenuMind 也诞生于 <a href="https://www.kaggle.com/competitions/google-gemma-3n-hackathon">Gemma 3n 挑战赛</a>，它是开发者 <a href="https://www.linkedin.com/in/mohamed-abdo95">Mohamed Abdo</a> 的项目。这款应用将菜单分析的概念更进一步。它不仅提供翻译功能，还能作为智能营养和过敏原指南。</p>

<p>用户可以拍摄菜单照片，多模态人工智能不仅会翻译菜品名称，还会识别潜在的过敏原或提供营养信息。这完美地展现了如何利用模型的推理能力，为有饮食限制的用户提供真正的价值。你可以在 YouTube 上观看该应用的演示视频。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*iwTiWN2x-5jx5kLm0cvQHQ.png" alt="" /></p>

<h3>紧急助手：离线急救助手</h3>

<p>另一个令人振奋的项目是<a href="https://github.com/TinyBigLabs/emergency-buddy">紧急助手</a>，这是一款可靠的离线急救助手应用。这款应用由 Siddharth Joshi、Vera Austermann 和 Jakub Niemiec 共同开发，旨在为各种紧急情况提供分步指导，涵盖从割伤、烧伤到更严重的伤情。</p>

<p>正如他们在 YouTube 上的演示视频所示，该应用的关键特性在于其可在设备本地运行，即使没有网络连接也能可靠使用。为了确保指导的安全性和准确性，团队利用 LoRA 技术对 Gemma 模型进行了微调，使其能够更好地识别医学术语。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*bjildf26qIGm-rKF2Hq_QQ.png" alt="" /></p>

<h2>未来路线图和社区</h2>

<p>flutter_gemma 插件正在积极发展。以下是即将推出的功能以及你如何参与其中：</p>

<h3>即将推出的功能</h3>

<p><strong>接下来：</strong></p>

<ul>
<li><p><strong>完整的多模态 Web 支持：</strong> 在 Web 平台上实现与实体平台相同的图像输入功能。</p></li>
<li><p><strong>文本嵌入支持：</strong> 添加生成文本嵌入的功能，这是实现设备端搜索的关键第一步。</p></li>
<li><p><strong>设备端 RAG 管道：</strong> 实现辅助类和示例，用于构建完整的检索增强生成系统，该系统可以查询本地向量数据库。</p></li>
</ul>


<p><strong>更远的未来：</strong></p>

<ul>
<li><p><strong>桌面支持（macOS、Windows、Linux）：</strong> 将设备端推理功能引入桌面平台。</p></li>
<li><p><strong>音频和视频输入：</strong> 扩展多模态功能，使其能够处理音频和视频流。</p></li>
<li><p><strong>音频输出（文本转语音）：</strong> 集成设备端文本转语音功能，使 AI 能够以语音方式响应。</p></li>
</ul>


<h2>为项目做贡献</h2>

<p>我创建 flutter_gemma 是因为我相信设备端 AI 是移动应用的未来。社区的反响非常热烈，我非常希望得到你的帮助，让它变得更好：</p>

<h3><strong>贡献方式：</strong></h3>

<ul>
<li><p><strong>报告问题</strong> — 发现 bug？请在 GitHub 上提交 issue。</p></li>
<li><p><strong>修复 bug</strong> — 发现你可以解决的未解决问题？欢迎提交 pull request。</p></li>
<li><p><strong>实现功能</strong> — 受到路线图的启发？欢迎为新功能做出贡献。</p></li>
<li><p><strong>分享示例</strong> — 开发了一些很棒的作品？与社区分享</p></li>
<li><p><strong>改进文档</strong> — 帮助新手更轻松地上手</p></li>
<li><p><strong>测试新模型</strong> — 试用新模型版本并分享性能数据</p></li>
</ul>


<p><strong>GitHub 代码库</strong> <a href="https://github.com/DenisovAV/flutter_gemma">https://github.com/DenisovAV/flutter_gemma</a></p>

<p>特别感谢所有为改进本项目做出贡献的开发者，包括 <a href="https://github.com/leehack">Jhin Lee</a>、<a href="https://github.com/Maksimka101">Zemlianikin Max</a>、<a href="https://github.com/arrrrny">Ahmet TOK</a>、<a href="https://github.com/gerfalcon">Csongor Vogel</a>、<a href="https://github.com/AlexVegner">Alex Vegner</a> 和 <a href="https://github.com/Vinayak006">Vinayak Amirtharajand</a>。</p>

<p>想了解更多人工智能驱动的 Flutter 内容？关注我，深入探讨移动人工智能、性能优化和实际应用策略。让我们携手共建智能移动应用的未来！</p>

<p><strong>未来比你想象的更近，离线代理是其中的重要组成部分！</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*dk1HG14_Q8SrnWgJgfUR7w.jpeg" alt="" /></p>

<p>本文是我关于移动应用中人工智能实际应用系列文章的一部分。接下来是：“使用 FunctionGemma 进行设备端函数调用”——订阅即可获取最新资讯。</p>

<p><strong>联系我：</strong> <a href="https://medium.com/r?url=https%3A%2F%2Fgithub.com%2FDenisovAV">GitHub</a>, <a href="https://www.linkedin.com/in/aleks-denisov/">LinkedIn</a>, <a href="https://medium.com/@denisov.shureg">Medium</a>, <a href="https://x.com/ShuregDenisov">X</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[使用LoRA微调Gemma实现移动端推理]]></title>
    <link href="https://alexhilton.github.io/blog/2026/03/13/fine-tune-gemma/"/>
    <updated>2026-03-13T00:00:00+00:00</updated>
    <id>https://alexhilton.github.io/blog/2026/03/13/fine-tune-gemma</id>
    <content type="html"><![CDATA[<blockquote><p>本文译自「Fine-Tuning Gemma with LoRA for On-Device Inference (Android, iOS, Web) with Separate LoRA Weights」，原文链接<a href="https://medium.com/google-developer-experts/fine-tuning-gemma-with-lora-for-on-device-inference-android-ios-web-with-separate-lora-weights-f05d1db30d86">https://medium.com/google-developer-experts/fine-tuning-gemma-with-lora-for-on-device-inference-android-ios-web-with-separate-lora-weights-f05d1db30d86</a>，由 Sasha Denisov发布于2025年2月4日。</p></blockquote>

<p><a href=""><img src="https://miro.medium.com/v2/resize:fit:1400/0*Ld0o1VW6z-VPbpgi" title="auto auto" ></a></p>

<!-- more -->


<h2>前言</h2>

<p>最近，我对边缘人工智能 (Edge AI) 产生了浓厚的兴趣。它最吸引我的地方在于它所蕴含的巨大潜力。在我看来，未来属于这种方法。人工智能模型正变得越来越强大、越来越精简，它们能够在大幅减少资源消耗的同时，提供令人印象深刻的结果。与此同时，设备的性能也在不断提升，使得直接在设备上运行资源密集型模型成为可能。</p>

<p>想象一下，人工智能代理无需网络连接即可高效运行。无需任何持续的维护成本。它们可以从你的行为中学习，同时将所有数据安全地保存在你的设备上，绝不会传输到其他地方。这听起来像是一个梦想，对吧？不，这不再仅仅是一个梦想。这已经接近成为现实，我坚信这个领域在未来将以惊人的速度发展。</p>

<p>在尝试这些技术的过程中，我意识到现有信息非常分散。对于刚刚涉足机器学习领域的移动开发者来说，要理清所有这些信息可能是一项艰巨的任务。因此，我决定撰写一系列文章，通过示例，一步一步地详细解释如何使用开源 AI 模型，并将其部署到移动设备和浏览器上。</p>

<p>作为本系列的第一篇文章，我将重点介绍一个我一直想深入探讨的用例：如何使用自己的数据微调模型，以便在移动设备上使用。但与每次都上传微调后的模型不同，你只需更新额外的 LoRA 权重，而无需更改基础模型。这种方法不仅高效，而且非常实用，我很高兴与大家分享所有细节。</p>

<h3>术语解释</h3>

<p>在深入探讨主题之前，我们先来解释一下标题中的一些术语，以防其中一些术语你还不熟悉：<strong>Gemma</strong>、<strong>微调（Fine-tuning）</strong>、<strong>LoRA</strong>、<strong>设备端推理</strong> 和 <strong>MediaPipe</strong>。</p>

<p>这些术语中的每一个都足以写成一篇文章，但我不会这样做，因为已经有很多相关的文章了。你可以点击提供的链接查看这些文章，我这里只做简要的定义。</p>

<ul>
<li><p><a href="https://ai.google.dev/gemma"><strong>Gemma</strong></a>：一个功能强大且灵活的开源 AI 模型，专为各种应用场景而设计。它足够小巧，可以在设备上使用，同时又能提供出色的性能，使其成为边缘 AI 应用的理想选择。</p></li>
<li><p><a href="https://en.wikipedia.org/wiki/Fine-tuning_(deep_learning)"><strong>微调</strong></a>: 指使用新的特定数据训练现有模型，使其适应特定任务的过程。微调允许你利用预训练模型的知识，并根据你的需求进行调整，而无需从头开始重新训练模型。</p></li>
<li><p><a href="https://huggingface.co/docs/peft/main/conceptual_guides/lora"><strong>LoRA（低秩自适应）</strong></a>: 一种用于微调大型模型的巧妙技术。LoRA 不是修改整个模型，而是添加小型高效的层来使模型适应特定任务。这保持了基础模型不变，从而使更新轻量高效。</p></li>
<li><p><strong>设备端推理</strong>：指直接在设备（例如智能手机或笔记本电脑）上运行 AI 模型，而不是依赖云服务器。这种方法提高了隐私性，降低了延迟，并且无需互联网连接。</p></li>
<li><p><a href="https://ai.google.dev/edge/mediapipe/solutions/guide"><strong>MediaPipe</strong></a>：谷歌开发的一个功能强大的框架，用于构建 AI 流水线，尤其适用于设备端应用。MediaPipe 可以轻松地将机器学习模型集成到实时移动或桌面应用中。</p></li>
</ul>


<p><strong>因此，文章标题可以扩展如下：</strong></p>

<blockquote><p>在本文中，你将学习如何通过使用你自己的数据训练（微调）来定制一个强大而灵活的 AI 模型（Gemma），使其适应特定任务。你无需修改​​整个模型，而是添加一些小而高效的层，从而实现轻量级的更新并保持基础模型不变（LoRA）。模型适配完成后，你就可以直接在设备上运行它（设备端推理），从而确保隐私、降低延迟，并消除对互联网连接的需求。你可以使用功能强大的框架（Mediapipe）将定制模型集成到本地运行的实时应用程序中。</p></blockquote>

<h2>第一章：设备端推理</h2>

<p>让我们从最后一步开始。要在本地设备上运行模型，你需要 <strong>MediaPipe</strong>。它提供适用于 <strong>Android、iOS 和 JavaScript</strong> 的 SDK，你可以在原生移动应用和浏览器中使用它们。此外，对于 <strong>Flutter</strong> 应用，还有一个名为 <a href="https://pub.dev/packages/flutter_gemma"><strong>flutter_gemma</strong></a> 的插件（也是由我开发的）。该插件允许你将 <strong>MediaPipe</strong> 无缝集成到 Flutter 应用中。</p>

<p><strong>MediaPipe</strong> 支持 <strong>LiteRT</strong> 格式（以前称为 <strong>TensorFlow Lite</strong>）的模型。好消息是，<strong>Google</strong> 团队已经为你准备好了这种格式的模型！你可以在 <a href="https://www.kaggle.com/models/google/gemma/frameworks/tfLite/"><strong>Kaggle</strong></a> 上找到 <strong>Gemma</strong> 和 <strong>Gemma-2</strong>，将它们下载到你的计算机，然后传输到你的手机以供本地使用。</p>

<blockquote><p>Kaggle 是一个面向数据科学和机器学习竞赛的在线平台，用户可以在这里访问数据集、编写和共享代码、与其他数据科学家协作，并参与挑战以构建预测模型。它还提供免费的云端 Jupyter Notebook 和用于学习人工智能和数据科学的教育资源。</p></blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*igU0KVSFYDICbx8GO832Tg.png" alt="" /></p>

<p>我在另一篇文章中更详细地介绍了<strong>在设备上运行模型</strong>，因此，有关如何将模型上传到设备的详细说明，请阅读<a href="https://medium.com/@denisov.shureg/the-dawn-of-offline-ai-agents-in-your-pocket-55fff80d794b">“把离线AI代理装进口袋里”</a>。但在本文中，我想重点讨论一些不同的内容：如果你拥有自己的数据集，想要用它来训练模型，然后再将其应用到你的设备上，该如何实现呢？</p>

<h2>第二章：微调——理论</h2>

<p>我之前已经给出了<strong>微调（Fine-tuning）</strong>的定义，但我们再回顾一下这个过程。</p>

<p>我们从一个<strong>预训练模型</strong>开始，它已经从一个大型数据集中学习到了通用模式。然后，我们使用来自我们自己数据集的额外数据<strong>进一步训练它</strong>。结果，<strong>微调后的模型</strong>能够适应我们的特定任务，并提供能够考虑这些新数据的响应。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/0*gsyxxOPwcoazG8US" alt="" /></p>

<p>有很多不同的<strong>微调</strong>方法，包括<strong>完全微调</strong>、<strong>顶层微调</strong>、<strong>基于适配器的微调</strong>等等。每种方法都有其自身的优缺点，因此适用于不同类型的任务。</p>

<p>就我们的情况而言，<strong>低秩自适应 (LoRA)</strong> 是最佳选择。这种方法能够在保持资源占用极低的情况下获得卓越的结果。此外，LoRA 还具有其他几个优势，使其特别适用于我们的场景——稍后我会详细介绍。</p>

<blockquote><p>低秩自适应 (LoRA) 是一种微调技术，它通过冻结模型权重并向模型中插入少量新权重，大幅减少了下游任务的可训练参数数量。这使得使用 LoRA 进行训练速度更快、内存效率更高，并且生成的模型权重更小（仅几百 MB），同时还能保持模型输出的质量。</p></blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/0*9uAypz2ElKWcEwl3" alt="" /></p>

<p>请看以下可视化图：想象一下，<strong>大型冻结机器人</strong>是一个<strong>预训练的AI模型</strong>。它功能强大，知识丰富，但却是静态的，并非针对特定任务而设计。<strong>小型机器人</strong>则代表<strong>LoRA</strong>，这是一个轻量级的自适应层，它无需修改大型模型的核心即可对其进行引导。小型机器人无需重新训练整个庞大的机器人（这将耗费大量资源），只需极少的计算成本和精力即可帮助其微调和调整行为。</p>

<p>这样，<strong>LoRA使我们能够在不“融化”整个冻结模型的情况下取得显著成果——只需在合适的位置进行细微但有效的调整即可</strong>。这就是为什么这种方法能够如此高效地将大型模型应用于新任务的原因。</p>

<p>但这种方法最令人兴奋的特点在于，它允许将 LoRA 权重与模型本身分开存储。这意味着我们每次微调模型时无需下载整个几 GB 的模型——这对移动应用来说是一项颠覆性的变革。</p>

<p>在<strong>标准的微调场景</strong>中，每次收集新数据并重新训练模型时，我们都需要<strong>重新下载整个模型</strong>。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/0*4jn7dxkOBILUBWVM" alt="" /></p>

<p>有两篇文章“<a href="https://aashi-dutt3.medium.com/part-2-fine-tune-gemma-2b-it-model-a26246c530e7">微调 — Gemma 2b-it 模型</a>”，作者是</p>

<p><a href="https://medium.com/u/b487344371f3?source=post_page---user_mention--f05d1db30d86---------------------------------------">Aashi Dutt</a> 和“[部署 Gemma 于<a href="https://medium.com/u/891e2ca7d96?source=post_page---user_mention--f05d1db30d86---------------------------------------">Nitin Tiwari</a> 的文章“Android”描述了这种方法，强烈推荐也去看一下。</p>

<p>然而，使用<strong>LoRA</strong>，我只需<strong>下载一次</strong>基础模型，每次微调后，只需更新一个<strong>包含LoRA权重的小文件</strong>。</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*ZtQbWu44asVxd2psxBC3mg.png" alt="" /></p>

<p>这无疑是一项巨大的优势，尤其是在资源受限的环境中，例如移动应用或 Web 应用。而这正是我要在此详细探讨的方法！</p>

<p><strong>MediaPipe 支持多种用于 LLM 推理任务的开放模型，让你可以轻松地在设备上直接运行强大的 AI 模型。以下是一些支持的模型：</strong></p>

<ul>
<li><p><strong>Gemma 2B 和 Gemma 7B</strong> — 由 Google 开发的开放权重语言模型，针对 NLP 任务的效率和性能进行了优化。这些模型非常适合文本生成、聊天机器人和文本摘要。</p></li>
<li><p><strong>Gemma-2 2B</strong> — Gemma 的更新版本，在设备端推理的效率和适应性方面有所提升。</p></li>
<li><p><strong>Phi-2</strong> — 一款紧凑而强大的模型，专为推理和通用自然语言处理任务而设计，针对小型硬件进行了优化，同时保持了出色的性能。</p></li>
<li><p><strong>Falcon-RW-1B</strong> — Falcon 系列中的轻量级模型，非常适合资源高效的语言生成和理解。</p></li>
<li><p><strong>StableLM-3B</strong> — 一款专为开放式文本生成而设计的模型，在相对较小的资源占用下提供强大的语言处理能力。</p></li>
</ul>


<p>在我的示例中，我使用的是 <strong>Gemma-2B</strong>，但所有这些模型的微调和部署方法都<strong>类似</strong>，可以根据用例和硬件限制灵活调整。</p>

<h2>第三章：微调 — 实践</h2>

<p>当然，你可以在<strong>笔记本电脑</strong>上微调模型，但这会消耗大量资源，最好使用<strong>性能更强大的机器</strong>。我使用的是<strong>Google Colab</strong>，这是一个基于云的平台，提供免费和付费的GPU和TPU资源**，让你无需高端硬件即可轻松训练AI模型。</p>

<blockquote><p><strong>Google Colab</strong>（或<strong>Colaboratory</strong>）是一个<strong>基于云的Jupyter Notebook环境</strong>，允许你在浏览器中编写和执行Python代码。它提供<strong>免费的GPU和TPU资源</strong>，非常适合机器学习、深度学习和AI模型训练，而无需强大的本地计算机。</p>

<p>✅ <strong>无需设置</strong> — 完全在云端运行。</p>

<p>✅ <strong>免费GPU/TPU资源</strong> — 非常适合训练AI模型。</p>

<p>✅ <strong>支持 Python 和流行的机器学习库</strong>，例如 TensorFlow、PyTorch 和 Hugging Face。</p>

<p>✅ <strong>支持协作</strong> — 可与他人实时共享和编辑笔记本</p></blockquote>

<p>🔗 <a href="https://colab.research.google.com/github/DenisovAV/flutter_gemma/blob/main/colabs/gemma_lora_1.ipynb"><strong>这是我的 Colab workshop链接</strong></a>，我使用一个<strong>公开的名人名言数据集</strong>来训练模型，使其能够根据提示语的开头（例如名言）以特定风格继续回答。你可以按照workshop的步骤进行操作，既可以复制我的流程，也可以根据自己的数据集和目标进行调整。</p>

<p>为了方便起见，我也会在下方<strong>详细分解步骤</strong>。</p>

<h3>步骤 1：选择运行时</h3>

<p>为了高效运行本教程，你需要在 <strong>Google Colab</strong> 中使用 GPU：</p>

<ol>
<li><p>点击 <strong>运行时</strong> → <strong>更改运行时类型</strong></p></li>
<li><p>在 <strong>硬件加速器</strong> 下，选择 <strong>T4 GPU</strong> 或 <strong>A100 GPU</strong>（如有可​​用，推荐使用）。</p></li>
</ol>


<h3>步骤 2：设置环境变量</h3>

<p>运行代码之前，请确保已将 <strong>Hugging Face Token</strong> 添加到 Colab 的 <strong>用户自定义变量</strong>：</p>

<ol>
<li><p>在 <strong>Colab 菜单</strong> 中，转到 <strong>工具</strong> → <strong>首选项</strong> → <strong>用户自定义变量</strong></p></li>
<li><p>添加 <code>HF_TOKEN</code> 作为键，并将你的 Hugging Face Token 作为值。在本工作坊中，我们将从 Hugging Face 加载模型。</p></li>
</ol>


<pre><code class="python">import os
from google.colab import userdata

# Set environment variables
os.environ["HF_TOKEN"] = userdata.get('HF_TOKEN')
os.environ["WANDB_MODE"] = "offline"
</code></pre>

<blockquote><p><strong>Hugging Face</strong> 是一个领先的 <strong>人工智能和自然语言处理模型</strong> 平台，提供庞大的 <strong>预训练模型、数据集和工具</strong> 库，用于机器学习。它提供 <strong>Transformers</strong>，一个用于处理深度学习模型的开源库，以及 <strong>模型中心</strong>，用户可以在这里查找、共享和部署用于各种人工智能任务的模型。</p></blockquote>

<h3>第三步：安装所需库</h3>

<p>安装所有必要的依赖项，以便进行微调、导出权重并将其与 MediaPipe 集成：</p>

<pre><code class="python">!pip install -q \
    transformers \
    mediapipe \
    bitsandbytes \
    peft \
    trl \
    datasets \
    fsspec==2024.6.1 \
    gcsfs==2024.9.0
</code></pre>

<p>以下是每个库的简要说明：</p>

<ul>
<li><p><strong>transformers</strong> — 提供用于自然语言处理 (NLP) 任务的预训练 AI 模型和工具。</p></li>
<li><p><strong>mediapipe</strong> — 一个用于在移动和 Web 应用程序上高效运行 AI 模型的框架。</p></li>
<li><p><strong>bitsandbytes</strong> — 提供内存高效的优化器和量化技术，有助于在硬件资源有限的情况下高效运行大型模型。</p></li>
<li><p><strong>peft</strong> — 一个用于<strong>高效参数微调（LoRA、适配器等）</strong>大型模型的库。</p></li>
<li><p><strong>trl</strong> — 一个用于<strong>强化学习 (RLHF)</strong>和微调大型语言模型的库。</p></li>
<li><p><strong>datasets</strong> — 一个用于访问和管理机器学习训练数据集的 Hugging Face 库。</p></li>
<li><p><strong>fsspec</strong> — 一个用于处理跨不同环境存储的文件系统抽象库。</p></li>
<li><p><strong>gcsfs</strong> — 一个用于使用 Python 与 <strong>Google Cloud Storage (GCS)</strong> 交互的库。</p></li>
</ul>


<p>这些库共同实现了对 AI 模型进行<strong>高效的微调、优化和部署</strong>，以支持设备端和云端推理。</p>

<h3>第四步：加载并保存预训练的 Gemma 模型</h3>

<p>要微调 <strong>Gemma</strong> 模型，我们首先需要从 Hugging Face 的 <strong>模型中心</strong> 加载 <strong>预训练模型</strong> 及其关联的 <strong>分词器</strong>。模型将保存在本地，以便后续用于训练。</p>

<pre><code class="python">import torch
from transformers import AutoModelForCausalLM, AutoTokenizer

# Define model ID
model_id = "google/gemma-2b"

# Load tokenizer
tokenizer = AutoTokenizer.from_pretrained(model_id)
tokenizer.save_pretrained("/content/gemma2b")

# Load model
model = AutoModelForCausalLM.from_pretrained(
    model_id,
    torch_dtype=torch.float16,
    device_map="auto"
)

# Save model locally
model.save_pretrained("/content/gemma2b")
</code></pre>

<p><strong><em>备注：</em></strong> 如果你想知道为什么我没有使用 <strong><em>4-bit量化</em></strong><em>，那是因为该模型目前在保存 Mediapipe 的 LoRA 权重时存在问题。一旦找到解决方案，我会相应地更新此步骤。</em></p>

<h3>步骤 5：使用预训练模型检查推理</h3>

<p>在进行微调之前，让我们测试一下 <strong>预训练的 Gemma-2B</strong> 模型，看看它如何根据给定的提示生成文本。这有助于我们在进行任何修改之前了解模型的基线性能。</p>

<pre><code class="python">text = "Quote: Imagination is"
device = "cuda:0"  # Ensure the model runs on GPU if available

# Tokenize the input and move it to the correct device
inputs = tokenizer(text, return_tensors="pt").to(device)

# Generate text with the model
outputs = model.generate(**inputs, max_new_tokens=20)

# Decode and print the generated text
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
</code></pre>

<p><strong>预期输出示例：</strong></p>

<pre><code class="bash">Quote: Imagination is more important than knowledge. Knowledge is limited. Imagination encircles the world. - Albert Einstein
</code></pre>

<p>此步骤有助于验证模型是否已正确加载并按预期运行，然后再开始进行微调。</p>

<p>备注：我引用了来自 <a href="https://medium.com/u/f428fad2b0d?source=post_page---user_mention--f05d1db30d86---------------------------------------">Mohammed Ashraf</a> 一篇很棒的<a href="https://medium.com/@mohammed97ashraf/your-ultimate-guide-to-instinct-fine-tuning-and-optimizing-googles-gemma-2b-using-lora-51ac81467ad2">文章“使用 LoRA 对 Google 的 Gemma 2B 进行 Instinct 微调和优化的终极指南”</a>中的示例。</p>

<h3>第六步：加载用于微调的数据集</h3>

<p>为了微调 Gemma-2B 模型，我们需要一个包含与目标任务相关的文本示例的数据集。此步骤如下：</p>

<ol>
<li><p><strong>加载数据集</strong> — 从 Hugging Face 的 <code>datasets</code> 库中检索一个开放数据集。</p></li>
<li><p><strong>格式化数据集</strong> — 将原始数据转换为模型可以使用的格式。</p></li>
<li><p><strong>准备分词</strong> — 将文本转换为用于微调的结构化格式。</p></li>
</ol>


<p>在本例中，我们使用一个<strong>英文名言</strong>数据集，但你可以将其替换为任何与你的用例相关的数据集。</p>

<pre><code class="python">from datasets import load_dataset

# Load a dataset of English quotes
data = load_dataset("Abirate/english_quotes")

# Function to format dataset properly
def formatting_func(example):
    if isinstance(example["quote"], list):
        return [
            f"Quote: {quote}\nAuthor: {author}&lt;eos&gt;"
            for quote, author in zip(example["quote"], example["author"])
        ]
    return [f"Quote: {example['quote']}\nAuthor: {example['author']}&lt;eos&gt;"]

print(formatting_func(data["train"]))
</code></pre>

<p><a href="https://medium.com/u/b487344371f3?source=post_page---user_mention--f05d1db30d86---------------------------------------">Aashi Dutt</a> 有一篇不错的<a href="https://aashi-dutt3.medium.com/part-1-step-by-step-dataset-creation-unstructured-to-structured-70abdc98abf0">文章“Step-by-Step Dataset Creation- Unstructured to Structured”</a>，解释了如何准备自己的数据集。</p>

<h3>第 7 步：配置和应用 LoRA 进行微调</h3>

<p><strong>LoRA（低秩自适应）</strong> 是一种通过添加小型可训练层而不是修改整个模型来高效微调大型模型的方法。</p>

<p><strong>步骤：</strong></p>

<ol>
<li><p><strong>定义 LoRA 参数</strong> — 配置关键设置，例如 rank、目标模块和任务类型。</p></li>
<li><p><strong>将 LoRA 应用于模型</strong> — 将 LoRA 层附加到特定的 Transformer 组件，以最大限度地减少内存使用。</p></li>
</ol>


<pre><code class="python">from peft import LoraConfig, get_peft_model

# Configure LoRA settings
lora_config = LoraConfig(
    r=8,  # Rank of the LoRA adaptation matrices
    target_modules=["q_proj", "o_proj", "k_proj", "v_proj", "gate_proj", "up_proj", "down_proj"],  # Layers to apply LoRA
    task_type="CAUSAL_LM",  # Task type: Causal Language Modeling
)

# Apply LoRA to the model
model = get_peft_model(model, lora_config)
</code></pre>

<h3>步骤 8：微调模型</h3>

<p>将 LoRA 应用于模型后，我们进入<strong>微调</strong>阶段，在此阶段，我们仅更新<strong>已适配 LoRA 的层</strong>，同时保持基础模型不变。这使我们能够<strong>高效地将模型</strong>适配到特定数据集，而无需大量的计算资源。</p>

<p><strong>微调过程详解：</strong></p>

<h4>1️⃣ 定义训练参数</h4>

<p>我们设置控制训练过程的关键参数：</p>

<ul>
<li><p><strong>批次大小</strong> (<code>per_device_train_batch_size</code>) — 定义一次处理的样本数量。</p></li>
<li><p><strong>梯度累积步数</strong> (<code>gradient_accumulation_steps</code>) – 累积多个步长的梯度，以模拟更大的批次大小。</p></li>
<li><p><strong>预热步数</strong> (<code>warmup_steps</code>) – 在训练初期逐步提高学习率，以稳定训练过程。</p></li>
<li><p><strong>最大步数</strong> (<code>max_steps</code>) – 指定训练迭代的总次数。</p></li>
<li><p><strong>学习率</strong> (<code>learning_rate</code>) – 控制模型在每个训练步中更新参数的幅度。</p></li>
<li><p><strong>FP16 训练</strong> (<code>fp16=True</code>) – 使用混合精度进行训练，速度更快，内存效率更高。</p></li>
<li><p><strong>日志记录步数</strong> (<code>logging_steps</code>) – 确定记录训练指标的频率。</p></li>
<li><p><strong>优化器</strong> (<code>optim="paged_adamw_8bit"</code>) – 使用内存高效的 8 位 AdamW 版本来优化训练。</p></li>
</ul>


<h4>2️⃣ 使用 <code>SFTTrainer</code> 进行高效训练</h4>

<p><code>SFTTrainer</code> 是 <code>trl</code> 开发的一个实用工具，专为大型语言模型的<strong>监督式微调</strong>而设计，可简化训练过程并与 <strong>LoRA</strong> 无缝集成。</p>

<h4>3️⃣ 训练模型</h4>

<p>仅更新 <strong>LoRA 层</strong>，基础模型保持不变。这显著减少了训练时间和内存使用量。</p>

<h4>4️⃣ 保存微调后的模型</h4>

<p>训练完成后，我们会保存 <strong>LoRA 微调后的模型权重</strong>，以便后续无需重新训练即可使用。</p>

<pre><code class="python">from transformers import TrainingArguments
from trl import SFTTrainer

# Define the training arguments
trainer = SFTTrainer(
    model=model,  # LoRA-enhanced model
    train_dataset=data["train"],  
    # Training dataset
    args=TrainingArguments(
        per_device_train_batch_size=1,  # Batch size per device
        gradient_accumulation_steps=4,  # Accumulate gradients over 4 steps
        warmup_steps=10,  # Steps to warm up the learning rate
        max_steps=100,  # Total number of training steps
        learning_rate=2e-4,  # Learning rate
        fp16=True,  # Use mixed precision for faster training
        logging_steps=1,  # Log metrics every step
        output_dir="outputs",  # Directory for saving checkpoints and logs
        optim="paged_adamw_8bit"  # Use 8-bit AdamW optimizer for memory efficiency
    ),
    peft_config=lora_config,  # LoRA configuration
    formatting_func=formatting_func,
)

# Train the model
trainer.train()

# Save the fine-tuned model
trainer.model.save_pretrained("/content/gemma2b/lora")
</code></pre>

<h3>步骤 9：使用推理测试微调后的模型</h3>

<p>使用 LoRA 对 <strong>Gemma-2B</strong> 模型进行微调后，运行 <strong>推理</strong> 进行测试至关重要。我们使用与微调前完全相同的提示符，并验证结果是否有所不同。这将使我们能够清晰地比较这些更改如何影响模型的输出，以及它是否更接近预期结果。</p>

<pre><code class="python">text = "Quote: Imagination is"
device = "cuda:0"  # Ensure the model runs on GPU if available

# Tokenize the input and move it to the correct device
inputs = tokenizer(text, return_tensors="pt").to(device)

# Generate text with the model
outputs = model.generate(**inputs, max_new_tokens=20)

# Decode and print the generated text
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
</code></pre>

<p><strong>预期输出示例：</strong></p>

<pre><code class="bash">Quote: Imagination is more important than knowledge.
Author: Albert Einstein
</code></pre>

<p><strong>为什么现在的输出不同了？</strong></p>

<p>由于我们使用 <strong>LoRA</strong> 对模型进行了微调，它已经从新的数据集中学习并相应地调整了其响应。之前，模型基于其原始训练数据生成通用响应。但是，微调后，模型的输出现在受到其训练所用的新模式、风格和信息的影响。</p>

<p>在这种情况下，如果我们使用<strong>哲学名言</strong>数据集进行微调，我们或许会看到更能体现这些思想的回复。如果我们使用<strong>技术文档</strong>进行训练，其补全结果将更加结构化和基于事实。这证实了<strong>微调成功地调整了模型的行为，同时保持了基础模型不变！</strong></p>

<h3>步骤 10：将微调后的模型转换为 MediaPipe 格式</h3>

<p>微调完成后，下一步是将模型<strong>转换为与 MediaPipe 兼容的格式</strong>，具体来说是<strong>LiteRT（原 TFLite）</strong>。这确保模型能够在<strong>移动设备和 Web 浏览器</strong>上高效运行，而无需占用大量计算资源。</p>

<h4>1️⃣ 定义转换配置</h4>

<p>我们创建一个 <code>ConversionConfig</code> 对象，用于指定：</p>

<ul>
<li><p><strong>模型检查点路径</strong> (<code>input_ckpt</code>) – 基础模型的保存目录。</p></li>
<li><p><strong>检查点格式</strong> (<code>ckpt_format</code>) – 定义已保存模型的格式（例如，<code>safetensors</code>）。</p></li>
<li><p><strong>模型类型</strong> (<code>model_type</code>) – 标识正在转换的 AI 模型（例如，<code>GEMMA_2B</code>）。</p></li>
<li><p><strong>推理后端</strong> (<code>backend</code>) – 定义计算后端（例如，<code>gpu</code> 或 <code>cpu</code>）。</p></li>
<li><p><strong>分词器路径</strong> (<code>vocab_model_file</code>) – 确保分词器已链接到转换后的模型。</p></li>
<li><p><strong>LoRA 检查点路径</strong> (<code>lora_ckpt</code>) – 要合并的微调后的 LoRA 权重。</p></li>
<li><p><strong>LoRA 等级</strong> (<code>lora_rank</code>) – 确保转换结果与 LoRA 配置匹配。</p></li>
<li><p><strong>输出路径</strong> — 指定最终 TFLite 模型的存储位置。</p></li>
</ul>


<h4>2️⃣ 转换模型和 LoRa 权重</h4>

<p>基础模型和经过微调的 LoRa 权重<strong>分别转换</strong>，以确保更新的灵活性。</p>

<h4>3️⃣ 保存转换后的模型</h4>

<p><strong>基础 TFLite 模型</strong>和<strong>LoRA 适配权重</strong>均以 <code>.bin</code> 文件格式存储，可在设备上动态加载。</p>

<pre><code class="python">import mediapipe as mp
from mediapipe.tasks.python.genai import converter

# Define conversion config
conversion_config = converter.ConversionConfig(
    input_ckpt="/content/gemma2b",  # Path to the original Gemma-2B model checkpoint
    ckpt_format="safetensors",  # Format of the checkpoint
    model_type="GEMMA_2B",  # Model type
    backend="gpu",  # Backend for inference (gpu or cpu)
    combine_file_only=False,  # Whether to merge files into one binary
    output_tflite_file="/content/output/gemma2b.bin",  # Path for the converted base model
    vocab_model_file="/content/gemma2b/tokenizer.model",  # Path to tokenizer vocab file
    output_dir="/content/output",  # Directory to save the converted outputs
    lora_ckpt="/content/gemma2b/lora",  # Path to the fine-tuned LoRA checkpoint
    lora_rank=8,  # Rank of the LoRA configuration
    lora_output_tflite_file="/content/output/lora.bin"  # Path for the converted LoRA weights
)

# Convert the model to TensorFlow Lite format
converter.convert_checkpoint(conversion_config)
</code></pre>

<h3>第 10 步：将转换后的模型上传到 Firebase 云存储 🚀</h3>

<p>好了，我们已经微调了 <strong>Gemma-2B</strong> 模型，并将其转换为 LiteRT 格式，现在它就放在 <strong>Colab</strong> 里。但是我们需要想办法把它导出来，对吧？有很多方法可以做到这一点——手动下载、使用 Google 云端硬盘、通过电子邮件发送（拜托，别这么做 😂）……</p>

<pre><code class="python">from google.colab import drive

# Mount your Google Drive
drive.mount('/content/drive')

# Copy the converted base model to Google Drive
!cp /content/output/gemma2b.bin /content/drive/MyDrive/gemma2b-base.bin

# Copy the converted LoRA weights to Google Drive
!cp /content/output/lora.bin /content/drive/MyDrive/gemma2b-lora.bin
</code></pre>

<p>在当前的示例中，我将模型保存到 <strong>Google 云端硬盘</strong>，但在不久的将来，我计划添加一个选项，允许用户<strong>直接将其保存到 Firebase 云存储</strong>。原因是 <strong>Firebase 云存储可以更轻松地将模型直接加载到移动设备上</strong>。你无需手动传输文件或单独下载，只需几行代码即可<strong>直接从 Firebase 获取模型</strong>到你的应用中。这将简化部署流程，使设备端 AI 的使用更加顺畅。</p>

<p>别担心——我会在下一篇文章中详细解释所有细节！</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*LZW3DfqOcGOfF7SbDcN5bg.png" alt="" /></p>

<p><strong>敬请期待！</strong> 📲🔥</p>

<h2>总结</h2>

<p>恭喜！🎉 你已成功：</p>

<ul>
<li><p>使用 <strong>LoRA</strong> 对 <strong>Gemma-2B</strong> 模型进行了微调</p></li>
<li><p>将其转换为 <strong>LiteRT（原 TensorFlow Lite）格式</strong>，以便与 MediaPipe 兼容</p></li>
<li><p>使其能够在移动和 Web 应用上进行 <strong>设备端推理</strong></p></li>
</ul>


<p>此工作流程无需依赖云基础设施，即可实现 <strong>保护隐私、经济高效的 AI</strong>。现在你可以将精心调校的模型集成到实际应用中，并根据自身需求进行进一步优化。</p>

<p>如有任何疑问，欢迎随时联系：</p>

<p>📧 <strong>邮箱：</strong> <a href="&#x6d;&#97;&#105;&#108;&#x74;&#111;&#x3a;&#100;&#101;&#x6e;&#x69;&#x73;&#111;&#x76;&#46;&#115;&#x68;&#117;&#114;&#101;&#103;&#x40;&#103;&#109;&#97;&#x69;&#x6c;&#46;&#x63;&#111;&#x6d;">&#x64;&#x65;&#x6e;&#x69;&#115;&#x6f;&#x76;&#46;&#x73;&#104;&#x75;&#114;&#101;&#x67;&#x40;&#103;&#109;&#x61;&#105;&#x6c;&#x2e;&#x63;&#x6f;&#109;</a></p>

<p>🔗 <strong>领英：</strong> <a href="https://www.linkedin.com/in/aleks-denisov/">Sasha Denisov</a></p>
]]></content>
  </entry>
  
</feed>
